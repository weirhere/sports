# Decisions log — StatSide (iOS + web)

Every product, design and architecture decision on this project, oldest first, with
the reasoning that produced it. Split out of `CLAUDE.md` on 2026-09-13, when that file
passed the 150k-character context limit and this log was 92% of it.

**Read this before changing product behavior.** The rules an agent needs up front live
in `CLAUDE.md` — this is the record of *why* the app is the way it is, and most of the
mistakes worth avoiding here are recorded as a row that already reasoned through them.
`CLAUDE.md`'s § Don'ts is the distilled version.

**Adding a row:** append one for every decision, dated the day it was made, with the
reasoning — not just the change. A row that says what changed and not why is what this
file exists to prevent. Keep the table unbroken: a blank line between rows silently
ends the markdown table, and the rows after it render as raw pipes.

**Web parity:** a PR that adds a row here must also add one to `docs/web-parity.md`
(shipped / pending / n/a / differs). `scripts/check-parity-ledger.sh` counts the rows
and fails web CI when they don't match; `[skip-parity]` in a commit message is the
escape hatch for work with no product surface.

| Date | Decision | Why |
|---|---|---|
| 2026-07-20 | Conference jump chips removed (supersedes the jump-scroll decision) | Accordions cover section navigation; the chip row was redundant. The Live toggle stays, now as its own `LiveFilterChip` |
| 2026-07-20 | Conference logos in section headers render grayscale (inverted in dark mode) | Headers are chrome, and the color budget's logo exception is team-specific (disambiguation); conference headers already carry the name. ESPN has no dark-variant conference logos, so dark mode inverts the grayscale mark |
| 2026-07-21 | Data: ESPN unofficial API for v1 | Free, no key, live scores. Risk accepted + contained via DTO layer |
| 2026-07-21 | Scope: Scores + Rankings + Game detail + Team following in v1 | Andy's call; scores screen leads |
| 2026-07-21 | Week-based navigation, not date-based | CFB's native clock; kills the empty-Tuesday problem |
| 2026-07-21 | Section order: Following → Top 25 → conferences (accordions) | Fixes ESPN's "where are my teams" failure |
| 2026-07-21 | Games duplicate across sections | Section completeness beats dedupe cleverness |
| 2026-07-21 | Conference chips jump-scroll, they don't filter | Filtering creates a mystery state |
| 2026-07-21 | One "Live" toggle is the only filter | The 3:30-Saturday use case |
| 2026-07-21 | Accordion state persists; Following + Top 25 open by default | FotMob's memory pattern |
| 2026-07-21 | Week rolls over Monday morning; Sunday still shows the completed week | Sunday is for catching up + new poll drops in place |
| 2026-07-21 | Monochrome + logos-in-color + single red live accent | Distinctive, and live state pops harder |
| 2026-07-21 | iOS 18.0 deployment target, iOS 26 SDK | Andy's call |
| 2026-07-21 | Rankings movement gets red/green (third color-budget exception) | Andy's call; movement direction reads instantly. Down reuses the live red; arrows keep it color-blind safe |
| 2026-07-21 | Conference logos render full color (supersedes the grayscale decision) | Andy's call after seeing it in practice; the color budget's logo exception now covers all logos, not just teams. Also drops the dark-mode invert hack |
| 2026-07-21 | App name: **StatSide** | Andy's call, closing open question #1. Wordmark + `CFBundleDisplayName` updated |
| 2026-07-21 | Live accent stays red (closes open question #2) | Decided from side-by-side renders; a 6 pt monochrome dot doesn't register at scroll speed, and Reduce Motion removes the pulse fallback |
| 2026-07-21 | Top 25 = any ranked participant (closes open question #3) | 2025 data: any-ranked is 15–21 games/wk, ranked-vs-ranked 3–6 — too thin for a section named Top 25 |
| 2026-07-21 | "Other" section = both-sides-unknown only | FCS visitors were double-bucketing ~48 Week-1 games into Other; an FCS visitor now stays in its host's conference section |
| 2026-07-21 | Dark mode backs conference marks with a light disc (`logoBacking`) | Navy marks (Big Ten, ACC) vanish on black; a backing disc is chrome, not color, so the budget holds |
| 2026-08-04 | Widgets + local notifications de-iceboxed (epic E7) | Apple rejected 1.0 (5) under 4.2.2 Minimum Functionality; they were the roadmap's "killer feature" anyway. Live Activities stay iced (no push story) |
| 2026-08-04 | pbxproj-edit rule amended: target/build-phase/build-setting edits allowed when adding targets, verified by lint + build; file references still never touched by hand | The widget extension is a genuinely new target; synchronized groups still handle all file membership |
| 2026-08-04 | Shared code lives in `StatSideShared/` — a second synchronized root group compiled into both app and widget targets | The alternative (exception-set deny-list on `sports/`) silently adds every future app file to the widget target |
| 2026-08-04 | App Group `group.com.andyryanweir.sports`; `FollowingStore` reads the shared suite, migrated once from standard defaults (standard copy kept) | Widget needs the follow set; migration flag lives in the suite |
| 2026-08-04 | Deep links `statside://game/{id}` / `statside://team/{id}` / `statside://teams`, scheme deliberately unregistered (no `CFBundleURLTypes`) | widgetURL and notification taps deliver to `onOpenURL` without registration; registering would force a partial Info.plist for external openers nobody needs yet |
| 2026-08-04 | Notifications: one local reminder 30 min before kickoff, app-wide bell toggle on TeamPage, permission asked contextually (bell tap or post-first-follow alert), never at launch | One reminder beats two under the 64-pending cap; per-team granularity is icebox material |
| 2026-08-04 | Haptics budget: exactly three (follow toggle, refresh success, live score change in game detail only) | Mirrors the color budget's discipline; GameRow haptics on a 60-game Saturday would machine-gun the Taptic engine |
| 2026-08-04 | UIKit import exception: `UIApplication.openNotificationSettingsURLString` for the bell's denied state | No SwiftUI equivalent exists |
| 2026-08-04 | Widget provider fetches ESPN directly (no app-written snapshot as primary) | The widget must be live at 3:30 Saturday without the app opening; WidgetKit's ~40–70/day reload budget is itself the politeness throttle (~48 requests on a full Saturday). Adds an unattended request surface to the unofficial-API risk — volume negligible |
| 2026-08-09 | Scores rows spend exactly as much date as the kick needs: "Today 3:30 PM" / "Tomorrow 3:30 PM" inside 48 hours, the bare weekday out to a week, weekday + date ("Sat, 9/5 3:30 PM") past that | On the Saturday itself — the app's whole use case — "Sat" is dead weight; but a weekday alone means *the next one*, so it lies about a game three weeks out. Scores-screen rows only; share text, widget, and schedule strings stay absolute because they outlive the moment they're generated |
| 2026-08-09 | A row under a day divider never repeats the date; `SectionAccordion` passes its existing `spansMultipleDays` down as `GameRow.dayIsLabeled`. VoiceOver always speaks the full date | Dividers and rows were both saying "Aug 29" in conference sections. The divider is the section's date mechanism, so the row defers to it — but only visually, since a VoiceOver swipe can land on a row without passing the divider |
| 2026-08-09 | Pinch on Scores collapses (in) / expands (out) every rendered section — FotMob's gesture, the app's first gesture. Fires once per pinch in `onChanged` at 0.8/1.25 magnification, persists exactly like per-header taps, day sections included, Scores only | Batch accelerator for the dozen-tap Saturday problem. No haptic (the budget of three holds; the animation is the feedback). Gesture-only: every header keeps its focusable toggle button, so nothing is pinch-gated for VoiceOver or motor-impaired users |
| 2026-08-09 | Team share = invitation framing ("Keep up with…" + record + next game) through `ShareSignOff` — the last share site to adopt it. Body never names the app; the sign-off is the only branding. No `statside://` link in shares | "Following X on StatSide" carried no link and no substance. Custom schemes aren't tappable in iMessage and there's no domain for universal links, so the store link stays the one tappable link; the unregistered-scheme decision holds |
| 2026-08-09 | Game shares attach a rendered matchup-card PNG (`GameShareCard: Transferable`, lazy async export); share text + store link move to `ShareLink(message:)` | The generic App Store card was the only visual in a shared thread. The card renders always-light at fixed type sizes (theme Font tokens bake against the process content size, so they can't be pinned via the environment) from `GameHeaderState`, the extracted detail-header derivations. Repointing the iMessage *link preview* itself would need per-game web pages — a backend — so it stays the App Store card, beneath the real game image |
| 2026-08-09 | Rows inside a day-grouped accordion drop the date entirely (`GameRow.timeOnly`) — kick time + network only. VoiceOver still speaks the full date | The accordion header already says "Saturday, Aug 29"; even the weekday the divider rule keeps is dead weight when the whole section is one day |
| 2026-08-09 | App-wide search is a native search tab (`Tab(role: .search)`) — the FotMob-style circular magnifier beside the floating tab bar on iOS 26, a plain fourth tab on iOS 18; corpus = shared team directory + FBS conferences + the selected week's already-loaded games (no new fetch surface); conference results emit a `Router` intent (`pendingConferenceId`), never a hardcoded destination | Andy's call after seeing per-header magnifiers (superseded same day): FotMob's bottom placement beats three header buttons, and the OS renders it. Time-to-answer beats corpus breadth, and the intent indirection keeps search decoupled from the in-flight conference-page worktree. Enabler: `ScoreboardStore` + new `TeamDirectoryStore` hoisted to RootView's environment — single fetch, polling lifecycle owned by the scene, not a tab |
| 2026-08-09 | Conference standings via a new `conferenceStandings()` protocol method; `fbsConferences()` and `ConferenceTeams` untouched | Same endpoint, second mapper: browse keeps its alphabetical contract, ConferencePage keeps ESPN's standings order (not derivable from records — tiebreakers). The standings mapper keeps empty conferences so the page can say "Standings TBA" offseason |
| 2026-08-09 | Conference follows are a second set on `FollowingStore` (App Group suite, new key); v1 scope is the Scores Following section only — widget and kickoff notifications stay team-driven (closes open question #5) | One store owns "do I follow this game"; a conference has no single "next game" for the widget, and ~9 reminders/week would evict team reminders under the nearest-24 cap. Conference follows count toward `followsAnyone`; an FCS visitor's game joins Following via its FBS host's conference |
| 2026-08-09 | Conference accordion headers gain a trailing standings button (`ConferenceStandingsLink`); the header surface stays the expand/collapse toggle; context menu + VoiceOver custom action mirror the GameRow precedent | Two side-by-side buttons beat gesture cleverness — the toggle's whole-width promise is never diluted, and the small trailing target gets redundant accessible paths |
| 2026-08-09 | Scores and Teams stacks switched typed paths (`[Game]`/`[Team]`) to `NavigationPath` | A homogeneous typed path can't push a second destination type; `NavigationPath` keeps every existing `NavigationLink(value:)` and the deep-link pending-id resolution intact. Rankings keeps its view-builder links but registers the same value destinations. (Main reached the same conclusion independently for game-detail team links — the merge kept `NavigationPath`) |
| 2026-08-10 | Rankings is the tables hub: CONFERENCES card (one row per conference, leader teaser, follow star, followed pinned first) below the poll — no 4th tab | Andy's call after weighing a Conferences tab: a tab would be a menu, not a destination, and Rankings already answers "who's good" — now at both levels. The teaser hides preseason (a 0-0 "leader" is last season's carry-over); a standings fetch miss hides the card, never errors over the poll |
| 2026-08-10 | Rankings root is a FotMob-Leagues-style row list: Top 25 row (→ `PollScreen`) above the conference rows (supersedes the poll-stays-inline-at-root first cut, same day) | Andy's call from the FotMob reference: 25 inline rank rows buried the conference list below the fold where nobody would discover it. The poll costs one obvious tap now; the whole hub is visible in one screen |
| 2026-08-10 | Search conference results land on ConferencePage — the Teams resolver pushes the standings page instead of expanding/scrolling the browse list (takes over the seam the search decision left open) | A conference's page beats its roster mid-list as a search destination. Resolution needs no loaded data (the page fetches its own standings), so the browse-list scroll machinery (`scrolledSection`, `scrollPosition`, the 450ms layout wait) came out entirely |
| 2026-08-16 | Team logos switch to ESPN's `500-dark` variants in dark mode, app + widget, via URL derivation (`URL.darkTeamLogoVariant`); missing dark asset → light fallback, with the 404 negative-cached in `LogoCache` | Black artwork (Ohio State's lettering) vanishes on the black background. The scoreboard payload ships no `logos[]` array, so the dark URL can't be decoded — derivation covers every surface from one helper. Share card stays always-light; conference marks derive nothing and keep `logoBacking` (no verified dark conference assets) |
| 2026-08-16 | Widget families become medium + large (small removed; lock-screen accessory stays), both rendering one FotMob-style stacked list: ★ Following header, 8pt-padded rows, medium 2 games / large 5 + updated/as-of footer, provider limit 5 for all families. Pre-game rows drop scores entirely (no 0–0, no dashes) and the status is time-only — weekday + time, date joining at 7+ days out, no network | Andy's call from FotMob side-by-sides: the 3-row medium was too tight, and FotMob's medium/large split fits the "list of my games" promise better than a single-game small. A 0–0 pre-game score is noise pretending to be signal, and the network doesn't earn its width in a widget row. Placed small widgets go blank — accepted |
| 2026-08-17 | Horizontal swipe on the Scores content pages to the adjacent week (left = next, right = previous), with a directional push slide; week-strip chip taps share the direction rule; season ends are a quiet no-op | Andy's call; the strip is a top-of-screen reach on a couch Saturday, and swiping the content is the FotMob-native way to walk weeks. Gesture-only accelerator like the pinch — chips keep every week tappable — and no haptic, so both budgets hold |
| 2026-08-17 | Unannounced kickoffs render "TBD", never the placeholder clock: ESPN's `timeValid: false` (date parked at midnight ET) maps to `Game.timeTBD`, honored by every time-rendering surface — schedule rows, scores rows, widget, shares, share card, Siri line — while the day part stays (it's real). Kickoff reminders skip TBD games | Team pages were showing "12:00 AM" for every un-timed future game; a reminder would have fired at 11:30 PM for nothing. Day-only degradation beats dropping the row's date, and the kickoff-encoding request id already handles TBD-becomes-real |
| 2026-08-17 | Team pages get a season selector: a `SeasonMenuChip` (extracted from the Scores header — one capsule, two call sites) trailing the SCHEDULE header, 2014 CFP-era floor. An explicit year fetches exactly that season — the silent year−1 fallback stays nil-year-only; `TeamSchedule.year` comes from ESPN's `requestedSeason`; the mapper trusts `recordSummary`/`standingSummary` only when `season.year == requestedSeason.year` (they always track ESPN's *current* season), so past seasons show a record derived from final results and no standing line; shares stay current-season; fetched seasons cache per page visit | Andy's ask. The Scores chip set the pattern and the floor; a user who picked 2019 must never silently get 2018. "1st in SEC" isn't derivable from games (tiebreakers), so honesty beats completeness for past standings — and the offseason page now shows last season's real record instead of ESPN's 0-0 |
| 2026-08-23 | Medium family gets compact metrics: masthead scales to 16pt star + new `sectionHeaderProminentCompact` (14 heavy), header gap 12 → 8, logos 20 → 16; card padding stays 8 in both families | The masthead + two 60pt cards overflow medium's ~138pt content box, and WidgetKit was crushing the padding. Consistent padding beats consistent type — scale the chrome, never squeeze the breathing room |
| 2026-08-23 | Widget header grows into a masthead: 24pt star, "Following" at new `sectionHeaderProminent` (16 heavy, −2% tracking), header-to-list gap tightens 16 → 12 | Andy's Figma remix, third pass: the widget has no nav chrome, so its header carries the identity weight a screen title would |
| 2026-08-23 | Widget status column gains a second line: the TV network under the day/time (`metaMedium`, right-aligned), pre-game and live only — supersedes the widget's no-network rule from 2026-08-16. Snapshot carries it optionally so old blobs still decode | Andy's Figma remix, second pass: the card rows have the vertical room the old one-line rows lacked, and "where do I watch" is the pre-game row's second question. Finals stay quiet — nothing left to tune into |
| 2026-08-23 | Widget rows become bgHeader cards (8pt padding, 8pt continuous radius, 2pt apart, stretching to split leftover height), dividers dropped; abbreviations move to `chip`/new `chipEmphasis` (14 medium/semibold), records + pre-game times to new `metaMedium`; large capacity drops 5 → 4 (provider limit follows), mock's 16pt header gap and 12pt footer gap adopted | Andy's Figma remix (StatSide file, ↳ Widget page): cards read as one game each at a glance better than hairlines, and five card rows were too cramped — Andy's call after seeing it installed. Stretch-to-fill is the mock's flex rows: no dead gap above the footer |
| 2026-08-23 | `textPrimary` light mode softens to 0.10 (#1A1A1A), off true black; dark mode stays pure white. Mirrored as the `text/primary` Figma variable (Theme collection, StatSide file) | Andy's call: true black on white causes eye strain. Contrast stays ~17:1 on bgPrimary, ~15:1 on bgRecessed. Backgrounds keep true black/white — the budget's mono ramp is otherwise unchanged |
| 2026-08-24 | TeamPage header gains its own conference line (`TeamConferenceLink`, replacing the standing line): conference mark + name, the season's placement folded in ("Big Ten · 1st") when a standing exists (hidden at offseason 0-0, past seasons, CFBD) — one tap target → ConferencePage's full standings. The id resolves schedule-payload-first (`team.groups`, now decoded; deliberately outside the summaries trust rule) → pushed team → `TeamDirectoryStore` lookup | The only conference link on a team page was the standing string, which vanished offseason, on past seasons, and for teams pushed from Rankings/game detail (nil `conferenceId`). Andy's call after seeing a merged record-row version planned: the conference reads as its own line, and "Big Ten · 1st" avoids double-saying the conference the standing string would. CFBD parity came free via `Conference.id(forCFBDName:)` |
| 2026-08-25 | TeamPage gets a team-color hero header — the color budget's **fourth exception**: nav bar + header paint in ESPN's `team.color` (schedule payload, newly decoded), white identity ink and a white logo disc (black ink on light colors via a luminance guard), bell + season chip + follow pill move into the hero, and a Games / Standings tab pair lands (Standings = the conference table with the team's own row anchored). Monochrome fallback when the payload carries no color; Overview/News tabs not built (no data source). `TeamConferenceLink` retired — the hero's conference · record line replaces it | Andy's call reviewing the P1 Figma variation, second pass ("make sure we get all the updates made") — FotMob's hero reads instantly as the team's home; chrome stays mono everywhere else |
| 2026-08-25 | Schedule results render as green/red score chips (`rankUp`/`rankDown` tokens, the W/L letter dropped); row logos grow to 32 pt; card headers go table-style (`CardHeader`) | Andy's Figma edit, same review. Mine-first score order keeps a non-color signal and VoiceOver still speaks won/lost; `bgPrimary` chip ink inverts with the scheme so dark mode's brighter green/red keep contrast |
| 2026-08-25 | Day dividers removed; the row's day line carries weekday + date from two days out ("Sat, 8/29" — Today/Tomorrow keep their window). Date grouping is the exception: rows stay time-only and the day accordion header pins to the top while its games scroll. TeamPage schedule rows push game detail (supersedes the 2026-08-09 divider/date-suppression pair) | Andy's calls: the divider was "just noise", and one consistent per-row pattern beats two date mechanisms; in date mode the sticky header does the date's job |
| 2026-08-25 | Conference accordion headers split into two surfaces: mark + name push the conference page, the rest of the row toggles (supersedes the whole-width-toggle promise and retires Scores' trailing standings icon — the name button inherits its accessibility label). Non-conference headers keep the whole row | Andy's call: "tapping the conference name should take you to the conference page, not expand the accordion" |
| 2026-08-25 | GameRow adopts the 2240 Figma matchup spec: 13-medium row scale (new `row*` tokens), rank after the name, records upcoming-only beside a hairline-divided 80pt status column, finals show score-only with sentence-case "Final". Week swipe becomes interactive (content tracks the finger, skeleton pane rides in, commit on distance or flick); chip taps keep the push | Andy's Figma + "swipe should start the moment the thumb moves". Live rows still spend weight; VoiceOver sentences unchanged |
| 2026-08-25 | ConferencePage moves onto the TeamPage hero template (monochrome — conferences ship no color); standings tables everywhere gain a place column + shared captions via `StandingsList`; hero tabs follow the Figma header component specs exactly (bold 14, 40pt gap, 14pt padding, 3pt bar, 50% inactive) | Andy's calls, same review session. One template for entity pages, one table language for standings |
| 2026-08-29 | Live rows show the network under the clock (Scores status column + accessibility-size layout), and the live game-detail header gains a quiet network line — the widget's pre+live rule adopted app-side. Finals stay quiet everywhere; the share card's status line is deliberately untouched | Josh Vertucci feedback: "I want to know what the scores are and quickly know where I can watch." A live game's detail page previously showed no network at all (the info card is pre-game-only) |
| 2026-08-29 | Scores grouping defaults to by-date (`.date`); conference grouping stays one chip tap away, stored choices untouched | Josh Vertucci: "'by date' takes priority — I want to know what's 'now' first." Users who never toggled the chip land on by-date after updating; the Saturday sort order lives on inside each day |
| 2026-08-29 | Scores gains a slate filter (supersedes "one Live toggle is the only filter"): a funnel chip opens an ESPN-style sheet — All / Top 25 / any conference — filtering every section in both groupings, composing with Live. Session-only, never persisted; the active chip fills and names the selection | Josh Vertucci's ESPN habit, and the by-date default left no conference view in date mode. The July "chips don't filter" objection was about unlabeled mystery state — a labeled chip plus reset-on-launch is the antidote, and the full slate always greets a fresh launch |
| 2026-08-29 | The filter sheet becomes the Scores view-options sheet: grouping (segmented By date / By conference) and the season picker move in from the header, which slims to wordmark + funnel + Live (same-day follow-up; supersedes the By-date-chip and season-chip header placements). The funnel chip's label carries filter + past season ("SEC · 2019"); grouping stays unlabeled — the section headers on screen already say which view is active | Andy: "the top is getting a little crowded" — five chips wrapped the wordmark and their own labels. One door for every view decision, and past seasons stay surfaced so a 2019 slate is never mistaken for this week |
| 2026-08-29 | The Live chip becomes a permanent header fixture, left of the funnel (supersedes the appears-only-while-live rule). Toggling it with nothing live lands on the narrowed-slate empty state ("No live games right now" / "No live SEC games right now") with one Show-all-games button that clears both live and the conference filter | Andy's call, same review: a stable home beats a chip that materializes mid-Saturday, and an explanatory empty state beats hiding the toggle |
| 2026-08-29 | Header controls adopt FotMob's grouped-capsule language: one bgElevated capsule holds the Live pill and the funnel segment (FotMob's calendar slot), chipEmphasis labels, ~52pt group, active segments fill textPrimary. Function, accessibility, and the color budget unchanged — the red dot stays Live's only color | Andy, from the FotMob reference: "I really like how fotmob does this in terms of tap target areas and larger buttons" |
| 2026-08-29 | Liquid Glass adopted for custom chrome, chrome only: the header control cluster, the selected week chip (ink-tinted glass), and `SeasonMenuChip` (both variants — the hero scrim becomes glass over team color) via `Theme/GlassSurface.swift` helpers, every site `#available(iOS 26)`-guarded with today's flat fills as the 18.0-floor fallback. Content cards, rows, and accordions stay ink — glass never stacks on glass, so the pills inside the glass group capsule keep their solid fills. System chrome (tab bar, search circle, sheets) was already glass from the iOS 26 SDK | Andy: "give the whole app a liquid glass effect" — scoped to the floating control layer per Apple's guidance; glassing scrolling content would shimmer everything and fight the FotMob density target |
| 2026-08-29 | ConferencePage gains a Games tab (TeamPage's tab pair, monochrome ink): the season's full conference slate as one card per week, postseason last, rows are `GameRow` pushing game detail; the season chip drives games and standings together, cached per year. Data: new `conferenceGames(conferenceId:year:)` on `ScoresProviding` — ESPN's scoreboard with `groups={confId}&dates={year}` returns the whole season in one call (verified live 2026-08-29, types 2+3 with per-event weeks); `Game.seasonType` newly mapped so a title game's restarted week number can't land it in "Week 1". Entry default: Games tab, except standings-anchored pushes (a team's standing line) which land on Standings; the page always opens at the top, hero in view (a scroll-to-current-week first cut was reverted same day — Andy: "it still should load at the top of the page") | Andy: "rankings conference pages has standings but no games." One fetch per (conference, season) keeps the polite-guest rule — no polling on this page |
| 2026-08-29 | The Scores filters persist across launches (supersedes the same-day session-only call): `liveOnly` and `scoreFilter` move to `UIStateStore` (App-standard defaults, stored-property pattern, `ui.liveOnly` / `ui.scoreFilter` with a token encoding) and `sections()` takes them as parameters like the grouping. Season selection stays session-scoped — it's time navigation, not a preference. UI tests pin both via the argument domain | Andy, after living with it: "you should be able to customize it to the way you want it and keep it even after a new session begins." The labeled chips and explanatory empty states are what made persistence safe — the July mystery-state objection no longer applies |
| 2026-08-29 | Turning the Live filter on navigates to where live games are: the current season's rollover week (`ScoreboardStore.currentWeekSlot`, retained from initial load; `selectCurrentWeek()` reapplies the rollover rule when coming back from a past season). In-season the jump slides like a week-chip tap; turning the filter off stays put | Andy: "the user will be navigated to the relevant week the live games are happening on" — filtering a future week to an empty screen answers the wrong question |
| 2026-08-29 | Team-page live games read live: the lead card retitles **Current game** while in progress and its body becomes the Scores `GameRow` itself (per-side scores, clock + network status column — "just reuse that same component"); TeamPage merges the scoreboard's fresher copy of a schedule game (`fresher(_:)`, optional-environment `ScoreboardStore`) into the card and schedule rows — the schedule payload carries no live scores or clock. Where scores still haven't landed, rows show "Live", never a dashed non-score; VoiceOver says "live now". Standings tabs (Team and Conference pages) drop the redundant "Standings" card header, and the team name fades into the nav bar's principal slot once the hero title scrolls under (`onScrollGeometryChange`, iOS 18) | Andy's live-Saturday review: NDSU's card showed "● –-–" mid-game, the tab + header double-said Standings, and a scrolled team page lost its identity |
| 2026-08-29 | The live accent turns green (supersedes "live accent stays red"): `liveAccent = rankUp`, one green app-wide; `rankDown` keeps its own red, still the app's only red. Every live surface follows from the token | Andy's call during the live-Saturday session |
| 2026-08-29 | Standings rows badge live fortunes: a team playing right now gets a dot — green winning, red losing, gray tied (`Game.liveResult(for:)`, `StandingsList.liveGames` from the environment scoreboard; current season only; VoiceOver speaks "playing now, winning"). The movement pair's colors put to live use | Andy's ask, watching live games against 0-0 tables |
| 2026-08-29 | Team and Conference pages: swipe between the Games and Standings tabs (threshold swipe, push slide, buttons keep everything reachable — the week swipe's sibling); the hero color extends behind a transparent nav bar at rest so the system's Liquid Glass buttons refract team color, FotMob-style (bar goes solid exactly when the inline title arrives; the color rides inside the scroll — a fixed background band bled green behind content and was reverted same day) | Andy's calls from the FotMob reference |
| 2026-08-29 | Conference-mode accordions clip their collapsing rows (`.clipped()` on the card VStack) | Collapsing rows painted over the next section's header until the animation settled |
| 2026-08-29 | The "FCS conference bug" investigation concluded no bug: ESPN's FBS standings confirm NDSU → Mountain West and Sacramento State → MAC as real 2026 realignment (their schedules corroborate), and truly-FCS ids (Tennessee State's 179) already degrade to no conference. What WAS broken: TeamPage's FCS veto keyed on "directory knows the team", and ESPN still ships the Sun Belt with zero standings entries — every Sun Belt page was silently losing its conference line and Standings tab. The veto is now disproof-shaped: an unknown team's claim falls only when the claimed conference has a roster that excludes it | Chased from Andy's "fix the FCS conference bug"; a membership-scrub hardening was designed and rejected when the Sun Belt hole surfaced — it would have stripped every Sun Belt game |
| 2026-08-29 | Hero-bar controls get contrast tints, still Liquid Glass: back and share become custom dark-tinted glass circles with white ink (system glass wrapper hidden on iOS 26 via `sharedBackgroundVisibility(.hidden)`; system back button hidden, swipe-back kept), and `SeasonMenuChip`'s onDark variant tints its glass dark. Untinted glass over team color washed out | Andy: "still very hard to make out... higher color contrast but still liquid glass" |
| 2026-08-31 | Hero color reaches the status-bar strip via `HeroTopBand` (supersedes the `-1000`pt in-content background inflation): a top-anchored band outside the ScrollView whose height tracks scroll offset — full top inset at rest, growing through the bounce, zero before the solid-toolbar handoff. ConferencePage carries the monochrome variant | Andy's bug report: the strip behind the glass back/share buttons showed plain gray — the inflated background lived inside the ScrollView, which clips its content. The fixed-band revert's objection (color bleeding behind scrolled content) can't recur: the band's bottom edge is the hero's top edge by construction |
| 2026-08-31 | Entity-page tab slides commit the push edge in a transaction BEFORE the tab switch (the week swipe's split), edge derived from tab ordinals; `.geometryGroup()` on the switched pane; `LogoCache` gains a synchronous `nonisolated` NSCache front read by `LogoImage` for first-frame paints; tab rows extracted to `HeroTabBar` | Setting edge + tab together made the outgoing pane resolve `.push` against the stale edge, so slide directions alternated wrongly; `.id(tab)` recreated every LogoImage whose actor-hop cache lookup landed a frame late — marks sat frozen as blank discs while cards moved |
| 2026-08-31 | The week swipe's incoming pane shows the adjacent week's real slate (supersedes the skeleton pane): `ScoreboardStore` caches each fetched week per season (`WeekSlot.id` key, cleared on season switch) and prefetches the ±1 neighbors once after every settle — never polled; the pane renders through `sections(from:)` with grouping/filters/expansion shared, interaction disabled, skeleton only before the first warm. `select(week:)` seeds from the cache before its fresh fetch | Andy's FotMob comparison: mid-swipe blankness answers nothing when the data is one request away. Seeding also killed the blank flash on swipe commits and chip taps. ~1 extra request per week change once warm keeps the polite-guest rule |
| 2026-08-31 | The hero's conference line drops the record — conference name only (amends the 2026-08-24 "Big Ten · 1st"/record line) | Andy's call after the Overview tab landed: the Record card now owns the numbers, and the hero was double-saying them one scroll above |
| 2026-08-31 | Dark mode adopts light mode's elevation logic (FotMob reference): nearer surfaces are lighter in **both** modes — dark ramp is now page/chrome 0.00 → card 0.11 → header tint/highlight 0.15 → elevated 0.17, replacing the numerically-inverted ramp (cards were pure black *below* an 0.08 page). New `bgCard` token (light 1.00 / dark 0.11) carries the card role; `bgPrimary` is chrome + inverted ink only (nav bars, screen roots, widget container, chip ink) and stays black in dark. Light values untouched everywhere; widget and StandingsList anchored-row highlight fixed by the retune alone | Andy's call with FotMob dark screenshots: "everything is elevation… things closer to the surface should be lighter." Cards had no dark elevation signal at all — the fixed black cardSurface shadow is invisible on black |
| 2026-08-31 | TeamPage gains an Overview tab — first and default: the Games tab's Next game / Current game card (kept on Games too) plus `TeamRecordCard` (conference + overall W-L from the standings payload, now fetched at page level). Past seasons: overall only, via the derived record; no conference → overall only; preseason hides the card on the 0-0 gate. Supersedes the "Overview/News not built (no data source)" note — standings are the source; News still has none | Andy's ask. The record split was already decoded (`vsconf`/`total`) and unused on the page; the tab row now always renders since Overview + Games always exist |
| 2026-08-31 | Entity-page headers paint `bgCard` — game, team, and conference pages plus their nav bars — and the **team-color hero retires** (supersedes the 2026-08-25 fourth exception; the color budget is back to three). `colorHex`/`Color(espnHex:)`/the luminance guard deleted; the tinted-glass bar circles and TeamPage's transparent-until-scrolled bar retire with the paint they existed for (system back button returns). Team logos render bare on the header (`500-dark` carries dark mode; the white disc dropped); conference marks keep the dark-only `logoBacking` disc — ESPN serves no `ncaa_conf/500-dark` variants (probed live, all 404). `HeroTabBar` drops the 3pt underline: opacity alone separates active from inactive | Andy's design review against FotMob's league page: "all headers should have the same color as the cards" |
| 2026-08-31 | Hero controls consolidate onto the toolbar row (FotMob's pattern): TeamPage puts bell + follow pill + share beside the system back button, ConferencePage its follow pill; the **season chip moves into the tab panes** — trailing above the first card on Games and Standings (Overview stays chipless). TeamPage's Standings tab gains past seasons via `conferenceStandings(year:)`, cached per year like ConferencePage; the Overview record card stays pinned to the current season, and live standings dots gate on it. Known pre-existing edge: divisional seasons (2019 AAC) nest entries under division children the mapper doesn't read → "Standings TBA" | Andy's calls, same review — five controls didn't fit one row, and the chip belongs with the content it scopes |
| 2026-08-31 | ConferencePage tabs reorder: Standings first and the entry default, Games second (amends the Games-default entry rule from the Games-tab decision; standings-anchored pushes land where they always did) | Andy, same review — a conference page's first question is the table, FotMob's Leagues order |
| 2026-08-31 | Game-detail scores go FotMob-centered: once a game has scores (live or final), one large "7 – 33" line sits between the logos (new `scoreHero`/`scoreHeroLive` 34pt tokens, `.fixedSize()` so the equal-thirds header can't wrap it), the status line demoted beneath it; the per-side score under each logo retires, with the loser's number keeping its muted ink inside the combined line. VoiceOver team links still speak "team + score" | Andy's FotMob reference: the final score is the page's headline, not a footnote under each logo |
| 2026-08-31 | Live clock breaks become a domain fact: `GameStatus.live` carries a `LivePhase` (`playing`/`halftime`/`endOfPeriod`) mapped once at the ESPN boundary from the status type name (`STATUS_HALFTIME`/`STATUS_END_PERIOD` arrive as `state: "in"` with the clock parked at 0:00), and every live surface renders through one shared `GameStatus.liveStatusText` — "Q3 5:24" / "Half" / "End Q1" — replacing five per-surface `[quarter, clock]` joins. VoiceOver speaks "halftime"/"end of 3rd quarter". CFBD infers halftime from a parked Q2 clock only | The E5 live-pass finding: halftime rendered "Q2 0:00" on all five surfaces because every renderer re-derived the line and ignored `detail`. A boundary mapping + single formatter keeps them from drifting again |
| 2026-08-31 | The 2026-08-29 self-popping live detail closes as an **XCUITest/iOS 26.5 ghost activation, not an app bug**: per-second accessibility querying during a hold occasionally fires a nav control with no touch (back button on the Aug-29 tree → the observed pop; a header team link on main → a TeamPage push). Ships instead: `FixtureScoresClient` (DEBUG-only scripted backend, `-data.provider fixture` + `poll.interval`), the `LiveDetailHoldUITests` regression suite, gated `GHOST_REPRO=1` diagnostics, and `scoresnav` path-depth logging in ScoresScreen | Reproduced on both trees with screen recordings; hand-driven and query-free holds never fire (0 in 8+ min vs 3/3 on the old tree under querying). The app can't defend against ghost taps of its own back button |
| 2026-08-31 | A pre-game summary never demotes a live scoreboard snapshot: `GameHeaderState.status` merges the two (kickoff doesn't un-happen; final/postponed still win), and every header surface, the share card, and the detail's poll gate render from the merge | Found via the fixture: one flaky pre summary for a live game froze the detail mid-drive — header demoted to pre AND polling cancelled for good, since a dead poll loop is the only thing that refetches the summary. `GameHeaderStateTests` |
| 2026-08-31 | The Leaders card goes FotMob two-sided: each category is a centered label with the two leaders anchored to their header sides — away left-aligned on the left, home right-aligned on the right — 40pt headshot discs on the outer edges wearing a 15pt team mark (the abbreviation column retired; alignment + badge carry team identity, VoiceOver speaks the team location). ESPN's `athlete.headshot` newly decoded; CFBD leaders stay photo-less (quiet disc); accessibility sizes keep stacked abbreviated rows. `LogoImage` gains a `.fill` content mode for cropped photos | Andy's FotMob reference ("if the player plays for the team on the left in the game header, show that player on the left"). Headshots ride the logo exception's rationale — content imagery in color, chrome stays mono |
| 2026-09-01 | The Scores sections pipeline memoizes in `ScoreboardStore` (keyed on full game content, bounded), sorts once up front instead of per-section, and `fetchSelectedWeek` equality-guards every store write | E5 performance pass, static half: ScoresScreen's body re-evaluates on every frame of the interactive week drag and asked for the full pipeline up to three times each, and a no-change 30s poll tick invalidated the whole scores tree (@Observable notifies on every set). Content keys make stale sections structurally impossible — a clock-only poll change still invalidates |
| 2026-09-01 | Overnight agent loop adopted: agent sessions work the backlog continuously — implement on the agent branch, PR, CI green, merge, reset, repeat — gated by new GitHub Actions CI (newest-Xcode build + `sportsTests` on `macos-latest`; UI tests stay local, they drive the live ESPN API). Nothing agent-authored merges on a red or absent check | Andy's call ("I wanna test running agents overnight"). Remote sessions have no Xcode, so CI is the loop's only "build it" — it had to exist before the first unattended merge |
| 2026-09-01 | The Sunday rollover rule tie-breaks by ESPN's current slot: among the week slots containing yesterday, ESPN's `(type, value)` wins, first-containing otherwise (supersedes plain first-containing) | E5 edge-case pass: Bowls (Dec 13 → Jan 28) and CFP (Dec 18 → Jan 28) overlap for the whole playoff, so first-containing pinned every playoff Sunday to Bowls. September is untouched — ESPN's flipped-forward week never contains yesterday, so the completed-week pin still fires |
| 2026-09-03 | `main` is protected: force-pushes and deletion blocked, a PR required (0 approvals — the author merges their own), and both CI checks required green. Admins are exempt, so Andy can still push straight to main; agent sessions can't. Both workflows drop their path filters — a required check that path-filters itself out of a PR never reports, which blocks the PR permanently instead of passing it | The repo went public, so standard runners are free and unlimited — the wasted half of every unfiltered run costs nothing. Turns the overnight loop's "nothing merges on a red or absent check" rule from convention into enforcement |
| 2026-09-05 | Game detail becomes tabbed — **Summary / Box score**, two tabs not three: Summary keeps every existing card in its existing order (Scoring included), and the tab row is hidden entirely when there's no player box score | Josh Vertucci: *"the one thing I'm obsessed with looking at is box scores… that's where I get a lot of context."* Splitting Scoring or Drives into a third tab would have moved a primary surface off the default tab to no one's benefit; hiding the row when `boxScore` is empty keeps pre-kick, CFBD, and player-stat-less games pixel-identical to before |
| 2026-09-05 | Box-score columns are carried from the payload per response, never named in code (`BoxScore.Category.columns` from ESPN's `labels[]`) | Verified live: a **live** `passing` group ships five columns and the **same group ships six once final** (QBR only lands at the end). Any hardcoded schema misaligns every row mid-game — the one thing a stats table must never do. Rows and totals whose count doesn't match the header are dropped rather than rendered |
| 2026-09-05 | Scoring rows say whose points those were three ways: the team's mark leads the row, the scoring side's number takes the weight in the running score, and the row gains its first VoiceOver label | Josh Vertucci couldn't tell who was scoring and was matching player names against the Leaders card to find out. Weight, not color — the budget stays at three. The emphasized number is what survives an unfamiliar FCS mark, and VoiceOver had the identical problem the sighted row did |
| 2026-09-05 | **The day replaces the week as the Scores screen's unit of time** (supersedes "week is the unit of time, not the day" and the never-a-date-scroller don't): a `DayStrip` of the season's days, swipe = ±1 day, a leading Today chip to jump home. Bounds come from `SeasonSpan` — a derived rule (August → the last league's rollover month), not a fetch | Andy's call. The week principle assumed one league. College football's Week 2 and the NFL's are different date ranges, and its single "Bowls" slot swallows four NFL playoff rounds whole — a week strip can only ever be honest about one league, and the app now shows both at once. A day is the only unit every league agrees on, and the one every future sport will |
| 2026-09-05 | **The league selector retires; leagues become accordions** on the day's slate — Following pinned first (cross-league), then one section per league, `GameSection.league` carrying the identity. The conference stack and the day sections both come off Scores; the breakdown by conference lives on Tables | Andy: "one accordion for NCAAF and one for NFL… a much more scalable solution." A segmented control shows one league at a time and grows a segment per sport; stacked accordions show them all and cost one row each. Sections stay complete — a followed game is in Following *and* its league |
| 2026-09-05 | Section assembly moves from `ScoreboardStore` up to `LeagueScoreboards`: the store is now one league's games held a day at a time, and the page's sections are assembled across leagues. `ScoresGrouping` and the grouping control retire with it | The sections span leagues now, so no single-league store can build them. Drops the sections memo with it — the pipeline is a filter and two buckets, not the three-way sort it replaced |
| 2026-09-05 | Scores fetches a **five-day `dates=` window** per league (`scoreboard(days:divisions:)`), centred on the shown day; the inner three days are recorded as loaded. One request covers the day and both swipe neighbours, so the prefetch and the week cache retire | Verified live 2026-09-05: `dates=20260904-20260908` works for both leagues and past seasons (a 2019 range returns 2019 games), but returns **no calendar** and pins `season.year` to the current season — so the strip's bounds are derived, not fetched. ESPN reads `dates=` on the Eastern clock, and the two-day margin absorbs the ET-to-local offset for every time zone; the outer days stay unrecorded so a half-slate can't pass for a whole one |
| 2026-09-05 | The app never opens on a dead screen: when the day it landed on is empty, `firstDayWithGames` probes forward in fortnights and moves there. Only ever on a day the *app* chose — launch and season switches; a day the user picked stays picked | An August Tuesday, and a past season selected from the funnel (which lands on the season's nominal August start, weeks before anyone plays). An app that slides out from under a deliberate tap is a worse bug than an empty Tuesday |
| 2026-09-05 | The slate filter narrows the league sections and **leaves Following alone**; Live composes with everything. A filter also hides the leagues it can't speak for outright rather than emptying them | Live is a state, not a scope. Narrowing "my games" to the SEC would silently empty the section for a Michigan fan — the mystery state the labeled chip exists to avoid — and "SEC" is not a question the NFL's slate can answer |
| 2026-09-05 | The Scores league scope retires app-wide with the selector: `uiState.league`, the cold-launch live-league auto-pick, and `followedGamesElsewhere` all go. Search's ranking tiebreak becomes `FollowingStore.preferredLeague` — the league you follow most, nil on a tie | Nothing has to be picked when both leagues are on the page. The tiebreak was the scope's only other consumer, and "the league you follow most" is a better signal than "the tab you last tapped" |
| 2026-09-05 | The tables hub drops its league selector for one accordion per league — College Football (Top 25 row, then its conferences) and NFL (AFC, NFC), each a Teams-browse card with persisted collapse state. Tables is now the one screen not scoped to the app-wide league; Scores keeps the selector, and following it no longer changes the league there. Following leads as before, now spanning both leagues in one card | Andy's call: a segmented control shows one league at a time and grows a segment per sport, while stacked accordions show them all and cost one row each — the scalable shape when a third league arrives. "Who's good" has an answer per league and they fit on one page, so scoping the hub was hiding half the answer behind a tap. The Top 25 row moves inside College Football (a poll row floating above two leagues belongs to neither), and Following's row ids are league-qualified — the SEC and the AFC are both group id 8 |
| 2026-09-05 | A divisional conference is one row in the tables hub — "Sun Belt", not "Sun Belt - East" and "Sun Belt - West" — via `[ConferenceStandings].foldingDivisions()`, which merges a parent's divisions, re-applies the mappers' tier sort (an unknown division id sorts last; its conference doesn't), and leaves divisions whose parent we can't name unfolded. A folded table is marked `spansDivisions` and reports no `leader`, so no row teases a division leader as the conference's | The hub names conferences; the standings still table each division, because that order encodes tiebreakers (2026-09-05's divisional decision is unchanged). Folding also restores the conference's own mark and follow id — the division rows carried the football-glyph fallback and would have followed half a conference |
| 2026-09-05 | The week-rollover machinery retires with the week strip: `WeekLogic.defaultSelection`, its private `lastGameDay`, and `League.completedWeekWeekdays` are deleted with their tests (supersedes the 2026-07-21 Monday-rollover rule, the NFL's Monday/Tuesday pin, and the 2026-09-01 Bowls/CFP tie-break). `WeekSlot` stays — `Scoreboard.weeks` is still decoded, and `CFBDClient` still files a day range against a season | The day strip left the rule with zero callers, and it can't come back unchanged: a week strip is only ever honest about one league at a time, so a future week surface would be per-league and re-derived. The expensive part — why several slots can contain yesterday during the playoff — is written down in the 2026-09-01 entry above, so deleting the code doesn't delete the reasoning |
| 2026-09-05 | A divisional conference's page keeps its divisions as separate tables — one card each, headed "East"/"West" (the conference's name stripped off ESPN's "Sun Belt - East"), each ranked from 1 — on ConferencePage and TeamPage's Standings tab alike; the hero count still sums them ("14 teams"). The top-two championship cut is suppressed whenever the tables are divisions, whatever `titleGameIsTopTwo` says for that season | Andy's call, closing the wart the hub fold surfaced: the merged table numbered teams 1 through 14 across two divisions ESPN never ranked against each other — the tiebreaker guesswork the standings contract forbids, printed as a place column. "Divisional" is read off the payload (`parentId`), not off our own table, so a conference that drops its divisions needs no code change; and in a divisional format the division winners meet, so a per-division top-two footnote would claim the wrong thing |
| 2026-09-05 | ConferencePage's Games tab gains a team filter — a menu chip leading the pane's control row, "All teams" or one member, narrowing the slate before the week grouping so filtered weeks vanish rather than empty. Roster is the season's standings (the slate carries every non-conference opponent too), slate teams as the fallback where ESPN ships none. Session-scoped like the season chip beside it, and held across season switches by a disproof guard: a pick the new season's conference doesn't have reads as "All teams" | Andy's ask. A conference's season is ~150 games; the one question the whole card stack can't answer at a glance is "when does my team play". Resetting on a season switch would punish flipping back and forth, but a stale name must never empty the pane — so the pick survives only while that season's conference can honor it |
| 2026-09-05 | The Top 25 is followable: a third follow set on `FollowingStore`, keyed by league (`following.pollLeagues`, tokens `"cfb"`), with a star on the hub row and a pill on the page. A followed poll leads the Tables Following section and still repeats inside its league's accordion. It stays out of `followsAnyone` and `preferredLeague` — those gate game-shaped surfaces, and a poll carries no games | Andy's ask. Keyed by league rather than a flag because the NFL has no poll today and a league that grows one must need no migration; the set is the conference set's shape, so both read the same way |
| 2026-09-05 | PollScreen moves onto the entity-page template (hero mark + title + ESPN's poll line, follow pill on the toolbar row, content on the recessed ground, inline title on scroll) with **no tab bar**, and the AP/Coaches/CFP segmented picker becomes a `PollMenuChip` in the season chip's slot — trailing, above the card it scopes | Andy's call: the poll page was the last screen not built like the others, and the three polls are the same 25 teams read by different voters — a filter, not two pages. A one-item tab row would be chrome saying nothing, so the page has none. ESPN's headline ("2026 AP Poll: Preseason") moves from card caption to hero subtitle, where the other pages put their subtitle |
| 2026-09-05 | The NFL gets a whole-league row on the Tables hub, above the AFC and the NFC: ESPN's league group (id 9, `Conference.leagueWideId(in:)`), all 32 teams ranked by win percentage, on the same ConferencePage the conferences use. Built by merging the conference tables the hub already fetched — `[ConferenceStandings].leagueTable(in:)` — so the row costs no request. Two things it needed fixed: conference follows now walk the group chain (`Conference.chain(for:)`), and the NFL's in-group standings column is captioned CONF | Andy: "we need to add another league in here for the whole NFL as well, not just the different conferences." `standings?level=1` was the obvious source and is the worse one: its current-season order is the same win-percentage ranking, but its past-season order is a stale seed interleave that puts 14-2 Baltimore ninth (probed live). Ordering it ourselves doesn't breach the never-sort-standings rule — that rule protects orders encoding tiebreakers, and the NFL ranks nothing across its conferences. The follow walk-up: ESPN's NFL scoreboard gives a team its *division* id, so a followed AFC, NFC or NFL matched no game at all. The caption: the mapper's `divisionRecord` fallback asked for a key the payload spells `divisionrecord`, so every NFL table has been showing the `vsconf` record under a DIV caption — the caption moved to match the number, since the 32-team table has a conference record to show and no division one worth a column |
| 2026-09-05 | The Teams tab becomes the follow list: one card per followed team, stacked, and the conference browse accordions retire with `TeamBrowseRow`. Joining a team moves into an **Add teams sheet** — search over the whole directory, with a curated popular shortlist standing in until the first keystroke replaces it with results; its rows toggle follows (onboarding and the sheet now share one whole-row `TeamFollowRow`). Reaching a team page for a team you don't follow is search's job; browsing by conference is the Tables hub's | Andy: "the teams page doesn't need to really surface all the teams grouped in accordions… just display each team that is a favorite as its own card." The tab was carrying ~250 directory rows to answer a question about a handful of teams. "Popular" is curated, not measured — the app has no analytics and ESPN publishes no popularity figure, so the shortlist is a hand-picked set of the biggest followings in each league, skipping any id the directory doesn't carry |
| 2026-09-05 | The entity pages' tab row and its filter chips are **sticky**: hero splits into an identity block that scrolls away and a pinned `LazyVStack` section header carrying the tab row (bgCard) over a bgRecessed control strip, on TeamPage and ConferencePage alike. The strip renders its gap whether or not that tab has a chip — a collapsed one merges a bgCard card into the bgCard tab row — and the tab strip's paint overhangs upward by the identity block's own bottom gap, covering the seam where a pinned header settles under the bar | Andy: "tabs and filters should be sticky as the user scrolls." A conference season is ~150 games and a team's is a full schedule; switching tab, team, or year shouldn't cost a scroll back to the top. Lazy only for the pinning — two children, and the section's content is the whole pane in one subtree, so the standings anchor scroll still finds its row |
| 2026-09-05 | The Scores header keeps Live and nothing else: the Top 25 chip and the season chip both leave (supersedes the Top-25-survives-the-funnel and season-rides-the-day-strip calls from the same day). The slate filter has no control on Scores any more — `ScoresScreen` passes none, so a filter stored by an earlier build can't narrow the day invisibly — and the strip runs the full width to its trailing edge | Andy: "remove the top 25 and season selector on this scores page." Live is a state the day itself is in; ranked-team and past-season browsing both have real homes one tab over on Tables, where a poll and a season chip scope a page that is about them |
| 2026-09-05 | The Top 25 becomes an entity page on the ConferencePage/TeamPage template: Standings and Games tabs, and the poll table adopts the standings tables' own column language (`#`/TEAM/OVR captions, the place gutter, 10pt rows) with a `MOV` column where a standings table has none. The poll chip (AP/Coaches/CFP) stays the Standings pane's control and scopes the Games tab too — its 25 teams are the slate's members, by the "any ranked participant" rule | Andy: "this page needs to mirror the ui of all the other league/conference pages in terms of the table and we need tabs for standings and games." The Top 25 *is* an entity whose members are 25 teams, so the same template answers it — and the poll table was the last table in the app speaking its own dialect |
| 2026-09-05 | Games tabs gain a shared control row on every page that has one — `SlateControlRow`: **Weeks** and **Date** as on/off toggles (two answers to one question, so turning one on turns the other off; both off is one chronological card) and **Team** as the dropdown, at rest labelled by the control's own name. Cards head "Week 1" or "Saturday, September 5" | Andy, in three passes: the filters first, then "All weeks should just read 'Weeks' and be a toggle... All teams should just be 'Team' and the dropdown is perfect", then "these filters need to go on all leagues/conferences pages (not just top 25)". Week and date are a *view* choice — every game stays in the list either way — where the team pick is the only narrowing one. The row is a plain HStack, not a scroller: a horizontal ScrollView clipped the chips' own shadows |
| 2026-09-05 | The season chip moves back to the toolbar row on every entity page — Top 25, ConferencePage, TeamPage — left of the follow pill, in a new `.bar` style (the follow pill's outlined capsule, since glass over an opaque bar reads as a smudge). Supersedes the 2026-08-31 move into the panes; TeamPage's Overview is the exception it always was and shows no chip, its record card being pinned to the current season | Andy: "season selector needs to be in the header not inline with the table on all conference/league pages." The season scopes every tab, so it belongs beside the page's identity rather than above one pane's cards — and the pane row is now the filters' |
| 2026-09-05 | Past-season polls come from ESPN's **core API** (`sports.core.api.espn.com`), the only surface with a season axis for rankings: the final AP and Coaches votes at `types/3/weeks/1`, the CFP's selection-day table at the last regular week (404 in the postseason). Its ranks name their team by `$ref` alone, so names and marks are resolved against a one-request `/teams` directory (760 teams, cached per client); a rank the directory can't name is dropped, and an empty directory fails the season rather than tabling dashes | The site API's `/rankings` is latest-only — it ignores `season`, `week`, `year` and `dates` alike (probed live 2026-09-05), so a season selector on the poll page had no other source. ~4 requests per past season keeps the polite-guest rule; the current season still takes the one shipped request |
| 2026-09-05 | A season's slate is fetched by **date window, never `dates={year}`**: `conferenceGames` now asks for the season's own span (Aug 1 → the rollover month's end), and the new division-wide `seasonGames(year:)` splits that span at November 1 into two `limit=900` requests. A bare year is the *calendar* year — it opened the 2026 slate with ten `season.year: 2025` bowl games and truncated before December (verified live 2026-09-05) | Found building the Top 25's Games tab, which needs the whole FBS season (~950 events) in one shot. The same bug was quietly seasoning every ConferencePage Games tab with last January's bowls |
| 2026-09-06 | **FCS moves inside College Football's accordion on the tables hub** — one card, 11 FBS conferences then 14 FCS ones, no second header and no separate collapse state (supersedes the FCS-as-its-own-collapsed-card shape E8 shipped). The two fetches stay separate and still fail independently; only the display folds, in one `foldingDivisions()` pass over the union, which is what sorts the divisions apart — the `.fcs` tier rung already sits below `.independent` | Andy: "FCS is separated from the rest of college football. We need to group FCS and FBS together all as college football." The hub's accordions are leagues, so a card beside College Football read as a third league — and the row a user is looking for is a conference either way. The opt-in rule that E8's collapsed card enforced lives in `ScoreboardStore.divisions` (following an FCS conference is what widens the fetch), not in whether the hub lists the row |
| 2026-09-06 | League accordions wear their league's mark — Scores and the Tables hub alike, in `ConferenceLogo`'s 18pt footprint (Following keeps its star; a card with no mark falls back to the football glyph). Supersedes the name-only header rule: college football's mark does exist, at `redesign/assets/img/icons/ESPN-icon-football-college.png`, which is what ESPN's own NCAAF scoreboard declares in `leagues[].logos[]`. Held statically on `League.logoURL` and deriving no dark twin — the backing disc is a header badge's dark-mode answer, and the NFL's light-inked `leagues/500-dark` shield would vanish into it | Andy: "add logos to the college football (NCAAF) and NFL accordions like we used to have for each conference." The 2026-09-05 probe searched the `leagues/500/` bucket, where only the NFL's is filed, and read the miss as "no mark exists" — the hole that forced name-only headers was a wrong lookup, not a missing asset |
| 2026-09-06 | NFL standings gain a **scope filter** — League / Conference / Division — as a menu chip in the Standings pane's control row, the team filter's twin one tab over. Which scopes a page offers is read off the league's hierarchy (`StandingsScope.scopes(for:)`), so the league page shows all three, a conference page the two below it, and every college-football page none. League and Conference are two arrangements of the response the page already has; Division is the one scope that costs a request — ESPN's shipped standings stop at the conferences, so it asks `standings?level=3` and caches it per year, only once a user picks it. The new `ESPNMapper.divisionStandings` walks the whole tree instead of `standingsGroups`' one-depth-or-the-other collapse, because a deeper response can carry both, and takes parentage from our own registry first (the payload's nesting only where the id is new) | Andy: "we need a filter on the standings page to sort through by the whole league … but also by conference (afc/nfc) as well as by division." One 32-row table answers "who's best in football" and nothing about a division race, which is the question the NFL's own season is built on. A scope is a *view* of one fetch rather than a different set of teams, which is why the hero still says 32 teams at every scope — and why the control is a dropdown, not a set of toggles: the three are one question. Fixes a hole on the way: an NFL division page (AFC East) had no table at all, because the shipped response never carries one |
| 2026-09-06 | **The preseason gets its own cards**, ahead of the regular season's, and the app's season now opens in July: `ConferenceSlate.groups(from:)` buckets `seasonType == 1` into its own namespace (`preseason-{week}`), and `League.seasonOpensIn` moves the NFL's `SeasonSpan` floor from August 1 to July 1 so the Hall of Fame Game is inside the window at all. Cards read "Hall of Fame Game", then "Preseason Week 1–3" — ESPN numbers the HOF weekend as preseason week 1 and the three preseason weekends as 2–4, so the label is the week number minus its opener; a league that numbers its preseason from 1 keeps its own numbers | Andy: "add a way to group preseason and hall of fame game separate from the regular season." The preseason's week numbers restart exactly like the postseason's, so the NFL's Hall of Fame Game and its opening Thursday were both filing under "Week 1" — the same bug the `seasonType == 3` split fixed for title games in 2026-08-29. The July floor is the honest span: a season opens when its first game can, and an August floor cut the front off the NFL's. Costs the day strip ~30 empty July days, which the launch's `firstDayWithGames` probe already walks past. **The 1 = Hall of Fame week mapping is reasoned from ESPN's calendar, not probed** — the labels shift by one if it's wrong, and nothing else does |
| 2026-09-06 | **College football breaks back down by conference on Scores; the NFL stays one accordion** (supersedes the one-accordion-per-league shape from 2026-09-05). CFB conference sections in tier order (P4 → G5 → Independents → FCS → Other), then the NFL's single section; every section but Following defaults open, and a conference header is two surfaces again — mark + name push its page (the 2026-08-25 split, back now that a header has somewhere to go), the rest toggles | Andy: "instead of two accordions on the scores page (college football nfl), let's actually switch back to the conference (big 10, sec) but nfl can remain as is." One "College Football" accordion is 60 rows on a Saturday with no way in, and conferences are how fans already carve one up. The NFL's 16 games in one section *is* the slate at a glance, and its divisions would be four rows a section |
| 2026-09-06 | **Following on Scores is your teams; a followed table gets hoisted instead.** A conference or poll follow no longer pours its slate into Following — it moves that table's section directly under Following (a CFB conference *moves* up the stack rather than being cloned; a poll or an NFL group, which the stack has no counterpart for, gains a section, its games still repeating in the NFL's). `FollowedTable` is the new shared identity over both follow sets, and `GameSection.table` is what makes the move a move | Andy: "if a user is following a table/league/conference, those teams/games shouldn't show up in the following list… the table should just show up higher up on the list." A Big Ten follow is ~8 games on a Saturday, which buries the three you actually care about. Sections stay complete: the game is still in the table's section and its conference's |
| 2026-09-06 | **Followed tables are drag-reorderable on the Tables hub**, and that order is the order they lead the Scores page in — one list, one order, both screens (`following.tableOrder`, `FollowingStore.orderedTables`). A new follow lands at the end; a set saved before dragging existed falls back to the hub's own tier order, so nothing needs migrating. Drag-and-drop rather than an edit mode (a `List` nested in the hub's ScrollView fights it for every gesture), with Move up / Move down VoiceOver actions since a drag is not an accessible affordance | Andy: "users should be able to drag and rearrange tables that they're following on the table page to reflect the order they want to see them under following on the scores page." The Following card was already both follow sets in one place; it just had no order of its own |
| 2026-09-06 | Day chips carry their month — "Sun, Sep 27" — and yesterday and tomorrow get their names beside today's | Andy's ask. The strip spans a whole season that crosses a year boundary, so a bare "Sat 5" stops meaning anything the moment you drag past the fortnight either side of today; and "Yesterday" is what anyone calls yesterday. VoiceOver already spoke all three names — the chips now agree with it |
| 2026-09-06 | The **Today jump leaves the day strip** for a floating button, centred over the slate just above the tab bar, and inverts — `textPrimary` ground under `bgPrimary` ink, dark on light and light on dark. It still only exists while it has somewhere to go | Andy's ask. Pinned to the strip's trailing edge it cost ~70pt of strip on every day but today; floating costs nothing and sits where the thumb already is. Inverted because it is the one control on the page that *changes* the day rather than describing it — the same pairing the selected day chip wears. The strip now runs its full width always |
| 2026-09-06 | **A followed thing gets a card, not a row**: the tables hub's Following section becomes one card per table (drag reorder intact), and the Add teams sheet becomes one card per team with the league headings dropped and the shortlist interleaved across both leagues (`PopularTeams.teams`, proportional round-robin). The hub's league-accordion headers grow to a card's height to match | Andy: one card holding fifteen teams under a heading "feels like a mistake to be leaving certain teams out" — separate cards read as suggestions, which is what they are, and the whole directory is a keystroke away. Interleaved because dropping the headings would otherwise have put fifteen college programs above the first NFL team. Two sizes of card in one stack read as an accident, hence the header height |
| 2026-09-06 | **Team routing is league-qualified end to end** (`TeamRef`), and `statside://team/{league}/{id}` joins the grammar; a bare legacy link prefers a followed team. Search results key on `followKey`, never `Team.id` | Searching "Browns" opened UAB: both are ESPN id 5, the intent carried only the id, and the directory publishes college football first. The same collision one layer up had the Bills' row wearing Auburn's mark (both id 2) — SwiftUI reused one row's state for the other as the query changed. `FollowKey` and `ConferenceID` already knew ids collide; the router and one `ForEach` didn't |
| 2026-09-06 | The poll picker's rows tag with a non-optional `String` (`PollMenuChip.options`) | `Poll.type` is `String?` and the selection binding is `String`, so no tag could ever match: the AP/Coaches chip looked alive and did nothing. The three other menu chips were already correct — `ConferenceTeamFilterChip` even spells its optional tags out, which is the pattern |
| 2026-09-06 | Widget: the kickoff's **day and time split onto their own lines** (`WidgetGame.statusDetail`, optional in the snapshot so old blobs decode), and scores drop to the app's row scale — 13 medium, 13 semibold live, a losing side giving up its colour rather than its weight | "Sun, 9/13 1:00 PM" does not fit a 64pt column at any text size, and the column is fixed on purpose (a `minWidth` made the score column zig-zag). `GameRow` already splits day from time, so the widget borrows the split rather than widening. The 17pt `score` tokens belong to the detail header; in a 60pt card they shouted over the team names beside them |
| 2026-09-06 | **A calendar sheet joins the day strip** — a button beside Live, opening season-bounded month grids (pinned weekday captions, inverted selected day, Today in the toolbar). Supersedes the "no calendar modal" don't | Andy's ask, from FotMob, which ships both. The don't was written when the strip replaced the week strip and the objection was to a *picker standing in for the axis* — the strip is still the axis. Dragging is fine for the fortnight either side of today and hopeless for "the Iron Bowl in November", which is the only thing this answers |
| 2026-09-06 | Team pages **fetch the preseason** (`seasontype=1`, a third parallel request) and split the Games tab into a card per phase — Preseason / Regular Season / Postseason — each rendered only where it has games | Andy's ask. The client only ever asked for types 2 and 3, so preseason never arrived; a fan checking in mid-August had nothing to look at. Splitting is what keeps exhibition football from reading as games that counted, and what lets the regular-season card keep the bye week. "Schedule" was renamed because once the preseason has a card the old name no longer says which games are in this one — and calling a card "Regular Season" while it held bowl games would have been a new lie. Note: kickoff reminders now see preseason games, against the 64-pending cap |
| 2026-09-06 | The Top 25 wears **college football's mark, not a trophy** — the hub row, the poll page's hero, and the Scores section header alike (`FollowedTable.logoURL`) | Andy: "Top 25 is going to become confusing" as sports are added. The name never said whose, which is fine while one league polls and wrong the moment a second one does |
| 2026-09-06 | Conference standings mark the **championship cut with a bar down the qualifying rows' leading edge plus a keyed legend** (FotMob's promotion/relegation mechanism), replacing the full-bleed divider. Ink, not colour | Andy's ask. The divider could only say where the line fell; the bar says which teams are in, and survives a scroll that leaves the line off screen. The budget stays at three — and VoiceOver hears "in the championship game" in the row's own sentence, since it reads no edges |
| 2026-09-06 | **The postseason becomes its own tab, drawn as a bracket** on the NFL's league page and the Top 25: compact match cards in two fitted columns joined by hairline connectors, a round chip row, and a threshold swipe between rounds. Deliberately not the card-list format every other pane uses — a round of games is a list, but a bracket is a shape, and the shape is the information | Andy's ask from the ESPN playoff-bracket and FotMob knockout references. Costs no request: the Games tab already fetches the whole season, and the playoff games stay there too, in date order — the bracket answers "who plays whom", the Games tab answers "when". The columns are sized to fit rather than scrolling sideways, which is what frees the horizontal axis for the round swipe; the entity pages' tab swipe stands down on this tab. The swipe is threshold-only, not finger-tracked like the day swipe: a tracking gesture must claim the drag from its first movement and the page's vertical ScrollView wins that every time — the day swipe can track because it is attached outside its scroll view |
| 2026-09-06 | **Bracket connectors are earned, never assumed**: a line is drawn only where a completed game's winner turns up in a later game, and a team that reached a round without playing the previous one is shown as a **bye** entry of its own. Each next-round game sits level with the midpoint of its own sources | The first cut laid both columns out chronologically and the result "doesn't make sense with how the advancements occur" (Andy): a winner's line crossed the column to reach whichever game the calendar put it beside, and the top seeds appeared in the second column having never been in the first. ESPN publishes no bracket tree, so the alternative to reading advancement off results is assuming games 1 and 2 feed the next round's game 1 — wrong the moment a format reseeds, and wrong invisibly. An unplayed round therefore draws nothing and wires itself up as results land; byes are only synthesised where a result already connects the two rounds, so no phantom byes appear where bowls precede a playoff |
| 2026-09-06 | Full-width tappable surfaces on the Scores swipe pane take a new `SwipeSafeButtonStyle` (`.buttonStyle(.swipeSafe)`) instead of `.plain` — the game row, the conference-name link and the accordion toggle. `.plain` fires on touch-up whenever the touch is still inside the button, and a full-width row is *wider* than any swipe, so the day swipe committed and the row's `NavigationLink` pushed a detail page on the way out. The style triggers on a `TapGesture` instead, whose movement tolerance is the system's own | Found cutting 2.1: `SmokeUITests` failed, and a hand-driven swipe (no accessibility querying, so not the iOS 26.5 ghost-activation artifact) reproduced it on 2.1 **and on a build of `eb4ad45`** — so it shipped in 2.0 and is live. Vertical drags were never affected: the enclosing ScrollView claims those and cancels the press. Chosen over a `.disabled()` flag driven from the pane's drag state, which would have to reach every row through the environment and flip mid-drag, invalidating the whole scores tree on the very gesture the E5 pass made cheap. Narrow capsules (Retry, the follow prompt, the day chips) keep `.plain` — a swipe leaves their bounds, so they cancel on their own |
| 2026-09-06 | The postseason tab is **the playoff only** — college football's bowls are excluded (a leftover rule, so "Quarterfinal at the Rose Bowl" still reads as a quarterfinal) — and the NFL's **Pro Bowl hangs beneath the final** as an exhibition rather than taking a round chip | Andy: "reserve that tab just for the playoffs", and the Pro Bowl "stack underneath the superbowl kinda how fotmob shows the bronze metal". Thirty-eight exhibitions under a "Bowls" chip were the biggest thing on a screen whose subject is a twelve-team bracket, and nothing about a bowl is bracket-shaped. ESPN files the Pro Bowl as `seasontype=3` week 4; a chip of its own put an all-star game between the conference championships and the Super Bowl, which is the one thing a bracket must never imply |
| 2026-09-06 | Game detail gains a **Plays tab** — every possession newest-first, each an accordion row expanding into its plays — and the **Drives card moves into it** (supersedes the 2026-09-05 line about not splitting Drives off the default tab). One card of accordion rows rather than a card per drive: a full game is 22 possessions, and the density target is the reason. Scoring narrows with the Games tabs' own control language — All plays / Scoring as two toggles answering one question. Tabs are now per-availability, so a game with no drives shows no Plays tab and a pre-kick game still shows Summary alone | Andy, from ESPN Play-by-Play and FotMob Commentary side-by-sides. Both reference apps expand a drive into its plays; ours stopped at "Punt · 5 plays, 20 yards, 2:39" because `drives.previous[].plays[]` was decoded away. The 2026-09-05 objection was that splitting Drives off would move a primary surface to no one's benefit — that held while the drive log was all there was, and stops holding once collapsed drives *are* the play list's top level; leaving it on Summary would print the same rows in two tabs |
| 2026-09-06 | A live game's detail page gets the **Gamecast strip** — possession mark, down and distance, the spot, the drive so far, the last play, over a monochrome field bar — leading the Summary tab. Derived entirely from `drives.current`'s last play (`end.shortDownDistanceText` / `possessionText` / `yardsToEndzone`), not from a second live-only payload, and ESPN drops `drives.current` at final, so the card retires itself with no clock check. The bar draws the away end zone left and the home right, matching the header's logo order, with an arrow for the drive's direction — `yardsToEndzone` counts toward whichever end zone the *offense* attacks, so the same number is opposite ends of the bar depending on possession | The Scores row has shown possession since 1.0 while the detail page — the screen you open *because* the game is live — showed a clock and nothing else. Monochrome because the header already carries the live dot and a field this small reads on position: the budget's own rule that a design wanting color usually wants spacing. **`drives.current` is reasoned from ESPN's live shape, not probed** — the captured fixture is a final game and ships none — so the strip needs an eyes-on check on a live Saturday; every field is optional, so a wrong guess is a missing card, not a crash |
| 2026-09-06 | The Game info card gets the venue treatment FotMob gives it: stadium name in ink over its city in meta gray, then the crowd as label/value pairs — Capacity beside Surface before kickoff, **attendance leading with capacity as its context** and an ink fill meter beneath once ESPN publishes one. Capacity itself comes from the **core API's venue resource** (`/venues/{id}`, reached via the venue id the site payload does carry), memoized per venue for the client's life; the card's pre-kick and played builders merge into one `GameInfoRows` | Andy's ask. The capacity row shipped with the 2026-08-25 preview cards and has never rendered once: ESPN's site API ships no `capacity` on any venue object, in either league, pre-game or final — so the field was decoded, always nil, and silently absent. The meter is ink, not FotMob's green: a meter is chrome, so the budget holds at three. One extra request per venue per session, never per 30s poll |
| 2026-09-06 | The widget stops showing yesterday: a result holds its slot only for the day it was played in (`GameSelection.isSpent`, with a six-hour grace so a game that finishes after midnight doesn't vanish the instant it goes final), the same rule clearing a canceled game and a kickoff ESPN never flipped off `pre`. The provider stops asking for ESPN's current week and asks for a **day window** instead — yesterday through a fortnight out — and a timeline carrying a result now expires at midnight rather than on the next hourly tick | Andy: "once game day is over and the next day has come, the final scores should clear and prioritize showing upcoming games instead." A week is only ever the asking league's week: on the Sunday after a Saturday slate, ESPN had rolled the NFL forward to next weekend while college football's week still held yesterday's finals — so the widget showed two NFL fixtures and two spent college results, and the Saturday those same teams play next was not in the payload at all. Fourteen days forward clears a bye week, the longest a followed team can go without a fixture; one day back is what keeps an overnight final in the payload while its grace runs. Still one request per followed league, so the reload budget is untouched — only the payload grows |
| 2026-09-07 | **A day swipe commits its day on the frame the thumb lifts**, not when the slide lands: `LeagueScoreboards.select(day:)` splits into a synchronous `show(day:)` and an async `loadSelectedDay()`, and the panes keep drawing the outgoing day from a new `ScoresScreen.settlingFrom` for the ~0.3s it spends leaving | Andy: "there is a second delay before the day in the strip changes to the accurate day." The commit was living in the settle animation's completion, so the strip, the header and the Today button all waited out a 0.32s spring plus the fetch's task hop before agreeing with the gesture. Separating the two lets the model move now and the picture catch up — the panes need a day of their own precisely because the selected one is no longer the one on screen |
| 2026-09-07 | A **team page's Standings tab gets the league/conference/division scope chip** — the NFL's team pages only. Where a conference page scopes down into what it contains, a team page scopes out into what contains it (`StandingsScope.scopes(forTeamIn:)` walks the team's own chain), so Division is the team's own four, Conference its sixteen, League the whole thirty-two — every scope a table with the team's row in it. The pane still opens on the conference, and the chip's ink becomes `isNarrower(than:)`: scoping out reads as quiet as the default, only a narrowed table wears the fill | Andy: "team standings page needs the league/conference/division selector that the league has." A Browns page tabling the AFC could answer neither "who's winning the North" nor "where do we sit in the league" — the two questions a division race is made of. A team sits in the middle of its own hierarchy rather than at the top of it, which is why the conference page's widest-view-of-itself default has no meaning here and the ink rule had to learn the difference |
| 2026-09-07 | **Scores section headers tag their league** — a quiet uppercase `League.shortName` caption riding the name ("ACC CFB", "Top 25 CFB", "Other CFB"), on every section that has a league and doesn't already say it: the NFL's own section is titled "NFL" and Following spans both, so neither takes one. VoiceOver hears the long form ("ACC, college football, 8 games") | Andy: the college football accordions need "NCAAF or CFB or something similar to anchor it to." Breaking college football back down by conference (2026-09-06) left every header on the page naming a conference and none of them naming a sport — "ACC" is only obviously college football to someone who already knows. Reuses the caption the game rows and search results already tag with, so the app has one word for a league everywhere |
| 2026-09-07 | The floating **Today button appears only once the Today chip is off the strip** — three days either side of today, and on any past season, where the strip has no Today chip at all (`LeagueScoreboards.showsTodayJump`, gating the button and the slate's bottom clearance together) | Andy: it "should only appear when the 'Today' affordance up top is out of view". One day out, the chip is still a thumb's reach away in the strip, so the button was a second Today saying the same thing louder. Two days is the last day the chip survives — it is already clipping at the leading edge there, now that every chip carries its month |
| 2026-09-07 | **Reordering followed tables is a hand-rolled drag, not a system drag session** (supersedes the `.draggable`/`.dropDestination` half of the 2026-09-06 reorder decision): a long press anywhere on a card lifts it, the grip lifts it with no press at all, the card tracks the finger one-to-one while its neighbours spring around the slot it left, and the commit and the landing are one spring. Per-card measured heights (`onGeometryChange`), and `FollowingStore.move(_:to:)` takes the insertion index the drag resolves to — `move(_:onto:)`, the VoiceOver path, now runs through it. The hub's ScrollView stands down for the length of a lift; `Top25Row` and `ConferenceListRow` take `SwipeSafeButtonStyle`, and their content stops hit-testing entirely while anything is lifted | Andy, from a FotMob Leagues side-by-side: FotMob has "a way more tactile experience of feeling like you're dragging the cards up and down." A system drag session is built for carrying an item *out* of a list — it detaches a small preview from the finger, badges it with a green insert plus (a copy verb, on a reorder), leaves the source card sitting in place, and moves nothing until the drop lands. A reorder should read as moving the card, so the card is what moves. Not a `List` in edit mode either: nested in the hub's ScrollView it fights for every gesture, which is what the scroll stand-down replaces — and why there is no edge auto-scroll |
| 2026-09-07 | The **haptics budget grows to four**: the reorder lift joins the follow toggle, the refresh success and the live score change (amends 2026-08-04) | The lift is the one moment the drag has to announce itself — the card leaves the stack and the finger owns it from there — and it is the follow toggle's class of event: one deliberate tap per gesture, not the per-row machine-gunning the budget was written against. The drop stays silent; the card visibly lands, which is its own confirmation |
| 2026-09-07 | **A widget row's kickoff always names its month** — "Sun, Sep 13", never a bare weekday (amends the 2026-09-06 split, whose day part only grew a date 7+ days out) | Andy: "widget needs to read what month and date. Not just day of the week." The 7-day rule was borrowed from `GameRow`, where a strip, a header and the day you tapped all say which week you're looking at; a widget row on a home screen has none of that context, so "Sun" is a guess. Abbreviated month rather than the app row's digits — it is what the day chips and the share text already say, and "Sun, Sep 13" still measures ~54pt inside the fixed 64pt column |
| 2026-09-07 | A game deep link may carry the day it kicks off — `statside://game/{id}?day=2026-09-13` — and the Scores screen fetches that day when the game isn't in the five it holds. The link's writing half moves into `StatSideShared/DeepLinkURL.swift`, beside the parser's own reasoning; a bare link still parses and still opens a loaded game, which is what a kickoff reminder writes | Andy: tapping a widget row "should navigate the user to that game detail page whether the game is in the past or the future." The widget lists games from yesterday to a fortnight out and the screen holds a five-day window, so an id alone was unresolvable for most of that list — and the writer being a hardcoded string in the widget target is how it stayed that way while the reader grew. The day is a hint, never an identity: the id is what resolves, one day fetch per intent, and an unreadable day degrades to no hint rather than landing the strip somewhere nobody meant |
| 2026-09-07 | Every load's end re-tries the pending deep link (`onChange(of:` the scoreboards' `isLoading`) | The intent arrives while the first window is still in flight, so resolution ran against an empty store, found nothing, and was never asked again — no observer watched the games land. That was every widget tap on an app that wasn't already running, the near days included |
| 2026-09-08 | **A Games tab opens on the next game, not on Week 1** — mid-season the cards already played fold behind one "Earlier games" row, so the pane's first card is the one with the next game in it. `ConferenceSlate.fold` splits the grouped cards at the first one that isn't spent, and only ever takes a **prefix**, so nothing about a season gets rearranged. A season with nothing left — every past season, and this one after the last whistle — folds nothing at all. Applies wherever `ConferenceGamesList` renders: ConferencePage and the Top 25, in both the week and date groupings | Andy: a Games tab "should default to a scrolled position (but with the full header instead of a collapsed header) of the next game instead of the top of the page." Those two halves can't both be true of a scroll — scrolling a 14-week slate to the current week takes the hero with it, which is exactly why 2026-08-29's scroll-to-current-week cut was reverted the day it landed. Folding gives both: the page is still at its true top, header and all, and the history is one tap up instead of one scroll up. Spent is `GameSelection.isSpent`'s rule, borrowed whole from the widget — a result holds its slot for the day it was played in, six hours of grace past midnight — so a Saturday's card stays put all Saturday night and folds on Sunday morning, and a card ESPN never dated can't fold away on a guess. TeamPage keeps its schedule unfolded: a team's season is a dozen rows and the page already leads with a Next game card |
| 2026-09-08 | **An unannounced kickoff is re-anchored to its Eastern day at the mapper** (`DayFormat.placeholderKickoff`, applied by `ESPNDate.parseKickoff` and the CFBD mapper): ESPN parks a `timeValid: false` game at midnight *Eastern*, which is the previous calendar day everywhere west of it, so every TBD game filed a day early for Central, Mountain, Pacific, Alaska and Hawaii — four of the six US zones. The day is the only real thing in the value, so it is what survives: read the Eastern day, rebuild it as local midnight (midnight, not noon, so a TBD game keeps sorting first within its day). Amends the 2026-08-17 "the day part stays (it's real)" clause — it was real only in Eastern | Walkley's field report, reproduced live the same day: 41 of the 71 games in `dates=20260924-20260928` carried the identical instant `2026-09-26T04:00Z`, a literal sentinel. Fixed at the mapping boundary rather than at the bucket, because `Game.date`'s day is read by the day strip, a row's day line, team schedules, shares, the widget and a deep link's `?day=` alike — one normalization beats six. Announced kickoffs are untouched: none of the 30 in that window differ between the Eastern and Central day, so the sentinel was the whole bug. The tests could not have caught it — `ScoreboardStoreTests` and `LeagueScoreboardsTests` both anchor fixtures at noon local *so games never drift between buckets*, which is the one case that is immune; `TBDKickoffDayTests` runs the rule against five real zones instead |
| 2026-09-08 | **The NBA and NHL join, through the league axis** — `League` gains `nba` and `nhl`, and with them a **sport segment** (`basketball/nba`, `hockey/nhl`; all three ESPN base URLs spelled `/football/` as a literal) and a **season-year translation**. Football names a season by the year it opens; the NBA and NHL name it by the year it *ends* — `season=2027` is Oct 2026 → Jun 2027, "2026-27" (probed live). Our own axis stays the opening year everywhere, so the translation lives at exactly one boundary, the query string, and `seasonLabel` is what a chip renders | Andy's ask, four weeks before both seasons open. The roadmap said a third sport "arrives through the league axis, and would be judged on whether its calendar fits a day strip" — the day strip (2026-09-05) is what makes a ten-games-a-night, six-month league work, so the thing that made college basketball "the harder one" stopped being a reason. `SeasonSpan` needed **no change at all**, which is the derived rule paying off: `year(containing:)` takes the latest month any league runs into, and every month still resolves to the season it belongs to. The union span widens from July–February to July–June, so the app no longer has an offseason |
| 2026-09-08 | The `Conference` registry becomes one `Registry` shape instantiated per pro league, and `Tier.nflConference`/`.nflDivision` are renamed `.conference`/`.division`. Both new team→division maps are hardcoded from `standings?level=3` | The NBA and NHL nest exactly as the NFL does, so the rename hands them the League/Conference/Division standings scope for free. The maps are hardcoded for the same reason the NFL's are: these scoreboards ship **no group id at all**, so without the table every game falls into "Other" and a followed conference matches nothing. A conference with no mark of its own wears its league's shield — but only where the league publishes none at all (`nba_conf` 404s), so FCS's United Athletic, which is missing one mark, still shows nothing |
| 2026-09-08 | **College football is the only league whose slate breaks down by conference** — `League.slateSplitsByConference`, replacing the CFB branch and the literal `if let nfl` beside it. The NBA and NHL each stand as one section | 11 games and 8 games are the whole night at a glance, which is the same argument the NFL's 16 won on 2026-09-06; their divisions would be one or two rows a section. Making it a property rather than a branch is what stops the stack growing an arm per league |
| 2026-09-08 | **Standings columns are per league**: football keeps `# / TEAM / CONF / OVR`, the NBA gets `W-L / PCT / GB`, the NHL `GP / W-L-OTL / PTS`. The league table's ranking key follows — win percentage for the NBA and NFL, **points** for the NHL | The leagues disagree about what a standing *is*. The NHL ships no conference record at all (`vsconf` is absent), so a shared CONF column would be a permanent dash under a caption promising a number — and it ships no `winpercent` either, so `leagueTable` fell to its source-order tiebreak and returned East's seeds then West's: a table calling itself a ranking while ranking nothing. Hockey's record is composed from wins/losses/OT losses, because ESPN's `total` summary is "53-22-7, 113 PTS" — a sentence, not a column |
| 2026-09-08 | A period is called whatever its league calls it (`League.periodFormat`), and past regulation everyone says OVERTIME and then counts — except a **hockey period 5, which is a shootout in the regular season and a second overtime in the playoffs**, so the label is only offered where the game could have one. The live clock never offers it: a shootout arrives as a final, not as a running clock | Verified live: `Final/SO`, `altDetail: "SO"`, period 5. The line score's SO column is where a shootout shows, which is also why the Goals card drops the attempts — see below |
| 2026-09-08 | **A derived scoring play is one that moved the game score.** Hockey gets the Scoring slot as "Goals" (ESPN ships no `scoringPlays` for it, so it comes off the play feed); basketball gets no card at all | ESPN flags every shootout attempt as a scoring play and stamps it with the *shootout tally* rather than the game score — 1–2 on all three attempts of a 2-2 game — so a plain filter listed three goals whose running score went nowhere and disagreed with the header above them. Basketball scores ~98 times a game: a chronological list of every bucket is the box score with worse formatting |
| 2026-09-08 | The Plays tab groups by **period** where a league has no drives (`PeriodPlayList`), collapsed by default; `PlayRow` is shared with the drive list | ESPN ships no drives for these leagues but does ship a flat feed (~490 plays a basketball game), and the period is the only unit either sport has *and* the only grouping the feed carries. Football's plays stay inside its drives — carrying them twice would print the same rows in two places |
| 2026-09-08 | **A Games tab is affordable per team and not per conference.** NBA and NHL conference and league pages show Standings alone; team pages keep Overview / Games / Standings | College football's season is ~950 events, which one `dates=` window carries. The NBA's is ~1,300 and it does not: probed live, a season-long request returns exactly **900 events and 12 MB**, silently truncating in February — and `groups=` is ignored outside football, so there is no narrow fetch to fall back on. A team's whole 82-game season comes back in one request, so the fold that opens a Games tab on the next game (2026-09-08) works there untouched |
| 2026-09-08 | `leagues[].calendar` is decoded leniently, and `leagues` becomes a lossy array | ESPN ships two different things under that key: football's labelled week periods, or — for basketball and hockey, whose `calendarType` is "day" — a flat list of ~229 ISO date strings. The string form threw, and because `leagues` was a plain array the throw took the **whole scoreboard** with it: every event of a single-day NBA or NHL request, lost to a field nothing reads for those leagues. A date *range* returns an empty calendar, which is why it stayed hidden until a one-day fixture was captured |
| 2026-09-08 | A box-score category no longer requires a name, and the entity pages read the season on **their own league's clock** (`SeasonYear.year(for:)` rather than `CFBSeason.year()`) | Two silent bugs the new leagues surfaced. Football splits its box score into named groups; basketball ships one with `name: null`, and the mapper's `guard let name` dropped every NBA box score on the floor — invisibly, since an empty box score is exactly how the tab hides itself. And a college-football rollover on a hockey page would call June "next season" while the Cup was still being played for; it was already a latent bug for an NFL page in early February |
| 2026-09-08 | The day fetch waits 250ms before asking, and a **cancelled request is never the refresh banner's copy** | Each day change is one request per league, and `URLSession` allows six concurrent connections to a host, so at four leagues an undebounced flick queued behind itself. The move itself stays synchronous — the strip follows the thumb on the frame it lifts (2026-09-07). Separately: the snap-forward probe cancelled its own fetch (it calls `show(day:)`, which cancels the snap task, from inside the snap task), so the app opened on "Couldn't refresh" with nothing actually failed. The task lets go of its handle first now, and a fetch abandoned on purpose has nothing to tell the user |
| 2026-09-08 | The NBA and NHL playoffs get **no Postseason tab** this pass; `Postseason.name(for:league:)` names no round for them | Their playoffs are best-of-seven *series*, and the bracket reads advancement off "a completed game's winner turns up in a later game" — true of every game of a series against the same opponent. Deferred rather than half-built: the data is there when we do it (`competitions[].series` carries each side's wins and `totalCompetitions`), and nothing is playable until April |
| 2026-09-09 | **NBA divisions wear their conference's mark** — the Eastern and Western Conference logos, which do exist at `nba/500/east.png` and `west.png` (both with a `500-dark` twin). This supersedes the 2026-09-08 note that the NBA publishes no conference marks: that probe searched the `nba_conf/` bucket, and ESPN files them beside the team logos. The NHL genuinely has none — every bucket and spelling probed — so its divisions keep the league shield | Andy: "can we use these eastern/western conference logos for the respective divisions to be able to identify what conference they belong to." Once the accordion lists six NBA divisions, the row has to say which half of the league it is in, and the mark is how — the same job the AFC shield does on AFC East. Costs no new mechanism: `logoURL`'s parent walk already hands a division its conference's mark. **Second time a wrong bucket has been read as a missing asset** (college football's own mark, 2026-09-06) — probe the team-logo bucket before concluding a mark doesn't exist |
| 2026-09-09 | **The NFL's slate stays one accordion on Scores** — division sections were built, seen running, and reverted the same day; the 2026-09-06 one-accordion call stands. Note the deliberate asymmetry with the row above: the tables hub lists an NFL *division*, and the Scores slate shows the NFL *whole* | Andy, looking at it in the simulator: "this feels weird." What the screen didn't survive is the NFL's interconference Sunday — a game belongs to both participants' divisions, and sections are never deduplicated, so 13 games became ~22 rows across 8 sections, with Falcons–Steelers reading as an NFC South game and an AFC North one at the same time. College football's conferences mostly play inside themselves, which is why the same shape reads cleanly there. The two screens ask different questions: a table is a standings race, where the division is the unit; a slate is what's on today, where the league is |
| 2026-09-09 | **The tables hub's pro-league accordions list divisions, not conferences** — the whole-league row stays on top, and the AFC/NFC and East/West rows come off. The hub's request for those leagues becomes the divisional one (`standings?level=3`), with the conference tables *folded back out of it* rather than fetched: one request either way, where asking for both would have cost three more on every hub load. Divisions are grouped by their conference and alphabetical inside it, because the mapper sorts by name alone and alphabetically the NBA's six interleave — Northwest between Central and Pacific, with nothing on screen to explain why. College football is untouched: its accordion still lists conferences, which is the same rung | Andy: "split into division rather than conference … conferences aren't as a priority here." A division is the race a team is actually in; a conference is a seeding pool for a bracket. A conference follow made before this still has its card in Following and still hoists its section on Scores — `followableTables(in:)` keeps them followable even though the accordion no longer lists them, and a team page's standings scope is still the way to reach one |
| 2026-09-09 | **StatSide has a domain — `statside.co` — and a game's web page now carries an OpenGraph card**: `opengraph-image.tsx` beside `web/`'s existing `/game/[gameId]`, rendering the share card's layout at 1200×630 through Satori, with `og:`/`twitter:` tags from the same derivations. Two deliberate departures from the app's card: it commits to **Eastern and says so** ("12:30 PM ET"), and it is a **re-implementation, not a port** — `GameShareCardView` is SwiftUI, so the two are kept in step by hand at `og-card.ts`. Inter is bundled (three weights) because Satori has no system fonts and its built-in fallback ships one, which would flatten a card whose whole hierarchy is weight and size. **The app's share link deliberately does not move**: a CFB-to-statside.co split was scoped and called off the same day, so every share still carries the store link | Andy: "let's use this graphic as the opengraph image… when sent via text or Slack or post on social", then "statside.co — i just bought it, build the web half". Messages already wears the card (`LPLinkMetadata`, 2026-09-05), but Slack, X and every other unfurler build the preview on **their own server** from the URL's og: tags — so nothing the app does can reach them, and only a page we serve can. This is the web-page path the 2026-08-09 decision named and deferred for want of a domain. **Why the app's link stayed put** (Andy: "actually all links should keep the store link"): the swap was never one line — those URLs resolve through the CFB provider, so an NFL, NBA or NHL event id 404s and a CFBD-sourced game carries CFBD's own numbering, and moving the link would take the invite off the one page that sells the app for a web page with no way back to it. So the card serves links shared **from the web**, and an app share keeps the Messages bubble it already had |
| 2026-09-09 | **A pre-game detail header leads with the kickoff time** — the time takes the slot a played game's score takes (new `kickoffHero`, 28pt semibold, `.fixedSize()` like the score line), the date drops beneath it in regular 15 secondary ink, and the network gets its own third line. The "at" goes with the join: `GameHeaderState.kickoff` returns the two parts separately, so nothing has to phrase them as a sentence. The share card takes the same split (36/20/17 against the card's fixed scale, one-and-a-half times the phone's) — Andy's call the same day, reversing the "keeps `statusLine`" first cut; the share **sentence** keeps its "at", where it is prose and reads as prose | Andy's ask. "Wed, Sep 9 at 8:20 PM" set at 12 semibold answered the page's one pre-game question in its quietest type, while the space between the logos — the slot the score fills the moment there is one — sat empty. TBD inverts cleanly for free: "TBD" headlines and the real day keeps the caption, where the old line said "Wed, Sep 9 · TBD" |
| 2026-09-09 | A conference row's leader teaser falls back to the overall record where a league keeps no in-group one (`conferenceRecord ?? displayRecord`), and a **whole-league row shows no teaser at all** | The NHL ships no `vsconf`, so every hockey row on the hub sat bare — name and a follow star — while the NBA rows above it read "Boston · 36-16". Football and basketball still tease the number their table is sorted by. The league row is the other direction (Andy: "we don't have to show a record at all on the league row"): its "leader" is only the best record in the sport, which is not what a league row is asked, and the number was an *in-group* record on a row spanning every group — the NFL's read as a conference record, the NBA's as a 41-11 matching no column on the page it opens |
| 2026-09-09 | **The wordmark drops the field glyph and is set as a mark** — `Wordmark`, one view for the Scores header (24pt) and the share card's sign-off (18pt): SF Pro's condensed width, "Stat" in black against "Side" in medium, −3% tracking. The header's mark grows 17 → 24 — the `heroTitle` scale, for the `heroTitle` reason: nothing above it names the screen — and still costs *less* width than the old glyph-plus-name lockup, which is what the condensed cut buys. Monochrome, unscaled by Dynamic Type as both sites already were | Andy: "remove the field icon and increase the size of the wordmark and stylize it a little more to seem custom." The glyph was `sportscourt.fill`, which is also the Games tab's icon — the chrome said "sports" twice a thumb apart — and a stock SF Symbol beside a stock SF Pro string reads as a placeholder logo, which it was (E1's "placeholder wordmark", never revisited). A drawn typeface is the real answer and needs a licence; a width axis, a weight split and tight tracking are what system type has that reads as drawn. It closes the wordmark half of E10's identity-chrome item — the Scores tab icon is still a football |
| 2026-09-09 | **The tables tab is renamed Leagues**, and its numbered-list icon becomes a trophy (`trophy.fill`); the screen's title follows, and the "Leagues" section heading below Following now only renders where Following actually leads — with no followed tables it sat one line under a nav bar already saying it | Andy's ask. "Tables" named the contents of the tab rather than the tab: what the hub lists is leagues, and a conference or a poll is reached *through* one. `list.number` drew the standings table the old name promised, so it went with the name — a trophy is what FotMob and Sofascore both put on this tab, and it can't be mistaken for the football the Games tab wears. The 2026-09-06 "not a trophy" call is untouched: that was about a *specific table's* identity mark, where a trophy never says whose — a tab icon stands for the category by design. Code names (`TablesScreen`, `Tab.tables`) are unchanged; only the words on screen moved |
| 2026-09-09 | The pre-kick **Game info card splits in two**: kickoff, the network and the forecast stay under "Game info"; the venue, its city, the surface and the crowd become a **Venue** card — the title the played-game card takes too (it was "Game info"). Inside it the venue block gains a divider above the surface, the card's zones now being the ground itself and the numbers about it. Shared row shapes come out as `GameInfoLine` (the icon-led line) and `KickoffInfoRows` (the new card's body) | Andy: "these are stadium/arena/field/weather details. Let's pull the day/date/time and network details into a separate card." One card was answering two questions — when to watch and where it's played — and the second half only existed pre-kick, which is what left the played-game card titled after a card it no longer resembled. The weather rides with the kickoff rather than the ground (Andy's follow-up): it is the other thing that stops mattering the moment the game starts |
| 2026-09-09 | The Game info card leads with a **league row**: the league's mark in the icon gutter, then a tappable `HeaderLinkBadge` per table the game counts toward — the league (a pro league's whole-league table, or FBS/FCS for college football), then each side's conference, away first. A conference game contributes one conference badge, not two. The mark replaces a "League" label (VoiceOver still hears the words), and a pro team's id is walked up from the division the mapper stamps it with to the conference above it. Badges wrap to a second line rather than truncating a name | Andy: "add a league row with a clickable badge that links to the league page", then the conferences, then the logo for the label. A game page named the two teams and nothing about the competition around them, so the tables it feeds were three taps away through Tables. The badges are those tables, which is why they dedupe by destination rather than by side — and why they name the page they open ("FBS", not "College Football"), the crumb language TeamPage's header already speaks |
| 2026-09-10 | **The web-parity ledger is enforced, and counted.** `docs/web-parity.md` is the record of where `web/` stands on every decision in the log above, and two scripts hold it: `check-parity-ledger.sh` **fails the web CI job** when a branch adds decision rows here without adding ledger rows there, and `parity-status.sh` prints the standing (shipped / pending / n/a / differs) on every PR and into its step summary. A PR template carries the same three-way question. `n/a` is a first-class status and a complete answer — a widget, a haptic, an XCUITest fixture has no web analog, and one line closes it for good; `[skip-parity]` in a commit message is the escape hatch for work with no product surface at all | The 2026-09-01 port was a one-shot with nothing holding it and decayed to 1.x in **eight days** — 89 decision rows here, one of which had reached the web. A ledger nobody is obliged to update is a document, not a mechanism; a count nobody can read is a feeling, not a measurement. The check is deliberately dumb — it counts rows, not meaning, because the failure it exists to stop is *forgetting*, not misjudging, and anything smarter would be a reviewer's job wearing a robot's coat |
| 2026-09-10 | **The web's Scores controls become one card**, at the top of the slate column: the day strip and its calendar, then the Live pill, the view funnel and collapse-all, sticky under the nav bar. The strip stops being `position: fixed` inset past the follow rail, the Live/funnel capsule leaves the nav bar's right slot, the collapse-all leaves its floating row above the first accordion, and the grid drops the `mt-12` it needed to clear chrome it didn't contain; the rail's own sticky offset follows the nav bar down from 7.5rem to 4rem. The card sits **outside** the day-swipe element — the strip scrolls horizontally under the same finger. Web-originated: the phone has no rail and no gap to close | Andy, from FotMob, which puts its date nav and its filter row in exactly this container: "move the date controls, live affordance, filters, etc into a card container so that leagues can be top aligned to that." Three controls in three places cost the page its geometry twice — the rail's width of dead air to the left of the strip, and the strip's height of dead air above every card on the page, because the grid had to clear chrome it didn't contain. In the column's own flow the rail top-aligns to the card and the leagues sit where cards sit. Sticky rather than merely in flow because the day is the screen's axis and a Saturday slate is long: the way to the next day must not be a scroll back to the top |
| 2026-09-10 | **A season is fetched in as many windows as it takes, and every conference page gets a Games tab.** ESPN truncates a `dates=` window at `limit` **silently** — no flag, no count, no cursor — so a window that comes back at exactly the limit is halved and both halves asked for, recursively, capped at three splits. The November 1 split retires with it: that date was right for college football's dense autumn and thin postseason and for nothing else. `League.canTableAWholeSeason` and `gamesWindow` are deleted, and `ConferencePage.rollingGames` with them | The 2026-09-08 integration concluded `groups=` was ignored outside football and that an NBA season could not be fetched, which cost basketball and hockey a real Games tab. **Both halves of that were wrong.** Re-probed live 2026-09-10: `groups=1` on a season-long NBA window returns the Atlantic's own **376** games, not the league's 900 — the original measurement was of an *unscoped* request. And the cap is per **group**, not per league: an NBA conference fits whole at 868, the NHL's Eastern truncates at 900 but halves to 665 + 296, and the NHL's whole league needs three windows. Since every group is reachable with enough windows, the width of the group stopped being a reason to refuse a tab. Verified in the simulator against live ESPN: the NHL's full-league Games tab — the worst case, which truncated at January 27 as one request — now runs to Boston's April 10 finale |
| 2026-09-10 | **The per-venue capacity request is deleted.** `GameSummary.venueCapacity` is still decoded off the site payload — it is simply always absent — so the Venue card's capacity slot and its attendance meter stay written and light up for free if ESPN ever ships the field. What is gone is the core-API `/venues/{id}` lookup added on 2026-09-06 to fill it | That request was added to rescue a Capacity row that had "never rendered once", and it did not rescue it: **ESPN publishes no venue capacity anywhere.** Sampled live 2026-09-10 across all four leagues, 100 core venue objects carried a capacity **zero** times, and the field is not among the keys that resource ships — `id`, `guid`, `fullName`, `shortName`, `address`, `grass`, `indoor`, `images`. So the row still never rendered and the app was paying a round trip per venue per session to learn nothing. Verified in the simulator: the card is pixel-identical without it |
| 2026-09-10 | **Live Activities are de-iced on path 3** — broadcast push channels (iOS 18+), one channel per *game*, an APNs-capable service holding **no user data**, one ESPN poll per live game. Local-only updates (path 1) are rejected **on the merits, not on cost**: an activity only advances while our process runs, so the card is fresh while you're in the app and stale the whole time you aren't, which is the inverse of the use case. Per-activity push (path 2) is path 3 with a device-token database and ESPN load that scales with installs. The client half — attributes, card, island ladder, entry point — is built first because it is identical under paths 2 and 3 and needs no server, but it does not reach users before the service exists. The card itself: one chassis with a swappable centre slot across pre-game / live / intermission / final, a fixed height so the lock screen never reflows mid-game, `liveAccent` green on the clock and nowhere else, `bgCard` rather than team colour, crests outboard of the numbers, and a **stale** state — which none of the four reference apps has, because none of them face this update problem | Andy, asked `docs/live-activities.md`'s closing question directly: "lets build this." The doc had separated three paths that a single 2026-08-04 Icebox line ("no push story without a backend") had collapsed into one, which was right about the destination and wrong about the reasoning. Path 3's load is a function of the slate (~60 concurrent games) rather than the install base, which is the only version the polite-guest rule survives; it is also the same infrastructure as the CFBD caching proxy, so the two decisions are cheaper made together. The design is drawn and locked in Figma (`↳ Live Activities`, direction A) after a four-app study — the chassis is what 4 of 4 reference apps use on this surface, and the second line carrying down, distance and possession is the thing the NFL's own card does not show |
| 2026-09-10 | **The web's first tab is "Games", not "Scores"** — the nav links, the bottom tab bar, the 404's way home, and the Teams empty state that named the screen. Route, view and every `scores*` symbol keep their names, exactly as the Tables → Leagues rename did | Andy: *"'Scores' should be 'Games', we already made that change in the iOS app. we need to carry that throughout the web experience as well."* True, and `sports/App/RootView.swift` has labelled that tab Games since the league axis landed — **with no row in this log**, which is why the web never learned it (the same class of gap E11's P3 item is already open on). The word is also the better one: the tab is the whole slate, and a slate at 11am on a Saturday has no scores in it at all |
| 2026-09-10 | **The web nav bar carries the wordmark on every route** and stops swapping it for the page's name; the per-page bar titles go with it, and Leagues, Teams and Games gain `sr-only` headings so assistive tech keeps the name the bar dropped. **The wordmark itself reverts to plain 17-bold system type** — no condensed axis, no Stat/Side weight split, no −3% tracking — and does *not* take back the 🏈 the 2026-09-09 decision retired. Web only; iOS still ships the drawn treatment | Andy, in three: *"when you navigate to leagues, teams, etc, don't change the logo to be the page title. keep the logo"*, *"change the StatSide logo back to the original version. i'm not liking this 'stylized' version"*, *"not with the football icon, just the wordmark"*. The bar was spending the app's one identity mark to say something two other controls already said — the active nav link on desktop, the filled tab on a phone — and leaving the mark visible on exactly one route, which is not an identity. On the type: the width axis, the weight split and the tracking were what made system type read as drawn, and in place they read as system type trying to look drawn, which is a judgment only the eye makes. The glyph stays gone because its own argument still holds (a stock icon beside a stock system-font string reads as a placeholder logo); the real answer is a licensed drawn mark, and this is honest about being system type until there is one |
| 2026-09-10 | **One content width for the whole web app — 1040px**, replacing the three per-route caps (Games 960, a game page 1040, everything else 80rem). `pageMaxWidth(pathname)` becomes the `PAGE_MAX` constant | Andy: *"the max width on the scores/games page should be the same max width on all the other pages so there is no content shift."* The nav bar is `mx-auto` against this value, so a page 320px wider moved the wordmark, the links and the first card 160px sideways on every tab change. 1040 is the one number that costs nothing to adopt: it is already the app's widest two-column layout and the only cap sized around a real constraint — a box score beside a 320px context rail. Games gains ~80px of slate, which the row spends on the team-name column since the status column is fixed, so the #112 rule it was capped for still holds; Leagues, Teams and Search give back 240px they were using to stretch a standings row |
| 2026-09-10 | **The web app carries an iOS download CTA**, on three surfaces and in no interstitial: a `GetTheAppPill` in the nav bar at every width (Apple's mark, then "Get the app", ink-filled the way the Today button is), a `GetTheAppCard` leading the Scores follow rail and closing a game page's rail, and Safari's own **Smart App Banner** through `metadata.itunes` on every page. The card's graphic is the app **drawn in SVG from the theme's own tokens** — a handset running off the card's bottom edge showing a day strip with today filled and three game rows, one of them live — not a screenshot. One link, `APP_STORE_URL`: storefront-agnostic (no `/us/`) and carrying no campaign parameters, matching the `ShareSignOff.appStoreLink` every iOS share has sent since 1.0. The rail's sticky column gains a viewport cap and its own scroll on the way. Web-originated: iOS is the thing being linked to | Andy: *"include a download CTA for the iOS on the web app"*, then *"include the logo in the button and create a card on the left side of the page above following teams/conferences with a stylized graphic of the app"*. The OpenGraph card (2026-09-09) means a shared game link now unfurls into Slack and X — and lands every one of those taps on a page with **no way to the App Store**, the hole the backlog named as "a way back to the App Store from the web page". The rail is the one column on the Scores page that isn't the slate, so the card pushes no games down; **above** the follow lists rather than below because the rail's length is the follow set's, so anything under it sits at a different height for every visitor and is off-screen for the ones with the most teams — which is also what forced the cap, since a sticky column taller than the viewport pins its top and puts the rest out of reach for good. Drawn rather than shot because a screenshot is a PNG to re-shoot every time the slate's type moves, an unreadable smear at 220pt, and a second asset for dark mode; the logo is Apple's mark alone and never the "Download on the App Store" badge, which is licensed artwork with its own colour rules — it would be the colour budget's fourth exception and a binary to keep in sync |
| 2026-09-10 | **The Live Activity broadcast service lives in `web/` on Vercel**, not the Cloudflare Worker `docs/live-activities.md` sketched — the Worker was E8's sketch, `web/` is already deployed and needs no new account, and `apns.ts` carries no framework so moving it is an entrypoint change. ES256 provider tokens are signed with Node's own `crypto` rather than a JWT package, so the feature adds **no dependency**. Broadcast sends go to APNs on **port 443**, which is what makes a serverless runtime viable at all; channel *creation* is on the management host at **2196** and is therefore out-of-band through Apple's Push Notification Console, with the first cut reading a static `APNS_CHANNELS` map. `PRIVACY.md`'s "no server of its own" is retired for a precise description instead: the service holds which channel a *game* is on and nothing that identifies a person | Path 3 was decided the same day and needs somewhere to run. The load-bearing find is a silent one: a Live Activity's `content-state` is decoded by ActivityKit rather than by any decoder we configure, and Swift's default `Date` strategy is the 2001 reference date — so a server sending ISO-8601, or Unix epoch against a decoder expecting 2001, fails the entire update with no error anywhere, just a card that never changes on a lock screen. `asOf` is now pinned to epoch seconds by explicit CodingKeys and asserted on both sides of the wire. The privacy rewrite is not a concession: broadcast channels are per *game*, so the honest description is stronger than a vague one |
| 2026-09-10 | **statside.co has a card of its own**, and every route that isn't a game inherits it: a root `opengraph-image.tsx` — the wordmark, "Fast, focused scores.", the four leagues spelled out, and the app drawn beside them, running off the bottom edge. It **fetches nothing and renders no live data**; and `twitter.card` becomes `summary_large_image` in the root metadata, which is a separate fix that was shrinking the game card too | Andy, posting the site: it unfurled as a grey placeholder box with a torn-page icon in it. The site had **no `og:image` at all** — the 2026-09-09 card covers a *game* URL and nothing else, so Leagues, Teams, every team and conference page had none either — and `twitter:card` was defaulting to `summary`, the small square thumbnail, which no `og:` tag can override because the card *shape* has no `og:` equivalent. Local-only because an unfurler caches what it gets: a failure sticks in Slack and on X for as long as the cache holds, so the one image representing the whole product makes no request that could fail. The phone is drawn rather than screenshotted for `GetTheAppCard`'s reasons, and its rows are shapes rather than teams for one of this card's own — a graphic naming a matchup invents a result, and an unfurl outlives any real one |
| 2026-09-10 | **The OpenGraph cards are set in Inter again**: the loader reads its `.woff` files with `fs.readFile` rather than `fetch`, and names each face in its own **static** `new URL(…, import.meta.url)` rather than an interpolated path. It moves to `web/src/lib/og/fonts.ts` with the palette beside it, one copy for both cards, and `fonts.test.ts` asserts the bytes are three *different* real typefaces | The game card has been drawn in Satori's one-weight fallback **since the day it shipped** — a card whose whole hierarchy is weight and size, rendered with no hierarchy, so a winner's name never looked heavier than a loser's. `fetch` of a `file:` URL answers `TypeError: fetch failed` / "not implemented... yet..." in Node's undici; Vercel's own OG examples use exactly that line and work because they run on **Edge**, where a bundled asset URL is an HTTP one. Nothing logged, because the loader's never-fail rule — a plain card beats no card — swallowed it. The interpolated path was a second failure hiding behind the first: with nothing to match statically, Turbopack resolved the folder and handed back its `LICENSE.txt` as a typeface ("Unsupported OpenType signature Copy"). Verified by rendering a real final both ways, and by checking the build traces all three faces into both routes |
| 2026-09-10 | **A team or conference link unfurls as itself** — each page's own hero at poster size: the mark, the name, and the one line the hero carries ("Big Ten · 7-1", "18 teams"), with the league as an uppercase kicker **above** the name, dropped where it would only repeat it (the NFL's whole-league table is a page called "NFL"). `CardShell` / `EntityCard` / `BrandCard` come out of the game card so all three cards share one chassis and a link to any of them arrives looking like the same product; both pages also gain a real `description`, deliberately number-free | Andy: "yes, do the team and conference cards too". They were inheriting the site card — right for Leagues or Search, a wasted answer on a URL that names a team. The kicker exists because the app never has to say which league you are looking at (you got there through one) and an unfurl arrives with no such context. Costs: a team card is **one** request, the schedule payload carrying identity and record together — a rank badge would cost a second for a number that freezes the moment the image is cached — and a conference card's identity costs **none**, the registry naming and marking it, so its one fetch buys only the team count and is allowed to fail into a card without it. The descriptions carry no figures for the same caching reason: the record belongs on the image, where it reads as the snapshot it is, not in a sentence that will still be there in March |
| 2026-09-10 | **The team page gains a Roster tab**, fourth after Standings — FotMob's squad screen in the app's own table language: a Coach card, then a card per position group with `# / PLAYER / AGE` captions over rows of *jersey gutter · 36pt headshot disc · name above "QB · 6' 2" · 225 lbs" · one right-aligned metric*. The grouping is always ESPN's and never derived, which is why **the NBA gets one card** — its payload ships no groups. The metric is per league (`League.rosterMetric`, `standingsColumns`' shape): **CLASS for college football, AGE for the pro leagues**. **No season chip on this tab** — the second exception after Overview. Rows are not links. The swipe stops walking tab ordinals and walks `visibleTabs`, so the conference-gated Standings tab can't strand it | Andy's ask, designed against Mobbin (FotMob's squad, the Premier League's; MLS's and DAZN's photo-tile grids rejected — a browse-by-face pattern can't carry 100 college players). Three live probes shaped it, all 2026-09-10: `athletes` arrives in **two shapes** and the NBA's is the flat one, so a grouped-only decoder returns nothing for basketball *silently* (an empty roster is how the tab hides itself); the endpoint has **no season axis** at all — `?season=2019` answers 200 with zero athletes — so a chip there wouldn't do nothing, it would show this year's roster under last decade's label; and college football publishes **no `age` whatsoever** (0 of 100), which is what makes the column per-league rather than shared. Headshots go through `URL.headshotThumbnail`: ESPN links the 600×436 press photo, ~200 KB, and a 100-player roster of those is ~20 MB to fill a screenful of 36pt discs — the CDN's combiner returns ~18 KB, and the `?w=` parameters the plain path accepts are ignored |
| 2026-09-10 | **Navigation destinations carry an identity**: `.id(team.followKey)` on every `Team` destination and `.id(destination)` on every `ConferenceDestination` one. The Roster fetch is additionally keyed by team and drops what it holds when handed a new one | A team page could show **another team's everything**. Found building the Roster tab, on a Lakers page rendering Ole Miss's coach, roster, schedule, next game and conference line under a Lakers crest — pre-existing and live in 2.x. Search and the widget route by *replacing* the value at a path position rather than pushing a second page, and a SwiftUI destination whose identity doesn't change is reused with every `@State` cache intact. The roster only made it visible: a college player carries no age, so the NBA's AGE column printed a column of dashes where the schedule had been quietly lying for longer. Keyed-by-team as well as `.id()` because a fetch-once guard is only safe about a roster it can name |
| 2026-09-10 | **The wordmark reverts to plain system type on iOS too** — no condensed width axis, no Stat/Side weight split, no −3% tracking (supersedes the 2026-09-09 drawn-mark treatment, following the web's own revert the same day). The size comes down with them but not all the way to the original 17: **21 on the Scores header**, Andy's call, and 15 on the share card's sign-off. The field glyph does **not** come back | Andy: *"we changed this back on the web version. we need to update here on iOS as well."* The width axis, the weight split and the tight tracking were what made system type read as drawn type; in place they read as system type trying to look drawn, which is a judgment only the eye makes. The size moved because 24 was affordable *because* the condensed cut bought the width back — on the plain cut it shouts. The glyph stays gone on its own argument: `sportscourt.fill` is also the Games tab's icon, so the header said "sports" twice a thumb apart |
| 2026-09-12 | **A widget row's kickoff says "Today" on game day and "Tomorrow" the day before** (`DayFormat.relativeDay`, shared with nothing else yet); two days out and beyond keeps the absolute "Sat, Sep 12" with its month. Supersedes the widget's absolute-dates-never-Today rule from 2026-09-06. What that rule was right about is what the rest of this change is: a relative word baked into a string outlives the day it was true, so both places holding one now expire it — `GameSelection.nextRefresh` pulls the timeline to midnight for a kickoff today or tomorrow (it already did for a result), and the stale snapshot re-derives a pre-game row's day line against the moment it is *served* rather than replaying the moment it was saved | Andy's ask. The widget's whole promise is the 3:30 Saturday glance, and on the Saturday itself "Sat, Sep 12" is the one thing on the row you already know — the same argument the Scores rows won on 2026-08-09. The absolute form survives past tomorrow for the reason it was chosen: a widget row carries no strip or header saying which week is on screen, so a bare relative word two days out would be a guess. The share text and the share card are untouched — those outlive the moment they are generated in a way a home-screen row does not |
| 2026-09-12 | **The Live filter renames today "Ongoing"** — the day strip's chip and the floating jump button that leads back to it, iOS and web alike (FotMob's word). It follows the *filter*, never the slate: the label holds while Live is on whether or not anything is in progress, and today is the only day it touches. | Andy, from a FotMob side-by-side. Live is a state the day itself is in (2026-09-05), and the chip was still calling it a whole day while the slate underneath was a handful of games in progress. Tying it to whether games exist would flip the label back to "Today" as the last one went final — the mystery state the labelled chips exist to avoid — and "No live games right now" already speaks for an empty slate. The jump button follows because it is the way *back* to that chip: a button naming a day the chip it lands on doesn't is the same one-word-apart problem the 2026-09-07 visibility rule closed, in reverse |
| 2026-09-12 | **A game destination carries an identity too** — `.id(game.routeKey)` on every `navigationDestination(for: Game.self)` (Games, Leagues, Teams), keyed the way `Team.followKey` keys a team page one layer down: league-qualified, because ESPN's event ids collide across leagues exactly as its team ids do. `GameDetailScreen` also names what it fetched (`loadedKey`) and reads its summary and its standings through that name, so a page handed a new game falls back to the pushed row's own header rather than the previous game's, and both its load and its 30s poll loop key on the game | Andy's field report: tapping a widget row *while a game detail page was already open* opened a page that was two games at once — the new game's title, league row, kickoff and network over the old game's logos, records, leaders and venue. A deep link **replaces** the value at the path's position rather than pushing a second page, and a SwiftUI destination whose identity doesn't change is reused with every `@State` intact — the same trap `.id(team.followKey)` closed for team pages on 2026-09-10, on the one destination that pass left out. It is also the destination that trap costs the most: every widget row, every kickoff reminder and every search result opens a game, and the stale half is the half a summary fetch fills |
| 2026-09-12 | **The Today button takes the slate home with it** — the jump scrolls the day's games back to the top as well as moving the strip, and it routes through `selectToday()` rather than selecting today's date, so a strip bounded to a past season re-bounds to this one. Its visibility rule is untouched: it still appears only once today's own chip is off the strip (2026-09-07) | Andy: tapping Today from a day you left scrolled to the bottom "should jump me back up to the top … snap me to today". A day change rebuilds the slate pane, so the arriving day sits at its top by construction — but that was an accident of view identity rather than anything the button did, and it left the pane sliding *out* stuck wherever the thumb had dragged it. The season half is a real hole the same tap falls into: `showsTodayJump` is true on a past season (the strip has no Today chip there at all), and `select(day:)` would have selected today's date inside 2019's bounds — a selected day the strip has no chip for, which is a screen with no way back |
| 2026-09-12 | **The Live filter applies to today and nowhere else** — swiping (or jumping) off today suspends it and the chip turns off with the day; coming home turns it back on. The stored toggle stays the remembered *intent* and `UIStateStore.liveOnly(on:)` is the effective filter, read per day so the pane sliding in under the finger already shows tomorrow's whole slate. Tapping the chip while it is suspended is a request to turn it on, so it still lands on today (the 2026-08-29 jump rule, unchanged) — a tap that may write no state at all | Andy: "if I swipe to tomorrow or yesterday, the live selector should automatically turn off to show whatever games are happening on those days." A live filter on a future day is a guarantee of an empty screen, and on a past one it hides every result the day exists to show — so the persisted filter's own reward (2026-08-29, "keep it even after a new session begins") was an explanatory empty state on every day but one. Suspended rather than cleared because the intent is what makes the trip back free: clearing it would cost a tap to restore something the user never turned off |
| 2026-09-12 | **The selected day is ink, not a pill** — the day strip's selected chip drops its fill (the ink-tinted glass capsule on iOS 26, the solid `textPrimary` capsule on the 18.0 floor) and becomes `textPrimary` at `chipEmphasis` against the unselected days' `textSecondary` at `chip`. Ported to `web/` the same day. The `Today` button and the calendar sheet's selected cell keep their inverted fills — the button because inversion is what marks it as the one control that *changes* the day rather than describing it (2026-09-06), the cell because a grid square with no fill has no selected shape at all | Andy, from a FotMob side-by-side: the selected date "should just be a darker gray like FotMob instead of being a pill". The strip is the screen's axis, not a control panel: a filled capsule reads as a button among plain text and makes the axis the loudest thing on a page whose subject is the slate beneath it. Weight and darkness are what the design system already uses for emphasis everywhere else — the same rule the color budget is written on, applied to chrome |
| 2026-09-12 | **The calendar sheet opens on the month you're already in**, which is what it always said it did: the anchor `.id()` moves off the `Section` onto the month header — a view the scroll reader can actually position — and the opening `scrollTo` moves off `onAppear` onto the main actor, twice. Scrolling back through the season is untouched; only where it starts moved | Andy's report: it opened on July whatever day the strip was on. Either half alone lands the sheet at the top, so both were hiding the other. `onAppear` runs before the scroll view's first layout — the day strip's own `onAppear` scroll gets away with that because its `HStack` isn't lazy and every chip exists to be scrolled to, where this `LazyVStack` has realized a month or two and the months in between are what the offset is made of. Hence the second pass: the first scroll is what realizes the months it travelled through |
| 2026-09-12 | **The active Live chip wears the accent, not the ink** — two new tokens (`liveTint`, `liveEdge`) give it a pale-green wash under a softened-accent hairline, `textPrimary` stays the label ink through the toggle instead of inverting to `bgPrimary`, and the dot grows 7 → 8pt. The tint is a fixed color per scheme rather than an opacity: the pill sits inside a glass capsule, whose translucency would land a fraction differently every time. The web's funnel chip keeps its ink fill — this is the Live control alone. Web took the same change in the same commit (`--live-tint` / `--live-edge`) | Andy, against a FotMob side-by-side: "the active live badge should look more like the FotMob version." The ink fill said *selected*, and it said it by swallowing the one thing on the chip that means live: the green dot went into a black ground where it barely registered. Colouring the control after its own subject is what the live accent's exception is for, so the budget holds at three — one green, one token, spent on the affordance that scopes live. It lands beside the selected-day row above, which took the opposite exit from the same complaint about filled pills: the strip is the screen's axis, so it drops its fill entirely; the chip is a toggle that has a colour of its own, so it spends it. Either way the label stops inverting, so only the ground moves and its contrast improves on the way (~14.6:1 both modes) |
| 2026-09-12 | **The Top 25 header's name opens the poll** — the Scores accordion split (mark + name push the table's page, the count and chevron toggle) now covers every section that names a real table, not just the conferences. `PollScreen` becomes pushable with nothing but a league (`PollDestination`): the polls it used to require came from the tables hub's own fetch, and Scores holds none, so the page fetches the season in progress itself exactly as it already fetched every past one. Web too, where the poll section's name had the same dead header and `/rankings/poll` was already a route | Andy: tapping a league or conference name "takes you to the respective league", and the Top 25 was the one followed table on the page whose name did nothing but toggle. The 2026-08-25 split read "there is nowhere for their name to go" — true of Following and "Other", and it stopped being true of the poll on 2026-09-05, when the Top 25 became an entity page like every other table's |
| 2026-09-13 | **A team page gets a Trophies tab**, derived from the games the team played: ESPN prints a trophy game's name in `competitions[].notes[].headline` ("SEC Championship") and the team's own result says who holds it, so a title costs no new endpoint — one card, a row per trophy, the count and the years. Three gates: **a headline is not a trophy** (branded kickoffs and neutral-site games share that field, so only known wording counts and anything else is silent — `Postseason.collegeName`'s pattern); **a series is decided by its last completed game**, which is also right for a single-game final, so one rule covers all four leagues and `competitions[].series` stays undecoded; and **a scheduled title game wins nothing**. `TrophyRegistry` holds the closed history before the season floor, ships **empty**, and is gated so an unpopulated league reports only what the wire says under a "since 2014" caption. The tab is **unconditional** — the one departure from "a tab only when they've won things" | Andy's ask. The data gap it closes is the familiar one: that headline is in every payload and was decoded on the scoreboard path only, so `ScheduleCompetitionDTO` never declared `notes` and a team's own title games were unnameable on its own page — W4c's "`Game.headline` wasn't decoded at all", one layer over. The registry is empty because no route to verifying a few hundred rows of sports history was reachable from the session that built it (ESPN, every reference site and every encyclopedia blocked; only truncated search summaries came through), and because three calls precede the data and are Andy's: which selector is a national title (Alabama claims 18, the AP says 13, title games say 6), whether pre-Super-Bowl NFL championships count, and where a defunct franchise's titles go (the original Senators' eleven Cups belong to no present-day team). The tab can't be conditional for the reason every other per-availability tab in the app *can* be: `Roster` gates on a client capability and `Box score` on a summary already fetched, where "has this team ever won anything" needs a dozen season requests — a tab that materialises seconds after the page opens reads as a bug, and gating on the current season alone would hide it on a team that won its conference last December. Mirroring the classifier over every real headline before it compiled caught the bug that mattered: "NBA Cup - Quarterfinals" was scoring a trophy, because "Quarterfinals" ends in "final" — and the NHL called its early rounds "Stanley Cup Quarterfinals" for decades, so a first-round exit would have been handed the Cup |
| 2026-09-13 | **The Game info card outlives the kickoff** — it stays on the Summary tab for a live and a final game instead of hiding the moment there are scores, in the slot it has always had (under the Gamecast strip, above the line score). The **Venue** card's placement rule is untouched: up top pre-kick, back down beneath the standings once scores exist, where it places the game and counts the crowd. Web took the same change in the same commit — its rail loses the `!scores` half of the gate, and the two cards now sit together there at every state | Andy: "once a game starts on the game detail page, the game info card is hidden. I don't want that." The card answers when this is, what it counts toward and where to watch it — and only the first of those three stops being a question at kickoff. The network in particular is *most* wanted while a game is live, which is the 2026-08-29 finding that put a network line in the live header in the first place (that line stays: it is the header's glance, this is the page's record). The kickoff time keeps earning its line too — "what time did this start" is a real question on a game you opened at halftime, and on a final it is the only thing on the page that dates it |
| 2026-09-13 | **A team's Overview tab gets a Venue card** — the game page's card addressed to a team: the ground's name over its city (`VenueHeadline`, extracted from `GameInfoRows` so both cards say it the same way), then **Home games** beside **Avg. attendance**. It costs **no request**: ESPN ships a venue on every competition of a team schedule, played or not, so the home ground is the venue the team's own non-neutral home dates keep naming (`TeamVenue.home(from:)` — modal, not first-seen, ties to the earlier date). Attendance is a season average over published gates only, its denominator spoken rather than printed. **No capacity row and no surface row**: ESPN publishes no capacity anywhere, and the schedule payload carries neither `grass` nor a venue id to chase one with. Web took the same change in the same commit | Andy: *"Team Overview tab needs to include the venue card for where its home stadium is and what their details are."* The page could name a team's conference, record, next game and squad and not the ground it plays on — the one fact about a team that never changes. Modal rather than first-seen because a hurricane week or a re-turfing moves one date, not a season's home, and neutral sites are out by definition, which is what keeps Georgia's home Sanford Stadium rather than the Mercedes-Benz Stadium it opens in. A *season average* because a single gate belongs to a fixture and this card is about a team. The two absent rows are the 2026-09-10 per-venue-request deletion holding: a row that can never render is worse than no row, and the payload that would rescue these does not exist |
| 2026-09-13 | **Game detail gains an H2H tab** — a series tally (two marks, two numbers, the leader's in ink and the other muted) over the previous meetings as `GameRow`s that push their own detail page. It is offered from the first frame and outside the summary gate, so a pre-kick page now carries a tab row where it deliberately carried none (2026-09-05) and a page whose summary *failed* still has a working tab. **The tally says what it counted**: ESPN publishes no head-to-head resource, so a series is walked out of one team's schedules season by season and the record is "Since 2017", never all-time — `windowLabel` rides in the card header where the number cannot be read without it. The window is anchored on the game's own season and looks back `League.headToHeadSeasons` (CFB 10, NFL 6, NBA/NHL 3), sized in *meetings* and expressed in seasons; only one side's schedules are fetched (a meeting is in both), exhibitions are excluded by a new two-phase `seasonGames(teamId:year:)`, and a season that fails is dropped while a series where *every* request failed throws — "no meetings since 2017" and "we couldn't ask" look identical on screen and only one is true | Andy's ask. The closest thing ESPN ships is the summary's `seasonseries`, and it is not close: **this season's** meetings only (the captured NBA and NHL summaries carry four Knicks-Nets games; college football's ships none at all), so a tab worth having had to be assembled. The cost is the honest part — two requests per season, 20 for college football — paid once on an explicit tap, bounded three seasons wide, cached for the page's life and never polled; `headToHeadSeasons` is the dial. A 2019 page shows the series **as it stood in 2019**: what these two have done since is not history that page can have known about. **The window floor and the request bill are reasoned from the schedule endpoint's verified shape, not probed** — this session had no route to ESPN — so the depth wants an eyes-on check against a real rivalry; every field is already optional, so a wrong guess is a short series, not a crash |
| 2026-09-13 | **The NFL's standings table is ESPN's own spread** — W, L, T, PCT, HOME, AWAY, DIV, CONF, PF, PA, DIFF, STRK, in that order, on the league page, both conferences, every division and a team page's Standings tab. W/L/T **replace** the OVR summary rather than joining it. Twelve columns don't fit a phone, so the identity half **pins** at the leading edge and the numbers scroll under their own captions beside it (`League.standingsScrollsHorizontally`) — which means the entity pages' tab swipe stands down on that pane, the bracket's rule for the bracket's reason. The game page's matchup slice keeps the CONF/OVR pair. Other leagues are untouched | Andy: "we need to display stats better other than just W and L in conference and overall." **Every one of those stats was already in the payload** the standings request returns — `wins`, `ties`, `home`, `road`, `vsdiv`, `pointsfor`, `pointsagainst`, `pointdifferential`, `streak` — and the mapper was reading two of them; `streak` had been decoded since 1.x with nowhere to render. Three columns beat one string because a tie is a real NFL outcome that "3-0" hides, and because PCT is what the table is ranked by. Pinned rather than a fitted subset: picking four of ESPN's twelve is picking which question a fan is allowed to ask, where a scroller costs one gesture and answers all of them. `standingsScrollsHorizontally` is a per-league fact, not a measurement — a set that fits must never become a scroller, because a scroller says there is more here and there wouldn't be |
| 2026-09-13 | **The privacy policy and the support page become routes on statside.co** — `/privacy` and `/support` in `web/`, prerendered static, rendered from markdown by a hand-rolled subset parser; a site footer on every route links both, and neither takes the nav bar's back chevron. The text is `PRIVACY.md` at the repo root, **mirrored** into `web/src/content/privacy.ts` under a byte-equality test that fails Web CI. `docs/index.html`, `docs/privacy.html` and the separate `weirhere/statside-site` repo retire with them | Andy, on finding he still had a second repo for two pages. Those two pages were the App Store's **Support URL and Privacy Policy URL**, served by GitHub Pages from that repo — which is private, with Pages not enabled, so **both had been 404**: a required App Store field pointing at a dead link, and nothing noticed because nothing checks a listing field. The copy being kept in step by hand was seven weeks stale on top of that, still claiming StatSide had "no server of its own" (retired 2026-09-10) and still selling a college-football app with a season picker that left the Scores screen on 2026-09-05. **Mirrored rather than read at build time** because Vercel builds this app with `web/` as its root directory: a `readFileSync` reaching above it works locally and could fail *only* in production, which is the one failure mode a legal URL must not have — so the copy is a copy CI cannot let drift, the parity ledger's own mechanism. The parser is hand-rolled at ~90 lines because the Don'ts require a conversation before a dependency and these are the only two prose documents in the product. The chevron exemption is the small one worth writing down: a chevron is `router.back()`, and these are the two URLs people arrive at **cold**, where there is no history to go back to |
| 2026-09-13 | **statside.co keeps Vercel Web Analytics and the policy says so** — a "What the website collects" section (page views, no cookies, no profile, nobody followed across sites) beside a "What the app collects" one that still opens **"Nothing."**. The App Store's "Data Not Collected" label is untouched: it describes the app | Andy's call, asked directly while the policy was about to be published on the page running the script. Analytics landed on 2026-09-09 (#110) at Andy's explicit ask, so the Don'ts' "explicit conversation" rule was honored — what never happened was the **write-down**, in either the decisions log or the policy, and `/_vercel/insights/script.js` answers 200 on production today. A page at statside.co/privacy claiming "no analytics" would have been false about the one claim on it a reader could check from the page itself. The iPhone app is unchanged and still collects nothing, which is why the two now have headings instead of one blanket answer |
| 2026-09-15 | **The app asks for a rating, at exactly one moment: a kickoff reminder fired and the user tapped it through to the game.** `ReviewPrompt` (`sports/Stores/ReviewPrompt.swift`) holds an in-memory arm set by `NotificationDelegate` on the tap and named by *game id*; `GameDetailScreen` spends it once that game's summary is on screen and loaded, waits 1.5s for the push animation to settle, then fires SwiftUI's `requestReview` (StoreKit's, so the zero-dependency rule is untouched). Guards, in order: the page must have loaded (a failed fetch leaves the arm intact — a game that never arrived hasn't earned anything), one ask per `CFBundleShortVersionString`, and a DEBUG-only `-review.prompt off` kill switch for UI suites. Opening any other game leaves the arm alone; the arm dies with the process | Closes open question #7, raised 2026-09-08 during a marketing pass and unscheduled since. `requestReview` appeared **nowhere in the app**, so a rating had no route to happen short of a user finding the listing themselves — and ratings feed both App Store search ranking and conversion, which is the funnel a 200-install app lives on. Andy, 2026-09-14, asked what to do instead of monetizing at this size: this is the cheapest of the three levers in `docs/monetization.md`. The trigger is the one written down in the open question rather than a launch count, because a launch count asks during whatever the user opened the app to do, and this moment is the app having just kept its only promise. Per *version* rather than per install: Apple throttles the system prompt itself and whether it renders is out of our hands, so the politeness we control is not asking twice about the same shipped thing. The kill switch is DEBUG-only so no release build can be argued out of asking |
| 2026-09-15 | **`aps-environment` is restored to `Config/sports.entitlements`** (`development`, both configurations), **ahead of the broadcast service rather than with it** — superseding the entitlements comment's own "restore it in the same change that ships the service." One key, no second Release entitlements file: Xcode substitutes `production` when exporting for App Store distribution. CI is unaffected (it passes `CODE_SIGNING_ALLOWED=NO`), and nothing in any build can use push until `LiveActivityController.isAvailable` is true, which remains a DEBUG-only default with no channels configured | Andy is standing up the APNs credential (Key ID and Team ID in hand, 2026-09-15) and **first light needs a real device** — the simulator cannot receive pushes — which needs the entitlement on the build. The original removal was right for its moment and wrong to keep: it was removed because the App ID carried no Push Notifications capability and signing failed on a profile that could not include the entitlement, and that reason evaporates the moment the capability is ticked, which is step 1 of the key walkthrough. So the sequencing constraint was never "ship the service first," it was "have the capability first," and conflating the two is what would have blocked a device test on an unrelated blocker. The comment now says what breaks and what to do about it (turn the capability on, never delete the key again) because deleting it is how the sequence got lost the first time |
| 2026-09-15 | **The Live Activity broadcast tick is 60 seconds, driven by an external pinger rather than Vercel Cron.** The 30-second target is retired: CLAUDE.md's "no faster than every 30s" is a *ceiling*, and `broadcast/route.ts`'s `STALE_AFTER_SECONDS = 120` ("two ticks' grace") had silently assumed 60s all along. cron-job.org's free tier at 60s, `Authorization: Bearer $LIVE_ACTIVITY_CRON_SECRET` against the existing route; **`vercel.json` is not created and never existed** — the route's own header had pointed at one for five days. `web/.env.example`, which documented neither the cron secret nor any APNs variable, now documents all six | Andy, asked the three questions the write-up ended on: *"60 seconds is fine, go with the pinger."* Pro is the destination rather than the start — Vercel's terms put commercial projects on it, so E18 shipping anything paid requires it anyway, and that is the day $20/month has a reason beyond the cron. Two things recorded on the way, both consequences of the pinger rather than of the cadence: the URL and secret now live in a third party's dashboard, which is what makes the shared-secret guard load-bearing rather than tidy; and **the tick is unconditional**, because nothing can know whether a game is live without asking ESPN, so 24/7 at 60s is ~5,760 scoreboard requests a day against a polite-guest rule that says *only while games are live* — with no state in the service, the pinger's own schedule is the only thing that can honor it, so the setup bounds it to game windows. Doing better in code means caching the next kickoff, which is state this service deliberately does not hold. First light stays hand-driven regardless: a pinger against an endpoint that has never talked to Apple tests nothing |
| 2026-09-15 | **The APNs transport moves from `fetch` to `node:http2`, because APNs speaks HTTP/2 and Node's `fetch` does not.** `sendBroadcast` takes an `ApnsTransport` seam (`url, headers, body → {status, apnsId, body}`) whose default is a new `http2Transport`; the third argument stops being a `fetch` stand-in and tests inject a transport instead. A 10s session timeout sits well inside the route's `maxDuration = 60`, so a hung connection costs one game's update rather than the tick. **Two things landed with it**: `sendBroadcast` catches, returning `{ok: false, status: 0, reason}` — status 0 because nothing was sent — with `sendFailureReason` naming an unreadable key separately from any other throw; and the poll route's response gains `channels` (size of the configured map) and `leagues: [{league, games, live, matched, error?}]` *(the `live` field is renamed `eligible` 2026-09-15, the day it first reported: it counts everything past pre-game, finals included, and read `live: 16` on a night with one game in progress)*, with the per-league catch carrying its error rather than swallowing it | **The first time this code ever ran against Apple it could not have worked, and the failure was invisible.** Production answered HTTP 500 with an empty body; the Vercel log said `TypeError: fetch failed` with an `HTTPParserError` cause. undici hands HTTP/2 binary frames to its HTTP/1.1 parser and throws, because APNs has been HTTP/2-only since the provider API existed. The 2026-09-10 row's "wire format verified against Apple's published specification" was true of the headers and payload and silently untrue of the thing carrying them — verification against a document cannot catch a transport that was never run. `node:http2` is built in, so the no-dependency rule holds for the same reason `node:crypto` signs the JWT. The catch and the accounting were built first, chasing the same 500 on the theory that a flattened PEM was behind it; they were the wrong diagnosis and the right instruments, and they stay because `pushed: 0, failed: []` had four indistinguishable causes and this route's only consumers are a pinger and a person with curl — **the response is the log**. The cheaper lesson, recorded because it cost four rounds: the Vercel runtime log had the answer the whole time, and nobody looked until after a PR had been written |
| 2026-09-15 | **`apns-expiration` becomes a required field on `BroadcastRequest`, and the poll route sets it from the game's own clock**: an `update` expires at its stale date (`asOf + 120`), a final at `asOf + 3600`. The header is now emitted unconditionally rather than only when a caller supplied one | The first broadcast that ever reached Apple came back **`BadExpirationDate`** — which was the good news, because it meant HTTP/2 worked, the JWT verified, the channel resolved, and Apple was rejecting one field rather than the request. APNs requires an expiration on a broadcast; absent, it reads as 0 and is refused. The field was optional in the interface, the route never passed one, and **nothing could catch that** — a live APNs round trip is the one thing CI cannot do. So the type is the guard: making it required turned an invisible runtime rejection into seven compile errors, all in tests, in one `tsc` run. The values are a product decision rather than a formality. An update is worthless once the card it would land on has gone stale, because delivering it puts a two-tick-old score on a lock screen, so an update dies at its own stale date; a final is worth an hour, because a phone put down before the fourth quarter should still come back to a dismissed card and the right result. An hour also sits well inside the 8 hours a most-recent-message channel stores |
| 2026-09-15 | **APNs accepted a broadcast** — `{"status":"ok","pushed":1,"failed":[]}` against a live NFL game, sandbox, from production. The service doc's fifteen-week-old status line, *"never once run against Apple,"* retires, and `docs/live-activities-service.md` now leads with what the response does and does not prove | Worth a row for the gap it closes rather than the line it adds. Three defects stood between the code as written on 2026-09-10 and a single accepted push, and **not one was findable by reading**: the transport was `fetch` against an APNs that has only ever spoken HTTP/2; `apns-expiration` was absent and is required on a broadcast; and an unhandled throw in the send path rendered both as an empty-bodied 500, which is the least informative failure a route can produce. The wire format *had* been checked against Apple's published specification and was correct — headers, payload, the epoch-seconds `asOf` trap and all — which is the lesson: **a document can verify a payload and cannot verify a socket.** What is proven now is enumerable because every step had been a guess: `node:http2` reaches APNs from Vercel, the ES256 token verifies against the real key, the channel resolves, validation passes. What is **not** proven is the thing a user would call the feature — `pushed: 1` means Apple accepted it for fan-out to a channel with zero subscribers, and a card on a lock screen still needs a debug build on a real device with the activity started. That distinction is written into the doc's status so a future reader does not mistake the first for the second |
| 2026-09-17 | **A day that never loaded now says so, and the loading skeleton is visible in light mode.** Two defects, one screenshot. **(a)** `SkeletonRows` drew `bgElevated` bars on the `bgRecessed` page — **both 0.93 white in light mode** — so every bar, disc and section header was invisible and the screen rendered as three featureless gray slabs with two hairlines across them. It now mirrors the slate it stands in for: `cardSurface()` cards with `bgHeader` headers, so the 0.93 bars sit on 1.00 white, and one accessibility element labeled "Loading games" instead of nine anonymous shapes. The `#Preview` moves from `bgCard` to `bgRecessed`. **(b)** `ScoresScreen.emptyState` read *any* unloaded day as "still loading", and a failed fetch never writes its day — so the error copy and its Retry button, which live in the loaded branch, were unreachable on a total failure. `LeagueScoreboards.isStalled(on:)` (attempted, not loading, not loaded) now splits a dead end from a wait, backed by a load counter rather than a flag so overlapping day changes, the 250ms settle delay and a no-op divisions change cannot read as failures. `ScoreboardStore.describe` gains three sentences where it had two: "No connection.", "The scoreboard timed out.", and "The scoreboard is unavailable (\(code))." | Andy, on a Thursday in week 3 with a full slate: *"Why are no games showing up on any day? I even turned my phone off turned it back on. There's no update."* The app had in fact recorded an error on every league, and had no surface anywhere able to show it — **both error surfaces were gated on data having arrived**: the refresh banner requires `!sections.isEmpty`, and the empty state's error branch requires a loaded day, so a *total* failure is the one case neither covers. That is the shape worth remembering, and it generalizes past this screen. The invisible skeleton is why it read as an empty app rather than a stuck one; the `#Preview` is why the collision survived a month, because it alone set `bgCard`, the single surface where 0.93 bars are visible. **The root cause of the fetch failures themselves is still unknown** and could not be found from here: ESPN is unreachable from the dev environment, and the app's own error string was the only remaining evidence — which is exactly what was being swallowed. The status code in the copy is the instrument that closes that loop, being the difference between "they moved the endpoint" and "your wifi is out" |
| 2026-09-17 | **The web app had the same stranded error, and a worse one — corrected the same day the iOS row above claimed it did not.** Andy's second screenshot: `statside.co` blank under its own skeletons, on the same Thursday slate. Four changes. `scores-view.tsx` takes the iOS `isStalled` gate, so the skeleton covers a load that is still out and nothing else. `use-league-scoreboards` sets `error` when **any** league fails rather than only when all four do, holds `isLoading` across the 250ms settle so a swipe cannot flash a false failure, and exposes `isStalled` (attempted, not loading, not loaded). `/api/scoreboard` forwards ESPN's own status as `upstreamStatus` while staying a 502 — the upstream status describes ESPN's answer, not ours — and `lib/api.ts` raises a typed `ApiError` carrying it, so the screen can print "The scoreboard is unavailable (404)." Finally `page.tsx`'s per-league `catch {}` logs. 8 new tests, 471 passing, typecheck, lint and `next build` clean | **The two apps failing together is the finding, and it moves the suspicion off both of them.** The iOS app reaches ESPN from Andy's phone; the web reaches it from Vercel, because the browser only ever talks to `/api/scoreboard`. Two networks, two runtimes, two HTTP stacks (`URLSession` and Node `fetch`), and even two different request shapes — the web asks `limit=900` with no `groups`, iOS asks `limit=400&groups=80` — all blank at once. What they share is `site.api.espn.com/…/scoreboard?dates=`. The prior row's parity claim was wrong on the half that mattered: `(main)/error.tsx` catches nothing here, because the seed catches per league and returns `undefined` instead of throwing, and the client gated its error behind `!isLoaded` exactly as iOS did. The partial-failure case was worse than iOS ever had it — one dead league left `isLoaded` false forever with `error` still null, a permanently skeletal screen the app considered healthy. The status forwarding exists because the root cause is *still* unknown and unreachable from a dev environment: ESPN and `statside.co` are both blocked by network policy, and StatSide is not on the connected Vercel team, so its runtime logs — which the route has been writing to `console.error` all along — could not be read either. The 2026-09-15 APNs row's lesson, that the Vercel log had the answer and nobody looked, applies again and is the first place to check |
| 2026-09-17 | **Found it: ESPN withdrew the `dates=A-B` range form, and dropped `limit`'s ceiling to 500.** The open question from the two rows above — the one both apps' error copy was built to expose — has an answer, and it is one request shape. `dates=20260915-20260919` now returns `400 {"code":400,"message":"Failed to get events endpoint."}` in all four leagues, for past seasons too, including `dates=20260904-20260908` — the exact window `ESPNClient` cited in a comment as verified live twelve days earlier. Six probes for six, and no separator, casing, `seasontype` pairing or dropped `groups=` revives it. Single-day, month (`202609`) and year (`2026`) tokens all still answer 200. Separately and just as quietly, `limit=501` no longer clamps: it collapses the response to ESPN's default 25 events, still under a 200. So `scoreboard(days:)` fans out and merges by event id: one request per day up to a week, one per month beyond it, in one task group, any request's failure failing the whole window and the answer clipped back to the span either way. `seasonGames(days:)` moves to one request per calendar month, clipped back to the span on the Eastern clock, with a month that comes back at the limit re-asked as its own days. `halve`, `maxWindowSplits` and `datesToken` are deleted with the range form that was their only caller, and `seasonWindowLimit` drops 900 → 500. 672 tests pass; verified live on the simulator, Scores and the NHL's full-league Games tab both | **Two apps, two networks, two runtimes, one URL — so the URL was the thing, and the previous row was right to say so and wrong to stop there.** What made this expensive to see is that both failures are *silent by design on ESPN's side*: a withdrawn parameter form answering 400 rather than ignoring the parameter, and a raised `limit` answering 25 rather than clamping or erroring. The second is the more dangerous of the two and was already live before this: `seasonWindowLimit` was 900, which is above the ceiling, so every season request had been answering with 25 games under a 200 — and the truncation guard, which watches for a count *at* the limit, could never once have fired. The guard was sound; the constant it guarded had drifted outside the range where the signal exists. That is the `dates=` silent-truncation rule turning up in a second form, and it is why the new limit is pinned to a bisected number with the bisection written down rather than to a round one that looks safe. The month replaces halving because halving needs to state an arbitrary sub-range and there is no longer a way to say one; a season is six or seven months, the same order of requests the halving rule already spent on the NHL, and unlike a half-span a month is a fixed, knowable size. Measured live the same day: college football's September is 323 events and the NHL's January 231, so the day fan-out is a guard that does not fire in normal operation rather than a routine cost. The day granularity for the Scores window is not a preference either — it is the only one left that can state five days exactly. The week-wide threshold above it exists because `firstDayWithGames` sweeps a fortnight at a time across all four leagues at once: at day granularity that is 56 requests on a quiet launch where it used to be four, which is the kind of thing that turns a polite guest into a rate-limited one. A month is always the cheaper request and never the cheaper answer — September is 323 events against a Saturday's 70 — so the polled end stays daily and only the one-shot probe goes monthly |
| 2026-09-17 | **The web half of the `dates=` fix, plus the November 1 split it finally retired.** `espnDayRange()` is replaced by `espnWindow(start, end)`, which returns day tokens up to a week and month tokens beyond it; `dayWindowUrl` and `seasonWindowUrl` now take one token rather than a span, and both default to the new `WINDOW_LIMIT` of 500 instead of 900. `scoreboardForDays` fans out over the tokens, merges by event id and clips to the span with a new `clipGames`; the first token's payload speaks for the window's season metadata the way the whole range's used to. `conferenceGames` drops its November 1 split for one request per calendar month, re-asking a month as its own days if it comes back at the limit. `canTableAWholeSeason`'s comment is corrected but its behavior left alone. 479 tests pass (11 new), typecheck, lint and `next build` clean; verified against live ESPN on the dev server — today's slate, the ACC's full season behind and in front of the earlier-games fold, and 200s from all four leagues through `/api/scoreboard` | **The web carried the same two bugs and a third the ledger had been recording as pending since 2026-09-10.** The range form and the 900 limit were identical to iOS, which is what the previous row predicted from the fact that both apps blanked at once from different networks. The extra one is the more interesting: `conferenceGames` still split its season at November 1 — a college-football date wearing the shape of a rule, right for a dense autumn and a thin postseason and right for nothing else, which iOS replaced with the halving rule and which the web never caught up on. Month tokens retire the question rather than re-answering it: a month is a fixed knowable size, so there is no date to pick and no league to pick it for. Left standing deliberately: `canTableAWholeSeason`, the gate that sends the NBA and NHL to a rolling four-week window instead of a season. Both halves of its reasoning have expired — the `groups=` claim was refuted live on 2026-09-10, and the 900-event window it is sized against no longer exists in either direction now that the densest month in any league we cover is ~240 against a limit of 500 — but retiring it would change what an NBA conference page *shows*, and a product change does not belong inside the fix that outdated it. Its comment now says so, which is the difference between a known gap and a stale one |
| 2026-09-19 | **A screenshot never names the price, including "free"** — the App Store closer frame (`as-08-closer.html`) drops "Free. Fast." for "Fast. Focused.", the two words the charter already uses for what the product is. The Description keeps "Free."; the `docs/social/` thread and Story closers keep it too | Apple rejected 2.3.0 build 17 under Guideline 2.3.7 on review date 2026-09-19, and its note is explicit that free counts: "references to free or discounted services are considered a price reference." The same note points at where price is allowed — the description — so the two fields diverge deliberately rather than drifting, and a future audit that syncs them re-earns the rejection. `01-hero`'s "No ads" pill and `07-quiet`'s four pills stay: 2.3.7 is about price, and an ad-free app is a description of the product, not of what it costs. Screenshots are metadata, so the fix is a replaced PNG and a resubmit, not a build |
| 2026-09-19 | **A roster group reads in jersey order, not ESPN's alphabet** — `ESPNMapper.sortedByJersey(_:)` sorts each group's players ascending by number before it becomes a `RosterGroup`, with the numberless behind them in the order they arrived; `transformRoster` takes the same sort in the same commit, so both halves ship together | Andy, on South Florida's offense: "Let's order the roster in numerical order." ESPN ships each group alphabetical by last name — 65 Acevedo, 12 Alexis, 76 Bellomy — which answers a question nobody asks a roster. The number is how a player is identified on a broadcast graphic, a depth chart and the field, and it is the column the card already leads with, so scanning for one should be scanning down a sorted column. The **grouping** is still ESPN's, which is the rule that matters here (deriving guards and forwards from positions would invent a structure the payload doesn't have); an order within a group is a reading decision, not a structure, so it's ours to make. Numeric rather than lexical — a string sort files 7 behind 14, which is exactly Kansas City's special teams. Anyone ESPN ships no readable number for keeps the tail alphabetical rather than being sorted to the front as a 0: that's 10 of 18 Lakers in preseason, so the tail is a real section of the card. The stability is written by hand on the iOS side because Swift's sort gives none — college football runs duplicate numbers on purpose, and a card that reshuffled its ties on every decode would be worse than the alphabet |
| 2026-09-20 | **The hero tab row scrolls itself, and `<main>` clips whatever still doesn't fit.** `HeroTabBar` was a bare flex row, so a five-tab team page on a 390pt phone made the *document* 445px wide and dragging the tabs slid the nav bar, the hero, the cards and the bottom tab bar sideways with them. It is now an `overflow-x-auto scrollbar-none` scroller whose buttons are `shrink-0`, and it carries its own `px-4` gutter — `HeroHeader` and `GameDetailView` hand it an unpadded surface — so tabs scroll out at the surface edge instead of being sliced off 16px short of it. Selecting a clipped tab reveals it nearest-edge with a 16px gutter, jumping on first paint and animating after, the `DayStrip` rule. Under it, `<main>` gets `overflow-x: clip`. Measured in headless Chromium at 390×844: document 445→390px, strip 461px inside a 390px box, nav/`h1`/card left edges unmoved through a full strip scroll, sticky row still pinned at `top: 56px` after a 1200px vertical scroll. 479 tests, typecheck, lint clean | Andy, from Safari on a USF roster page: *"the web app is shifting the entire app as I scroll the horizontal tabs."* The interesting half is the guard, not the scroller. `clip` and not `hidden` because `hidden` makes `<main>` a scroll container, and a scroll container re-anchors every `position: sticky` inside it — the pinned tab strip and its control chips are exactly what would have broken, so the obvious one-word fix for a horizontal-scroll bug is the one that quietly breaks the header this bug is about. `clip` clips without scrolling and leaves sticky resolving against the viewport, which the probe checked rather than assumed. The guard earns its place because this class of bug is invisible in the only place it is cheap to catch: on a desktop the tabs fit, so nothing overflows, and the row is only too wide on the phone where nobody is running DevTools. Every row that can outgrow a phone now scrolls itself — hero tabs, day strip, standings table — and `clip` is the net under the next one. It also means a future overflow fails by being cut off rather than by shifting the app, which is the better of the two failures but still a failure: the net is not a licence to stop giving a wide row its own scroller. Left alone deliberately: the iOS `HeroTabBar` is a bare `HStack` with the same five tabs and no `ScrollView`, which cannot shift an app but can compress the labels. That is filed in BACKLOG E5 as unverified, because it needs a simulator and a screenshot, not an inference from font metrics |
| 2026-09-20 | **The widget's footer timestamp is a button** — `arrow.clockwise` + "Updated 8:01 PM" as one `Button(intent: RefreshWidgetIntent())`, the stale "as of" line included — **and the live ask tightens 15 minutes to 5**, paid for by the nothing-is-live tick going 1 hour to 4 (`GameSelection.liveInterval` / `quietInterval`). Empty states (`noFollows`, `noGames`) move to the quiet tick too | Andy, with an 8:09 clock over an "Updated 8:01 PM" footer and two live games on it: "so much can change during this 8 minute window." A widget cannot poll, it can only *ask*: WidgetKit answers out of a daily reload budget it weights toward the widgets a person actually looks at, so asking for 15 capped the app below what the system would hand it on the one day it hands out most — a fall Saturday, when the target user checks twenty times. Five takes whatever that attention earns and degrades to the old cadence when it earns nothing, which is why the button is the load-bearing half and not the consolation one: a reload a person asked for is the only reload the budget doesn't charge for, so it is the only refresh the app can *promise*. The hour was the wrong place to spend the rest — a fixture three days out reads the same at 2pm as at 1pm, and the two things that genuinely expire (a kickoff, a midnight day line or spent result) already pull the tick in on their own, so hourly bought twenty-odd reloads a day of nothing out of the live window's allowance. The control is chrome: monochrome `.plain`, the `meta` weight the line already had, no second red. Medium keeps the footer for staleness only — its content box cannot fit masthead, two cards and chrome (2026-08-23), and squeezing the cards to buy a button is the trade that rule refuses |
| 2026-09-20 | **The Games tab tap goes home** — tapping Games while Games is already on screen pops whatever the tab has pushed and scrolls the slate back to its top, animated. `RootView`'s `TabView` selection becomes a computed `Binding` (a re-tap runs the setter with the value it already holds, which is the only place the gesture is visible); a Games re-tap bumps the new `Router.scoresHomeCount`, and `ScoresScreen.goHome()` clears the path and reuses the Today jump's `slateHome` anchor. Home is the top of *this* page and not a reset of it: the day, the Live filter and the accordions are left exactly as they were | Andy: "Tapping the games tab should always load the user at the top of the games page even if they're already on the games page and have scrolled." The tab bar was doing nothing at all on the tab it was already on, so a slate left at the bottom of the Big Ten stayed there and the only way back was to thumb it — and a re-tap is the gesture people arrive with, from every other tab-bar app on the phone. Going through the Router rather than a parameter keeps one channel for intents that come from outside the view hierarchy, the widget tap's channel, and leaves the other two tabs free to claim the same gesture later. The day is deliberately not part of home: going back to **today** is a different move with a different answer (a day two weeks out is often exactly where you meant to be), it already has a control of its own, and that control is on screen on every day that isn't today |
| 2026-09-20 | **A roster row is a link, and the page behind it is Profile alone.** `PlayerIdentity` carries the row's own facts into `PlayerPage` — no fetch, because there is no endpoint to fetch from — and the destination is registered in all four stacks (Scores, Tables, Teams, AddTeamsSheet) with `.id(player.id)`, the id namespaced by league the way `FollowKey` is. **No tab row**: the design settled on four tabs and three of them (Games, Stats, Career) have no confirmed source, so the page shows the one it can fill | E13 kept roster rows deliberately unlinked because *"a chevron would promise one"*; E20 re-opened that, and the promise and the page now ship together. The one-tab decision is the app's own rule rather than a new one — an empty roster hides the Roster tab, game detail hides its whole tab row with no box score — applied to the same problem: three dead tabs promise pages that don't exist, which is the mistake the unlinked rows were avoiding. The door is the roster and not the box score, inverting E20's own value ordering, because a roster row holds every Profile fact and a box score row holds a name and a stat line. `scripts/probe-athlete.sh` is the gate on the other three tabs |
| 2026-09-20 | **A player URL carries the team id** — `/player/{league}/{teamId}/{athleteId}`, and the page finds the athlete inside the roster it fetches for that team | A URL has to rebuild its page from nothing: a shared link, a refresh, a crawler. There is no athlete endpoint to rebuild from (E20's P0 is unanswered), and the roster endpoint — the only thing that knows this player — is addressed by team, so the team id has to be in the path or the page cannot exist at all on a cold load. iOS needs none of this because its destination carries the facts in memory, which is the first place the two implementations have genuinely diverged in shape rather than in timing. One request, no new endpoint, and 404 for an athlete id this team has no player for |
| 2026-09-20 | **A hero with no tab row pays for its own bottom padding.** `HeroHeader`'s identity block never had one — the tab row supplied it, because `HeroTabBar`'s labels carry 14px above them on the same card surface. The player page is the first hero in the app with no tabs, so its card colour stopped flush against the subtitle's descenders. Web-only: `!tabs && "pb-4"`. | The padding was always optical rather than owned, and nothing noticed while every hero had tabs. Andy caught it on statside.co and read it correctly on sight — *"probably because there's optical padding when tabs exist"*. Fixing it on the header rather than on the player page keeps the next tabless hero from re-learning it. **iOS needs nothing**: `PlayerPage`'s hero is inline in a `VStack(spacing: .lg)` on `bgRecessed`, not on a card-colour header surface, so there is no bottom edge to sit flush against. |
| 2026-09-20 | **A box score name links to the player page, and only when ESPN sent a real id.** `BoxScorePlayer` gains `athleteId?: string` beside `id`: `id` stays the list key and keeps its `${teamId}-${name}` fallback, `athleteId` is `nonEmpty(athlete.id)` and is the only one a row routes on. The link carries the team from that game's box score. | E20 named this trap before it was built: a synthesized key must never become a navigation destination or the app pushes a page for an athlete who doesn't exist. Identity needs a value always, navigation needs one only when it's genuine, so they are two fields. **One dead end accepted rather than hidden**: the player page looks the athlete up in that team's *current* roster (no season axis, 2026-09-10), so a box score from a season the player has since left 404s. Every door into the page has that floor, and one athlete endpoint fixes all of them. |
| 2026-09-20 | **An empty athlete id is not an id.** The box score's row key was `athlete.id ?? "\\(teamId)-\\(name)"` on both platforms; `??` only catches nil, so ESPN's empty string became the row's identity and two such rows in one category collided. Now `nonEmpty(...) ?? ...`. | ESPN's two ways of not knowing a field are absence and `""`, and the codebase already had `nonEmpty` for exactly that on both sides — this one line just never used it. Found by writing the `athleteId` test above rather than in the wild, which is the argument for testing the boring half of a change. |
| 2026-09-20 | **The team on a player's hero is a badge that links to the team page.** Crest and team name move into a pill on `bg-bg-elevated`; the rest of the line (`#2 · WR`) stays quiet text beside it. The badge is **not** inside the `aria-hidden` the rest of the meta line carries. | The team was already the one part of that line with somewhere to go, and it didn't look like it. The accessibility half is the load-bearing part: a focusable element inside `aria-hidden` is reachable by keyboard and invisible to a screen reader, which is worse than the duplication of announcing the team twice. `playerMetaLine` handed no team name already returned exactly the rest of the line, so the split cost no new helper. |
| 2026-09-20 | **iOS: the team on a player's hero is a `HeaderLinkBadge` that pushes `TeamPage`.** `PlayerIdentity` gains `var team: Team?` (a `var`, for `Team.league`'s documented reason) so the badge has a value; the crest stays outside the badge because the house component is text-only. The hero's `accessibilityElement(children: .ignore)` moves off the `HStack` and its sentence onto the title. | Matching the web's badge, in iOS's own existing language rather than a second kind of pill — `TeamPage` and `ConferencePage` already say "this name is a destination" this way. The accessibility move is the part that isn't cosmetic: a control inside an ignored subtree stays tappable and leaves the accessibility tree, so VoiceOver would never reach the badge while a sighted user could. A door that knows a team's *name* but not the team (a box score row, when that opens) keeps the name in the line and draws no badge, rather than a badge that pushes nothing. |
| 2026-09-20 | **`HeaderLinkBadge`'s fill is a parameter, defaulting to `bgRecessed`.** A badge sitting on recessed ground passes `.bgCard` instead. | The badge was built for a `bgCard` header, where a recessed capsule reads as a shape cut into it. `PlayerPage`'s hero sits straight on `bgRecessed`, where that fill **is** the ground: invisible in both appearances, and `bgElevated` is no escape because it and `bgRecessed` are the same value in light (0.93). The rule underneath is that the fill is one elevation step away from whatever the badge sits on, which is a direction and not a constant. Caught by reading the token table rather than by a build, which is the only reason it isn't a shipped bug. |
| 2026-09-20 | **The Leaders card links, and the whole side of the row is the target.** `GameLeader.athleteId` (web) and `LeaderCategory.Leader.athleteId` (iOS) carry ESPN's id, `nonEmpty` on both. The web routes on it; iOS carries it without linking. The `aria-label` that was on a plain `div` becomes the link's accessible name. | E20 called this a one-line add reading the same `AthleteDTO`. True on iOS, where `id` was decoded and discarded; **not** on the web, whose `EspnLeaderEntry.athlete` shape had never declared the field, so it had to be declared before it could be read. The target is the whole side, photo and stat line included, because a name on a two-column phone card is a small tap. iOS doesn't link for the box score's reason: it hands `PlayerPage` what the calling screen knows, and a leader row knows a name, a stat line and a photo, so `profileRows` would be empty and the page thinner than the row that pushed it. |
| 2026-09-20 | **`transformLeaders` is exported.** It was the only transformer in its file that wasn't. | Not a design change, a testability one: there were zero tests over the leaders path, and the alternative was driving it through `transformGameSummary` with a whole synthetic summary payload. Every sibling transformer is already exported, so the privacy read as incidental rather than deliberate. Three tests landed on the back of it. |
| 2026-09-21 | **The game page stops fetching standings in college football; it reads them out of the summary it already has.** `/summary` ships a top-level `standings` block carrying both competing conferences in full, each entry with `total` and `vsconf` — which is exactly `matchupStandingsColumns`. `GameSummary.matchupStandings` carries them, `currentStandings` prefers them, and the speculative `conferenceStandings()` is skipped for that league. The other three keep it. | The card was costing a whole league's tables to render two rows that arrived free inside the request the page had to make anyway — a second round trip per game page open, on the screen the app's speed claim is loudest on. Per league rather than everywhere, because the payload is per league and the don't about generalizing a shape applies to data too: the NBA's and NHL's blocks ship the **division** (Boston's Atlantic, Vegas's Pacific), and short of the card's columns at that — no `total` summary in basketball, no `gamesplayed` in hockey. A division table under a caption the card would have to invent is worse than the request it saves. The NFL keeps the fetch for the plainer reason that no NFL summary has ever been captured, and a decoder written against a guessed shape is what the DTO rule exists to prevent. The fallback keeps the card honest in the other direction: a payload that comes back without the block fires the old fetch once per game — sequential, so it costs a round trip only on the path where the free copy didn't arrive, and flagged so the 30s poll can't turn it into a request every tick. CFBD lands on that path by construction, since it ships no standings block at all. |
| 2026-09-21 | **`isSameConference` is decoded by nothing, deliberately.** The summary's standings block ships the flag, and `MatchupStandings.slot(for:)` keeps deriving the same fact from the two teams' own tables. | The flag compares `conferenceHeader`, which is `"FBS"` on both sides of a college-football game — so it reads `true` for Miami (ACC) vs Indiana (Big Ten), which is precisely the case the card needs to tell apart. E21's own row named it as the fact the app derives by hand and could stop deriving; reading the payload is what showed that the hand-derived version is the only one that can't be wrong. Written down because the field is sitting there and looks like an answer. |
| 2026-09-21 | **`TeamDTO` reads `groups` where ESPN sends no `conferenceId`, and `conferenceId(from:)` learns the shape the summary and the rankings use.** A conference names its division root (FBS 80, FCS 81) as its parent, or names no parent at all; a division names its conference. `isConference` is only present on the schedule endpoint's copy. | Two payloads ship a team this way and both were decoding a team that didn't know what conference it was in: the summary header (`{id: "5"}`, bare) and every ranked team in `/rankings` (`{id: "5", parent: {id: "80"}}`). The matchup-standings row above needs it — the summary's groups carry no id of their own, so the conference each table belongs to comes from the competing team standing in it. The existing helper's rule, tuned to the schedule endpoint, answered `nil` for the first shape and `80` for the second. The rankings fix is a side effect and a welcome one: a ranked team now resolves its own conference instead of falling through to the directory. |
| 2026-09-21 | **The hero tab row's unselected ink becomes `textSecondary`, and the half-strength primary it used goes.** `HeroTabBar` drew inactive tabs in `textPrimary.opacity(0.5)`; it now draws them in the app's own audited second-rank ink. The 3pt underline stays out. | A half-strength primary composites to white 0.55 on a light card — **3.35:1** at bold 14, under WCAG AA's 4.5:1 for anything below 18.66pt bold, on the one control whose job is to say where else you can go. Dark mode measured 5.17:1, which is how it survived: half the audit passes, and the failing half is arithmetic rather than something you see. The fix is the heavier ink rather than the underline's return because the underline came out for a reason (2026-08-31, redundant with the ink) and contrast is what was actually wrong. `textSecondary` because the app already owns this decision — its value is 0.42 and not 0.44 *precisely* so it clears 4.5:1 on the darkest surface secondary text sits on — so the row stops hand-rolling an opacity over a backdrop it can't see and takes the token: 5.32:1 on a light card, 6.37:1 on a dark one, 4.55:1 worst case anywhere on the ramp. It was the only `.opacity()` on a text token in the app, so nothing else inherits the bug. Four screens change: TeamPage, ConferencePage, PollScreen and game detail. `ContrastTests` is the instrument the component never had — every ink-on-surface pair, both appearances, plus the old composite kept as a measurement so the next person reaching for `.opacity(0.5)` on a label can read the number it produces. |
| 2026-09-21 | **Recording, late, that the game share is one rich bubble and that it cost the app its second UIKit exception.** `GameShareItemSource: NSObject, UIActivityItemSource` vends the store link with an `LPLinkMetadata` carrying the rendered `GameShareCard` as both `imageProvider` and `iconProvider`; `GameShareSheet` is a `UIViewControllerRepresentable` over `UIActivityViewController`. Shipped 2026-09-10 and never written down. | A game share used to land in a thread as three separate things — the PNG, the text bubble, and an App Store link preview wearing the store's stock product shot, so the only picture of the app sat next to the picture of the game nobody had linked it to (Andy, 2026-09-05, from a share Brent sent back). `LPLinkMetadata` lets the *sender* supply the preview image, which supersedes the 2026-08-09 conclusion that repointing the preview needed per-game web pages and therefore a backend: it needs neither. `ShareLink` cannot vend link metadata, so the toolbar share drops to UIKit — the second exception after the bell's `openNotificationSettingsURLString`, and the backlog row called for this log entry when it was scoped. The tradeoff taken: a single URL item drops the image-only targets (Save to Photos, Instagram) from that sheet, so the Scores row's context-menu `ShareLink` stays untouched and keeps vending the graphic as an image. Written down now because a session walking the backlog in priority order read the row as open and came within a file of rebuilding what was already there — which is the exact failure the log exists to prevent. Still owed, and the whole feature: whether Messages transmits sender-supplied metadata *to the recipient*, which is a real device and not a simulator. |
| 2026-09-21 | **Recording, late, that the Live Activity client surface is built.** `GameActivityAttributes` splits static per-game facts from a `ContentState` of `awayScore`, `homeScore`, `phase`, `headline`, `detail` and `asOf`, hand-coded `Codable` so the broadcast service and the struct agree in writing; `GameLiveActivity` draws the lock-screen card and the full island ladder (expanded, `compactLeading`, `compactTrailing`, `minimal`); logos come from `WidgetLogoFetcher.cachedLogo(for:)`, the widget's mechanism, so a card renders once with no `.task` and no `AsyncImage`. | E12's own sequencing is the reason this went unrecorded: the client half was built deliberately ahead of the service and is gated out of the product by `LiveActivityController.isAvailable`, so nothing user-visible changed on the day it landed and nothing prompted a row. The design decision inside it is worth keeping: **there is no `stale` phase**, because staleness is not a state the game is in — it is a property of how long ago we heard, which is what `asOf` carries. A phase for it would have let the card claim a status the payload never sent. |
| 2026-09-21 | **The backlog gets reconciled against the code, on a cadence, and the PR template gains a backlog-closure section.** First pass today: four shipped features were still sitting open, one row's premise had gone false, and one row contradicted itself across two parentheticals written hours apart. All six are corrected in place rather than silently flipped. | A list you walk top-down for work has to be right about the code, and this one had drifted far enough that a session picked the next item, opened the file, and found the feature already built — twice. The cause is structural. A PR that ships something has three couplings to satisfy: a decisions row, a parity-ledger row, and CI. Two are enforced by scripts; closing the backlog row is the one nobody checks, so it is the one that rots. The cheap half of the fix is the template section, which costs a human one glance and has no false positives. The expensive half — a `check-backlog-closure.sh` that can tell *should have closed a row* from *legitimately closes none* — is not obviously solvable and is deliberately not guessed at. One methodological finding is worth as much as the corrections: an agent session's clone of this repo is **shallow**, so `git log -S` cannot date anything — it bottoms out at the graft and names the root commit, which is how a 506-file commit came to look like it introduced `GameShareItemSource` under the message about moving the CI actions onto Node 24. Provenance here comes from reading current code, never from history. |
| 2026-09-21 | **A trophy's identity folds case, and ESPN's shouting is not a second trophy.** The Rams' Trophies tab printed "NFC Championship 1 · 2021" and "NFC CHAMPIONSHIP 1 · 2018" as two rows where the shelf holds two of one thing. Three parts: `TrophyKind`'s `Hashable` now compares `(tier, singular.lowercased())` rather than the synthesized whole, so no casing can ever split a row again; the conference-championship classifier re-cases **one word** — the common noun, "CHAMPIONSHIP" → "Championship" — and leaves the conference's own letters as ESPN sent them; and where two spellings do merge, the row prints the calmer one, counted by uppercase characters with a lexicographic tie-break, rather than whichever season arrived first. `TrophyRegistry.coveredKinds` keys on the folded identity to match. **The years were checked and are correct**: this app's axis is the season a title was won in, so the Rams' NFC titles are 2018 (played 2019-01-20) and 2021 (played 2022-01-30), which is also the NFL's and Pro-Football-Reference's own convention. The Eagles' win people remember as "2018" is the game played 2018-01-21, which is the **2017** season — the same ambiguity Google's own AI Overview has to disambiguate in a parenthetical. No year changes. | Andy, off his own Rams page. The cause is the feature's founding trade: the name of a conference trophy is taken from the wire by design (`conferenceChampionship` — "a conference we have never heard of still reads correctly"), and the cost of taking the name from the wire is taking its typography too. So the fix draws the line at identity rather than at the name: the letters stay ESPN's, and only what counts as *the same trophy* stops being. Re-casing the conference's own letters was considered and refused — on an all-caps string there is no rule that tells "SEC" from "BIG TEN", and a pass that title-cases both turns one of them into "Sec". Folding identity makes that guess unnecessary, which is why it is the part that carries the fix. The conference-finals path is de-shouted whole because it can be: it only matches "<side> Conference Final(s)", three ordinary words with no acronym to mangle. |
| 2026-09-21 | **The iOS hero tab row scrolls itself too — and it was wrapping, not truncating.** `HeroTabBar` becomes a horizontal `ScrollView` carrying its own `Spacing.lg` gutter, with `lineLimit(1)` + `fixedSize()` on the labels and `scrollBounceBehavior(.basedOnSize)`; the four callers (TeamPage, ConferencePage, PollScreen, GameDetailScreen) drop their outer padding and `frame(maxWidth:alignment:)` and hand it an unpadded surface, still painting their own background full-bleed. Selection reveal follows `DayStrip` — jump on first paint, animate after, `.center` anchor. Verified on iPhone 17 Pro / iOS 26.5 against a real Georgia team page: the five tabs render on **one line** with Trophies off the trailing edge, a swipe brings Trophies fully in and pushes Overview to "…w", and the Next game / Record / Venue cards below hold their left edges through the whole scroll — the web bug's shift-the-whole-app failure has no iOS analog and now provably stays that way. | The iOS half of 2026-09-20, which fixed the web strip and deliberately left this open as BACKLOG E5, *"unverified, because it needs a simulator and a screenshot, not an inference from font metrics"*. Andy supplied the screenshot (a Rams page: "Overvie/w", "Game/s", "Standi/ngs"), and it says the prediction was wrong in the direction that matters. E5 expected the labels to **compress or truncate**; they **wrapped**. That is the worse failure and not the milder one, because this row has been a pinned section header since 2026-09-05: a truncated label keeps its row height, where a wrapped one changes it, so the strip that content slides under was resizing itself. `fixedSize` is therefore the load-bearing half of the fix and the `ScrollView` is the consequence — labels that refuse to squeeze must overflow somewhere, and a scroller is the only place that stays legible. `basedOnSize` because a row that fits must not rubber-band: a scroller says there is more here, and on the three-tab conference, poll and game pages there isn't — the standings table's own rule (2026-09-13) applied to a control instead of a table. **Deliberate divergence from web:** that strip reveals a clipped tab nearest-edge with a 16px gutter; iOS centres it, because `DayStrip` is the scroller this app's users already have their hands on every Saturday and matching the platform beat matching the other implementation. **Still owed:** `HeroTabFitUITests` compiled for the first time today (written 2026-09-20, never built) and its premise was rewritten — "does the row fit" is answered and now deliberately false, so it asserts no-wrap, no-squeeze and reachability instead — but the suite is **red for an unrelated reason**: its `openTeamPage` helper can't find `search.appWide`, so it never reaches the tab row. The fix above is verified by hand and by screenshot, not by that test. |
| 2026-09-21 | **A Games re-tap comes home to today, too** — superseding the day clause of 2026-09-20. `goHome()` now routes through `jumpToToday()` whenever `LeagueScoreboards.canJumpToToday` says there is somewhere to go; the Live filter and the accordions are still left exactly as they were, so the day is the one piece of page state that home resets. `canJumpToToday` is the entire gate and needed no new logic: it is already false on today, and already false in the offseason, where today sits outside every league's span and `selectToday()` would strand the strip on a day it cannot show — so a June re-tap still only scrolls to the top. `jumpToToday()` rather than `select(day:)` for the reason that function's own comment gives: it goes through `selectToday()`, so a strip still bounded to a past season re-bounds to this one instead of selecting a date the strip has no chip for, and it takes the slate home itself, so the `slateHomeCount` bump belongs to it on that path and must not be made twice. Verified on iPhone 17 Pro / iOS 26.5: from Wed Sep 23, a Games re-tap returns the strip and the slate to Today. | Andy: *"i still want a tap on Games to move the active day to be today."* This reverses an argument made a day earlier and worth restating so the reversal is legible rather than looking like drift: 2026-09-20 held that the day is **not** part of home, on the grounds that a day two weeks out is often exactly where you meant to be, and that the way back already has a control of its own on screen on every day that isn't today. That reasoning isn't refuted here — it is outweighed. A tab-bar re-tap is the one gesture people arrive with from every other app on the phone, and what they arrive expecting is the app's front page, not the top of whatever they were last looking at; a Today button sitting in the corner is a second way to ask for the same thing rather than a reason the first way should decline. The offseason gate is what makes the reversal safe: the case where *going to today is wrong* is exactly the case `canJumpToToday` already refuses. |
| 2026-09-21 | **The search page is rebuilt around the thumb.** The title goes left onto the shared `PageHeader`; scope pills (All · Teams · Players · Games · Conferences) replace the section headings; every result gets its own card instead of a section card holding many rows; and the field moves from the top of the screen to the **bottom**, on a `safeAreaInset` so it rides above the keyboard. | Andy, from two FotMob screenshots. The field is the one that matters: at the top it sat at the farthest point on screen from the thumb typing into it, and with the keyboard up the field and the keys were a screen apart. The pills turn the taxonomy from a label into a control, which is what let the headings go — and once a mixed list has no headings, one card per result is what keeps a game and a team from reading as one list of one kind of thing. The pills **scroll**, and their labels carry `lineLimit(1)` + `fixedSize`, for the lesson the hero tab row taught the same morning: a fixed row of labels does not fail by truncating, it wraps, and a wrapped control row changes the height of whatever sits under it. They are filled where the day strip's chips are deliberately ink-only — a day is a position on an axis where one is always current, so weight alone reads correctly, but a scope is a filter that is either on or off, and the app already paints that state on the Follow pill. |
| 2026-09-21 | **Search opens on your recents, not on an empty state** — and what is remembered is the result you tapped, not the string you typed. Teams and conferences persist as `(id, league)` and resolve against the live directory at render time; a **player** is a snapshot; a **game** persists as `(id, day)` and resolves against the loaded slate. Ten entries, most-recent-first, deduped by id, with a dismiss control on each row. | Andy: *"the search page should show a list of all your recent searches instead of an empty state every time."* The old empty state taught the corpus in one sentence and then said the same sentence forever, on a screen people reach repeatedly for the same handful of teams. Typing "geor" and opening Georgia should hand back *Georgia* — one tap from the page — not four letters and a second search. **The three storage shapes are three different answers to staleness, not an inconsistency.** A team resolves live because the directory is already in memory, so nothing duplicates a model and no logo URL rots in storage. A player is a snapshot because there is no athlete directory to resolve against and re-asking ESPN on every render would make opening an empty search box cost a request; the price is that a traded player wears his old club until he is opened again. A game resolves live because it is the one result whose facts move — a stored score freezes the moment the game goes live, and a frozen score is worse than no row. Games were left out of the first cut on the grounds that they expire; Andy asked for them, and resolving live is the answer to the objection that excluded them. |
| 2026-09-21 | **Athletes are in search, through ESPN's own search endpoint** — one request for all four leagues, debounced 300ms, cancelling superseded queries, arriving *beside* the instant results rather than instead of them. College results are filtered against the team directory, so FBS and FCS stay and Division II does not. | The E20 backlog row had this filed as infeasible: search costs nothing per keystroke because every other corpus is already in memory, and athletes have none — the roster endpoint is per team, so four leagues means ~228 fetches for an index that goes stale weekly. `scripts/probe-athlete.sh` was written 2026-09-20 to settle it and never run, because its header said agent sessions cannot reach ESPN. **That was stale; this session's sandbox reaches ESPN fine, the probe answered 200 on all three candidate paths in all three leagues, and the feature is one request.** The probe also caught the trap the DTO rule exists for: a `player` result's `id` is a GUID no other ESPN endpoint accepts, and the real athlete id lives only inside `uid` (`s:70~l:90~a:3895074`), so the obvious field would have built a page that fetches nothing. **The division filter asks the directory rather than a hardcoded list** — `college-football` indexes D-II too, and a "McDavid" query returns Mars Hill beside Harvard. Harvard is a real FCS page here and Mars Hill is nothing; the directory already knows which is which, is already in memory, and will follow the app automatically the day it adds a division. |
| 2026-09-21 | **The player page fills itself, and its team badge is a real link.** Arriving from a roster it already had height, weight, position and jersey; arriving from search it had a name and a club, so the Profile card rendered empty. It now fetches `.../athletes/{id}` when the card would otherwise be blank, and resolves the payload's `team.id` through the directory so the hero badge carries the crest and pushes to the team page. The page also joins the entity-header pattern — `bgCard` band, card-colour nav bar — that TeamPage and ConferencePage have had since 2026-08-31. | Andy: *"the player page has no information but on the web version, we have their profile card."* The web builds its card from the roster row and says so in `web/src/lib/player-profile.ts`: *"Nothing here is fetched from an athlete endpoint, because there isn't one we've proved out."* That was true when it was written and stopped being true the same day the probe ran. Resolving the team by **id** rather than by matching the club string is the difference between exact and nearly: search gives "Baltimore Ravens" as text and no identifier, and the athlete payload gives the id the directory is keyed on. Two follow-ons fell out of the move to the card band: `PlayerIdentity.teamLogoURL` became a `var` so the crest can be backfilled, and the badge's `fill: .bgCard` override had to go — correct while the hero sat on recessed ground, and exactly the failure `HeaderLinkBadge`'s own comment warns about once the hero moved. |
| 2026-09-21 | **Search owns its navigation stack.** A result tap pushes on the Search tab's own `NavigationStack` instead of handing the Router a pending intent; Back returns to the results with the query and the scope intact. The Router path stays for the doors that genuinely originate outside the view hierarchy — widget taps, notifications, deep links. | Andy: *"tapping back should take the user back to the search list rather than back to the teams page unnecessarily."* The old path switched to the Teams tab and pushed there, so Back landed on a list of followed teams nobody asked for and the query was gone. **The interesting half is what this retired.** A player pushed from search had been failing intermittently all session, and it was chased through three wrong fixes: a missing listener (`onAppear` as well as `onChange`), then an equal-value `onChange` that never fired when the same player was opened twice — the exact failure `scoresHomeCount`'s comment describes one screen over, *"a counter, not a flag"* — then `task(id:)` to cover the union. Each made it work more often, which should have been the tell that the trigger was wrong rather than the listener. None of them was the bug. An on-screen tap was being handed to a cross-tab channel built for waking the app from outside it, and removing the handoff removed the race. |
| 2026-09-21 | **The root tabs share one masthead.** `PageHeader` carries a left-aligned title at the `Spacing.lg` gutter with an optional trailing control slot, and Games, Leagues and Teams all use it; Teams and Leagues drop `.navigationTitle(.inline)`. The row has a **52pt floor**. | Andy: *"the leagues and teams pages should have the page title left aligned and in the same place StatSide is on the Games tab. The Add button on the Teams page should be vertically centered with the Live/Date buttons."* Three root tabs had two different mastheads — one drawn, two inline-centred — and the eye caught it moving between them. The 52pt floor is the whole reason this is a component rather than three copies of a spec: on Games the row's height comes from the grouped capsule, a 44pt tap target plus 4pt of padding each side, and a tab whose trailing control is a bare button would make a shorter row and centre that button a few points higher. Stating the floor once puts every tab's controls on one line whatever they are — measured at 95.8pt and 96.0pt for the Live capsule and the Teams plus. Leagues keeps the full row height with no trailing control at all, so the content below starts at the same place on every tab and switching tabs doesn't jog the page. |
| 2026-09-21 | **CFB becomes NCAAF, in display only.** `League.shortName` and `League.displayName` both read "NCAAF"; a new `League.spokenName` keeps "College Football" for VoiceOver. `rawValue` stays `"cfb"`. | Andy, in two passes: *"we should change CFB to NCAAF everywhere in the app"*, then *"lets change College Football to NCAAF."* It is what ESPN calls the league in its own payloads — a search result's `description` reads "NCAAF" — and what the scoreboards and broadcasts use, where "CFB" was closer to shorthand this app coined for itself. **`rawValue` is deliberately untouched**: it is the league half of every `FollowKey`, every recent-search entry and the widget's stored selection, so renaming it would silently orphan followed teams on upgrade. **The spoken form is deliberately different**, and the argument was already in `SectionAccordion`: an initialism is a caption, not a word, and a VoiceOver swipe that lands on "ACC" has no marks and no screen to read against — "N-C-A-A-F" letter by letter is worse than the phrase it abbreviates. That rule moved onto `League` so every spoken surface inherits it rather than one accordion. |
| 2026-09-21 | **A team reads by its full name, with `LEAGUE • Conference` under it**, in search rows, the followed-teams list and the team page hero — which is allowed two lines rather than scaling down to keep one. The nickname leaves the subtitle and joins the name. | Andy, in four passes, and the order the two halves arrived in matters. The league tag was first **dropped** from search cards, then the conference was added under the name, then the league came **back** as the first half of that subtitle. That is not a reversal: as a trailing `CFB` chip the league was a third column competing with the name, and as the subtitle's first word it does the disambiguation work the conference alone could not — "Eastern" is the Lightning's conference and the Bucks', and on its own it was the least informative line on the screen beside a "Big Ten". League-first because a mixed list then sorts into four groups before the eye parses the specific conference, wide-to-narrow the way an address does. The hero wraps rather than shrinking because a masthead scaled to 70% to hold one line reads as a mistake where a wrap reads as a long name. The inline nav-bar title stays the short location on purpose: it is one line between a back button and three controls, where a full name can only truncate. |
| 2026-09-21 | **The Leagues hub opens closed, for this run of the app only.** Every accordion starts collapsed; expansion lives in a `LeagueSectionExpansion` store held by `RootView`, in memory and never in `UserDefaults`. | Andy: *"the leagues accordions should always be closed by default. If the user opens an accordion and switches pages but comes back, it can remain open... but if a user force closes the app and reopens it, it will be reset."* That lifetime is exactly a process's, which is why nothing is persisted: writing it to disk would make it survive the force-quit too, and quitting is how someone says start me over. The hub is a list of leagues, not of contents — the Saturday question is "which league", and four expanded tables push the fourth league off screen before it is asked. **Held by `RootView` rather than the screen**, because a tab's content is rebuilt when the tab is reselected and `@State` inside `TablesScreen` would reset on every visit, which is the "comes back" half of the rule and the part that would have failed quietly. It is a **separate store** from `UIStateStore.collapsedConferences` on purpose: that set is the Teams browse screen's, it is persisted, and it means the inverse — sharing it would have flipped a screen nobody asked about. |
| 2026-09-21 | **Find a league narrows the hub, and opens what it matched.** A field under the title filters both lists: a league survives if its own name matches, or if any table inside it does, in which case only the matching tables show; Following is filtered the same way. A matched accordion renders expanded without writing to the expansion store. | Andy: *"a find league search at the top of the leagues page... which would then filter the accordions (relevantly expanded) and following options that meet the criteria."* The field sits at the **head** of the list, unlike the Search tab's, which moved to the bottom the same day — that field *is* its screen's purpose and belongs under the thumb, where this one narrows a list you are already reading. "Relevantly expanded" is load-bearing now that accordions default closed: a filtered accordion that stayed shut would hide the thing you just asked for behind one more tap, which would make the filter useless on the same day the default changed. Not writing that expansion to the store is what lets clearing the field return the hub to whatever you actually had open. A league whose own title matches keeps all its rows: you asked for the league, not one table inside it. |
| 2026-09-21 | **The Leagues hub answers one question, and its rows sit on one rail.** Four things come off it: the leader and record from a followed conference, the table count from a league header, the "#1 Texas" teaser from the Top 25 row — and that teaser's *spoken* label with it. All three row types (league header, conference row, Top 25) take a shared 26pt mark box and `Spacing.md`; the league header moves from `sectionHeader` (13pt) to `teamNameEmphasis` (15pt semibold); the section heading below Following becomes "All leagues, conferences, divisions"; `ListSectionHeading` takes `Spacing.lg` above instead of `sm`; and Top 25 wears a trophy. | Andy, in six passes across one sitting, which read as six asks and settle into two. **The hub answers "which table"** — a standing, a record, a count and a `#1` were each answering a different question in the same row, and the chevron already says there is something inside. **And a list with no rules, no ground change and no caps has only its geometry to separate things with**, so every part of that geometry has to be deliberate: at 13pt the league header was *smaller* than the conference names it parents and read as their caption; at `Spacing.sm` a boundary between two lists sat as close as two rows within one. The alignment bug is worth keeping as a caution — boxing two of three row types to fix a rail is what created the third misalignment, which is why the three now share one constant instead of three independent numbers. The trophy **supersedes 2026-09-06**, which chose the league mark because "Top 25" says nothing about *whose*: still true in the abstract, but on a hub where every other row wears a real crest, a fifth football among four identified nothing its neighbours didn't — and the accordion it sits in is headed NCAAF. |
| 2026-09-21 | **Games search reaches past the loaded window, through the matched teams' schedules.** `TeamScheduleSearchStore` fetches `/teams/{id}/schedule` for the teams a query matched — three of them, one request each, on the athlete search's 300ms clock, cached per team for the session — and `Game.union(_:loaded:)` folds the result into the slate. The section leads with the next kickoff and is captioned "Games for matching teams". | Andy raised it three times, which is how often a limit has to bite before it is a bug: *"searching for games only shows the most recent final score and games that are coming up today and tomorrow... when I search by a team that played yesterday, I don't see the next time they're going to play."* The corpus was `allLoadedGames` — the day you are on plus a five-day window — so next weekend had never been requested. **ESPN's search endpoint cannot fill it**: the one that gave us athletes the same day returns no scoreboard events, only broadcast listings keyed to watch URLs, across sports this app does not cover (probed live). `/teams/{id}/schedule` returns a whole season in one request and `teamSchedule(teamId:year:)` was already on the protocol, used three ways — so the feature needed no endpoint, no DTO and no mapper. **Driven by the matched teams rather than the raw string**, so a request only follows a search that already succeeded. **Two rules carried rather than re-derived.** The slate's copy of a game always wins, because a schedule is fetched once and never polled and its score is frozen at fetch time — which is precisely how `ConferencePage` came to show a live game stuck at halftime (2026-09-01), and why `merging(_:withLive:)` exists. And the order is next-first, not chronological: the question is *when do they play next*, and a season sorted by date buries that under three months of finals. **The caption is the scope, said out loud** — this finds games for teams you typed, so a conference query returns none of its games and the three-team cap answers a six-team query with three. The athlete search was held to the same rule when its fallback was scoped; a silent partial list is the one thing not to ship. |
| 2026-09-21 | **One player page, whichever door opened it.** The hero's team badge reads `displayName ?? location` off the resolved `Team` rather than the door's `teamName` string; the line beside it reads `shown` rather than the identity the door pushed; and the roster door now carries the same full name the others do. | Andy: *"for some reason we have like two different type of player pages that need to be resolved into just one"* — with two screenshots of the same player, one badged "Los Angeles" over "#4 · WR", the other "Los Angeles Rams" over nothing. Both were `PlayerPage`. **The name split because each door carried its own string**: a roster row handed over `shortDisplayName ?? location`, ESPN's search index handed over its own subtitle, and the badge printed whichever arrived. **The missing line was a stale read** — `metaTail` asked `player`, the value the door pushed, while everything above it asked `shown`, the filled one, so the search door's number and position never appeared after the athlete fetch landed even though the Profile card below listed both. Neither was visible from one door: a page is only inconsistent when you have seen it twice. The fix is the same shape in both places — read the resolved value, not the door's copy — and the headshot needed nothing, because all three of ESPN's endpoints serve the identical URL (probed live). |
| 2026-09-21 | **The Leaders card's players link to their pages.** Each leader row pushes a `PlayerIdentity` built from `Leader.athleteId` and the side's `Team`; a leader with no id stays plain text. | Andy: *"the players should link to their player pages."* This **supersedes the "carried, not yet linked" note on `Leader.athleteId`** (2026-09-20), which set the exact condition it was waiting for: the id was carried but unused because a page built from a name and a stat line would have been thinner than the row that pushed it, and the link would land "with the athlete fetch." That fetch shipped earlier the same day, so the condition was met rather than overridden — the page a leader opens now is the page the roster opens. The row needs the side's `Team` and not just the id, because the league lives on the team and `PlayerIdentity.id` is namespaced by league; ESPN reuses athlete ids across them, and a destination whose identity doesn't change is reused with its state intact. A CFBD-built leader carries no id and is left unlinked rather than given a link to nowhere. |
| 2026-09-21 | **The player hero is a name and a club.** The number and the position leave the hero for the Profile card, which already listed both; `spokenSummary` drops them too, and `PlayerIdentity.metaLine` / `metaLineWithoutTeam` retire with them. | Andy: *"drop the shirt number and position from the page header, include their age in the profile card."* On a page with one card on it, the same two facts appeared twice within 100pt of each other — and, until the fix above, read differently in the two places. The card wins because its rows are labelled: "#4 · WR" asks you to know what an unlabelled run of abbreviations means, where "Position — Wide Receiver" does not. **The spoken sentence went with them** rather than staying behind: a label that announces facts the screen doesn't show makes the page longer to hear than to read, and the Profile rows already speak each one with its own name attached. Age needed no work — `profileRows` has carried it for the pro leagues since the card was built, and college football gets Class there instead, which is `RosterMetric`'s rule and not a gap. |
| 2026-09-21 | **A team in the Add teams sheet reads as one name.** `TeamFollowRow` prints `displayName ?? location` in one weight instead of setting the location against the nickname in two, and wraps to a second line rather than truncating. | Andy: *"We need to update this to not have the secondary text. Just one string of text. 'Ohio State Buckeyes', 'Dallas Cowboys'."* This row was the **last one in the app still splitting a team's name**: search rows, the followed-teams list and the team page hero all moved to `displayName ?? location` earlier the same day, and the sheet kept a shape that read as two facts about a team rather than its name. The split also cost a layout: the pair needed a stacked variant at accessibility text sizes to keep from becoming two ellipses side by side, and one string simply wraps, so `isStacked` and its `dynamicTypeSize` read leave with it. |
| 2026-09-22 | **Hide all/Show all on Scores, scoped to the sections that aren't yours.** A capsule control sits between what's yours — Following plus every hoisted conference/league/poll — and the day's remaining slate, collapsing that remaining stack to one line ("N other sections") in a single tap. `GameSection` gains `isFollowed`, set true only on Following and on a hoisted table (never merely because a section carries a `table` identity — every conference section does, followed or not, so it can be matched for hoisting). `ScoresScreen.scoresRows(for:hideOthers:)` splits on that flag and inserts the control only where both sides are non-empty; the day-swipe preview pane shares the same split so it never previews a slate the live pane wouldn't show. State persists (`ui.hideOtherSections`) like `liveOnly` and `scoreFilter`. | Andy, from two FotMob screenshots: a "Hide all"/"Show all" row sitting under your favorited leagues, above everything else, with the count of what's hidden. The app already builds this exact split for a different reason — Following and hoisted tables lead the stack so a follow's payoff shows up first (2026-09-06) — so the feature needed no new grouping, only a way to mark which sections that hoisting already produced and a control that reads the mark. The alternative read of "hide all" was the pinch gesture's own collapseAll/expandAll, which was rejected: pinch already means *every* section including Following, and reusing its semantics for a control that must never touch Following would have made one gesture mean two different things depending on which control triggered it. Not verified on-device or in the simulator — this session has no macOS/Xcode toolchain, only the Linux remote environment; the `isFollowed` split and `hideOtherSections` persistence are unit-tested, but the FotMob-alikeness of the actual control (spacing, capsule weight, caption wording) is unchecked. |
| 2026-09-24 | **The app opts into ProMotion.** `CADisableMinimumFrameDurationOnPhone = YES`, carried in a partial `Config/sports-Info.plist` merged into the generated one (`INFOPLIST_FILE` beside `GENERATE_INFOPLIST_FILE`). | Coard Miller (LinkedIn): *"feels like it's running a bit slow, maybe at 30fps."* Without the key, iOS caps every animation the app drives itself — the day push, accordions, springs, the live dot — at 60Hz on a 120Hz iPhone, while system scrolling runs at 120Hz beside them; the mismatch is what reads as sluggish. It lives in a file because Xcode has no `INFOPLIST_KEY_` for it: an unknown `INFOPLIST_KEY_*` build setting is **silently dropped** from the generated plist, which the first attempt did — verified by reading the built `Info.plist`, not the build log. |
| 2026-09-24 | **The Scores screen stops rebuilding itself per frame.** `LeagueScoreboards.sections` memoizes again, keyed on the day, the follows, the filters and a per-store `revision` that bumps on every `gamesByDay`/`divisions` write (so a hit is a counter compare and still registers the observation a later write needs); `days()` caches per season and time zone, and `DaySlot` stores its id and both labels; `DayStrip` and `SectionAccordion` are `Equatable` and ignore their closure props; each game row is its own `SectionGameRow`; the conference sort looks tier and name up once per conference. Reinstates the 2026-09-01 memo that the 2026-09-05 day-axis row dropped. | `dragOffset` is `ScoresScreen` state, so every frame of a day drag re-ran the whole body — a season of eager day chips (~365 calendar decompositions and ~730 date formats), the section pipeline twice (the preview pane asks too), and every visible accordion, because a fresh `onToggle`/`onSelect` closure never compares equal. The 2026-09-05 row dropped the memo because the pipeline had shrunk to "a filter and two buckets"; it has since grown conference bucketing, registry lookups, hoisting and sorts back. `SectionsMemoTests` changes one input at a time — a refetch, a game going live, a follow, the Live filter — and expects a different answer, which is the only property that makes a memo safe. |
| 2026-09-24 | **Opening and closing sections is a 0.18s ease-out, not the default spring** — a tap, Hide all/Show all, and the pinch alike (`ScoresScreen.accordionAnimation`). | The same diagnosis the Live toggle got on 2026-09-21: a bare `withAnimation` is a spring whose settle is a drifting tail, and on a 40-row conference that tail reads as lag rather than motion. A touch longer than the filter's 0.12 because a section's height actually travels. The Hide all control (2026-09-22) had copied the bare `withAnimation` from the accordion; both move together. |
| 2026-09-24 | **The render path stops redoing fixed work.** Font tokens cache per content-size category (still computed, so Dynamic Type still lands); `LogoCache` decodes with `preparingForDisplay()` on its own actor rather than letting `UIImage(data:)` inflate a 500px PNG on the main thread the first time a row draws it; `LogoImage` resolves its dark variant once per body; the athlete search and profile fetches are `@concurrent`, so their decode leaves the main thread. Logos keep full resolution. | Each is small and each ran per row per body pass. The logo decode is the one that shows: it landed mid-scroll, or inside the frame a section expanded. Full resolution stays because the share card draws the same cached marks at 3× on a 600pt canvas — a downscale sized for a 28pt row would blur the one image people send to each other. Under approachable concurrency a plain `nonisolated async` func runs on its caller's actor, so the athlete clients had been decoding on main from `SearchScreen` on every debounced keystroke. |
| 2026-09-24 | **The live dot pulses in Core Animation, on one shared clock.** `LiveDot` hands the pulse to a `UIViewRepresentable` whose layer carries a `CABasicAnimation` phased by `CACurrentMediaTime()`; Reduce Motion and `liveDotPulses == false` still draw the plain SwiftUI circle. The app's third UIKit exception. | SwiftUI steps its own `repeatForever` on the main thread every frame, one per dot, and a Saturday puts 20–60 live rows on screen (a followed game counts twice). With ProMotion enabled above, that would have been the main thread ticking at 120Hz for as long as anything was live — a battery cost as much as a smoothness one. A layer animation runs in the render server. Phasing from media time rather than from `onAppear` also puts every dot on one beat, where before each started its own whenever it scrolled in or a section opened. Static renders keep the SwiftUI circle because `ImageRenderer` can't see a UIKit layer. |
| 2026-09-24 | **Player stats come from `site.web.api.espn.com`, and the player page gets its four tabs.** `PlayerStatsClient` reads `common/v3/.../athletes/{id}/stats` (every season, with the club, plus ESPN's career `totals`) and `/gamelog?season=`; the page shows Profile · Games · Stats · Career whenever the player has a stats line, and Profile alone when ESPN has nothing — the page's own 2026-09-20 rule, now satisfied. Profile gains a **This season** card. The client is called directly, like `AthleteProfileClient`, rather than through a `ScoresProviding` seam. | Coard Miller asked for individual stats, and Andy had read ESPN as blocking them. It wasn't a block on the data, it was a block on one host: `site.api.espn.com`'s athlete and stats paths sit behind an Akamai rule that 403s by User-Agent, and nothing had ever been recorded because no session could reach ESPN to see it. Every agent session since 2026-09-13 had deferred E20's probe for that reason. The same data answers 200 from `site.web.api` in all four leagues. The seam goes unbuilt because the page never used one — search is ESPN-only, and CFBD could only ever answer `false`. |
| 2026-09-24 | **Career is ESPN's rows and ESPN's totals; the app sums nothing.** The Current season card picks its numbers by ESPN's **category** name (`passing` → yards/TD/INT, `averages` → PTS/REB/AST, `goaltender` → W/GAA/SV%, skaters → G/A/PTS), matched on the payload's stable `names[]` and never on labels; an unknown category shows its first three columns. A traded player's two lines for a year are not added together. | E20 had planned to derive a career by aggregating per-season game logs, and set out the costs that would come with it — our totals disagreeing with ESPN's, one request per season, and silent holes. None of it is needed: `/stats` ships a row per season and its own `totals`. The registry is keyed by category rather than by league because a receiver is the same question in the NFL and in college, and ESPN already orders each player's categories by what they do. |
| 2026-09-24 | **Box score names link to player pages** — only where ESPN sent a real athlete id (`BoxScore.Player.athleteId`), never through the synthesized `teamId-name` fallback that keeps a row identifiable in a `ForEach`. Ink carries the link, not a chevron. | The last of E20's three doors, and the one Josh Vertucci's 2am box-score habit actually uses. It waited for a page with something on it past a name and a number, and the Games and Stats tabs are that. A fallback id pushed as a navigation target would open a page for nobody. |
| 2026-09-24 | **Team stats land in both places: a four-number Season stats card and a Leaders card on Overview, and a full Stats tab.** The headline numbers are a per-league registry over ESPN's stat `name`s (football: points and yards per game, 3rd-down %, turnover differential; NBA: PPG/RPG/APG/FG%; NHL: goals, GAA, SV%, faceoff %). The Stats tab prints every category in ESPN's own names, with a Team / Opponents switch in football, whose payload carries the opponents' block with ranks. It sits after Games, and the season chip stands down on it, as it does on Roster. | Coard Miller asked for team stats; Andy chose "both" when offered the Overview cards, a tab, or both. The tab row has scrolled since 2026-09-21, so a sixth tab no longer means cramming, and that retires the E5 worry about a five-tab team page not fitting. The chip stands down because the statistics endpoint answers for ESPN's current season only, and the card already names that season. |
| 2026-09-24 | **ESPN's team-stats season label is not trusted across a rollover.** In September the NBA and NHL answer with a season ESPN calls "2026-27 Preseason" whose numbers are all of 2025-26: 82 games played. A preseason answer with a full season's games (20 or more) is labelled as the season it actually is; one with fewer is a real preseason and isn't shown. Leaders fall back a season the same way when the current one has none, and the card names the season. | Printing "2026-27" over last season's 82 games would be a false statement the app made, with ESPN's label as its cover story. The 20-game floor tells a full season from a few exhibitions without trusting the label. The leaders' "now" is the app-wide `SeasonSpan` clock, the one the player page's This season card uses, so the two surfaces agree about what "this season" means. |
| 2026-09-24 | **The Scores slate scrolls under the tab bar again: its clip cuts the top edge only.** The day pager's `.clipped()` (2026-09-21) cut all four edges at the safe area, so the slate stopped on a hard line above the floating tab bar, with nothing under the glass. `TopEdgeClip` keeps the reason the clip exists (a transition can't paint over the masthead or the day strip) and leaves the bottom open. The fade under the bar is the system's scroll-edge effect, not ours. | Coard Miller, with a screenshot: "the tab bar is creating a cutoff for the content below." Andy first read it as a Liquid Glass settings difference, because FotMob also cuts on his phone, which has Reduce Transparency on and Tinted set. But Coard's FotMob scrolled under the glass on the same phone where ours cut, so the bug was ours. Handing the edge to the system is what makes it follow each user's Clear, Tinted and Reduce Transparency settings, where a hand-drawn gradient would get one of them wrong. |
| 2026-09-24 | **The game page shows the pre-game line: ESPN's headline line and the total, and nothing else.** One row in Game info, "LIB -2.5 · O/U 50.5", read from `pickcenter[0]` in the summary the page already fetches, so it costs no request. It shows before kickoff only: ESPN keeps shipping the line on a final, but Game info stays after kickoff and the line shouldn't. `details` is printed as ESPN leads with it, which in hockey is the moneyline ("ANA -185"), the way hockey lines are quoted. No provider name or logo, no moneyline column, no per-team odds, no bet links. | Coard Miller: seeing "what the O/U and spread is" is how he decides what to follow ahead of time. That's a scores question, not a betting one. The 2026-09 icebox row stands for everything else in `pickcenter`, because the rest (sportsbook branding, moneylines, bet slips) is where a scores app starts reading as a sportsbook's. The Scores row was considered and passed over: it would cost density on every pre-game row for a number most people check on one game at a time. |
| 2026-09-24 | **Betting lines sit behind a Settings switch, off by default, and when it's on they show on pre-game Scores rows as well as the game page. That switch is why the app has a Settings screen.** The Games header gets a gear, as its own glass circle beside the Live/calendar capsule. (It first went beside the Teams plus and moved the same day at Andy's call.) Settings holds Betting lines, Kickoff reminders (the same state as the TeamPage bell) and the version. The row line comes from the scoreboard's own `odds` at no extra request, printed as a third line in the status column that narrows from `LIB -2.5 · O/U 50.5` to `LIB -2.5 · 50.5` to the spread alone. `GameLine` now also records who was favored, which nothing prints: it's for the Tight filter. | Coard Miller, second message: lines on "both" pages, "but I think it's just a toggle… might be frowned upon." Off by default keeps the app a scores app for everyone who didn't ask. The switch needed a home, and the app had none. The Scores header is the front door and no place for a gambling switch, and a Settings.bundle would be a switch nobody finds. Andy put it on the Games page, the screen people are on nearly all the time. |
| 2026-09-24 | **A Tight filter beside Live: live games that are late and within one score, or where the underdog leads.** One score is 8 points in football (4th quarter or later), 6 in basketball (4th or later) and a goal in hockey (3rd or later). The underdog rule starts in the second half, counts halftime, and uses hockey's 2nd period in place of a half. It needs the pre-game favorite, so `ScoreboardStore` carries a game's line forward when a later payload drops it. That's in memory only: a relaunch mid-game loses the line, and then only the late-and-close rule applies. Tight and Live are exclusive, both narrow today only, and either one renames today "Ongoing". Tight's active state is ink, not the live green. The thresholds live in one per-`League` table. | Coard Miller: "a filter for close games that are live… if it's an upset or a closer game than the spread suggests it might be worth watching." It uses the line without printing it, so it works with Betting lines off. It's a scores question that happens to know the favorite. **Named Tight, not Close** (Andy): in a header, "Close" reads as a dismiss button. The runners-up were "Crunch time" (long, and football-flavoured) and "Nail-biters" (doesn't fit a chip). |
| 2026-09-24 | **Live Activity channels are made on demand, on the first pin of a game, and kept in Upstash Redis.** `/api/live-activity/channel` became find-or-create. It checks the game with ESPN (an unknown id gets a 404), gives a channel only to a game that is live or kicks off within 12 hours, creates the channel at Apple, and records `{channelId, kickoff, endedAt}` with `SET NX`. A pin that loses a race deletes its own channel. The broadcast tick reads the store, stamps `endedAt` on a game's first `end`, and reaps a channel an hour after that, or a day after kickoff for a game that never ended. It removes a record only after Apple confirms the delete. With no store configured, both routes read the old `APNS_CHANNELS` map. The Redis client is `fetch` against Upstash's REST API, so it adds no package. iOS now defaults to the production service URL. `isAvailable` stays off. | E12 blocker 3. The hand-made map could cover one test game, and a Saturday is about 60. Making channels **on pin** rather than for the whole slate keeps what the service holds proportional to what people actually watch, and still never to how many people watch. Redis is new infrastructure, agreed in the Coard-feedback plan: the smallest durable store Vercel offers, holding game ids and nothing about people. While building it, Apple's docs showed the sandbox management host is on **:2195**. The service doc had said :2196 for both environments, so the first sandbox probe would have failed against the wrong port. |
| 2026-09-24 | **The kickoff reminder offers "Pin to Lock Screen", and it opens the app instead of pinning in the background.** A `kickoff` category on every reminder. The action is registered only where the pin could work (`isAvailable` and `areActivitiesEnabled`), and that is re-checked on every scene-active, because a reminder's actions are whatever is registered when it fires. The game page does the pin once it has loaded, via `Router.pendingPin`, which is spent whether or not the card starts. | E12's second door, and a nudge from Coard Miller's "I'd want live activities." The 30-minute reminder is when a fan decides whether they care. A background start would feel smoother, but it runs with no summary and on a budget the system can cut short, and a pin that silently doesn't happen is worse than one that shows you the game while it happens. No rating ask on this path: a StoreKit sheet over the card the user just asked for would spend the moment. |
| 2026-09-24 | **The game page gets a Win probability card: ESPN's matchup predictor before kickoff, and the home side's chance through every play after it.** Before kickoff it's a split bar in two grays ("LIB 56% · CCU 44%, ESPN matchup predictor"). Once under way it's a single-ink line over a dashed 50% hairline, with home's crest at the top and away's at the bottom, headed by whoever leads and their percentage. The line takes over from the predictor once it has two points. It sits after the line score, which puts it after Game info and Venue before kickoff. It redraws on the page's existing poll, with no new request. It's absent where the summary has neither block, which is the NHL. | Coard Miller: "a chart like espn does with their win probability… Could you see a line of 2.5 and suggest that it could potentially be a close game?" That's the icebox's "one worth arguing about", now argued. The predictor answers "is this going to be close?" without being a betting number, and the line is the rare ESPN extra that spends no color: one ink, no team colors, no gradient. Hockey gets no card rather than one invented for it (the payload test). |
| 2026-09-24 | **The Win probability card is now one row, in the shape of FotMob's "Who will win?": each side's crest and percentage, with the favorite's number in ink and weight.** It supersedes the same day's split bar and per-play line. The header's trailing caption, in the slot where FotMob shows "Total votes", names which number is shown: "ESPN predictor" before kickoff, "Live" during the game (the latest play's value), and "At kickoff" once final (the first play's value). Away is on the left and home on the right, matching the game header. No draw column, no vote, no chart, no color. | Andy, with FotMob's card as the reference: "the percentage breakdown and the winning percentage stands out in a meaningful way." It's read-only: nobody has to answer anything to see the number. A final shows the kickoff value because the live value at the whistle is always 100–0, which says nothing, while the kickoff value is what an upset was measured against. Emphasis is weight, not FotMob's green, because of the color budget. |
| 2026-09-24 | **Leagues list alphabetically: NBA, NCAAF, NFL, NHL.** This applies to the Scores accordions, the Leagues tab and a new follow's default position. `League.displayOrder` is the one source, and `allCases` keeps its declaration order for the fetch fan-out and season spans that depend on it. College football's conferences keep their tiers (Power 4 → Group of 5 → Independents → FCS, A–Z within each), and the pro divisions stay grouped by conference (Andy's call on both). | Andy: "alphabetize the leagues." College football led because it came first (CLAUDE.md, "still leads the fall"). With four leagues on one page and the season running July to June, A–Z is the order a reader recognises without being told. It gives up football-first in the fall, and a followed conference is still hoisted above everything on Scores, so a college fan's own sections still lead. |
| 2026-09-24 | **A followed card lifts only on a deliberate, still hold: half a second anywhere on the card, or 0.2s on the grip, and the finger may drift no more than 5pt during it.** Before, the lift came after 0.25s with 12pt of drift allowed, and the grip lifted with **no hold at all** after 3pt of movement. The web's card-body hold gets the same change, from 350ms and 8px to 500ms and 5px. Its grip stays instant, because on the web it's a small `touch-none` handle that a scroll can't start on. Same interaction, same haptic, same spring. | Andy: scrolling the Leagues tab over the Following cards "thinks you're trying to move one," and the interaction "is right, it's just triggered too quickly." The grip was the main offender. Its hit area is each card's whole trailing strip, which is where a right thumb scrolls, and 3pt is the first frame of any vertical flick. A slow start to a scroll also lasted 0.25s inside the 12pt allowance. The fix is a hold that stays still: a scrolling finger moves from the first frame, and a finger that means to reorder settles first, which is how iOS's own lift tells the two apart. FotMob's Edit/Done mode was considered and not taken: it adds a mode to answer a timing problem. **Also fixed, found while testing this:** a lift the system cancelled, rather than one the finger ended, never called `drop()`. The card stayed raised and the hub couldn't scroll. `@GestureState` resets on cancellation too, so its falling edge now always drops the card. |
| 2026-09-25 | **Widget game rows read name-then-rank, keep the record only until there's a score, and draw the divider on every row.** In a team line the abbreviation now sits beside the logo, with the rank after it. The abbreviation is 14 medium in every state (live no longer bolds it). Pre-game, the record is right-aligned in the slot the score will take. Live and final rows drop the record. The hairline before the status column is drawn on pre-game rows too, and the final status reads "Final" / "Final OT" instead of "FINAL" / "FINAL OT". Applies to both the medium and the large widget, which share `WidgetGameRow`. Loser muting is unchanged. | Andy remixed the large widget in Paper and asked for the code to match. The 2026-09-06 "record in every state" rule is reversed: the record was season context competing with the score for the same glance, and the app's `GameRow` already hands the slot from record to score. With records and scores sharing a trailing column, every row has a number column to divide from, so the pre-game exemption for the divider goes too. Weight on live now belongs to the score and the dot alone. "Final" matches the app's rows and the Live Activity, the only surfaces that still shouted. |
| 2026-09-25 | **The Hide all/Show all capsule hugs its label and chevron, centered, instead of spanning the slate.** Full width, it read as one more section header sitting between the cards; as a pill it reads as what it is, a control over the stack below. Sixteen points of side padding, the same height as before, and the "N other sections" caption stays centered beneath it. | Andy, 2026-09-25: "I want the hide all button to hug its contents not fill the width." |
| 2026-09-25 | **Settings gains an Appearance picker: System, Light, Dark, System by default.** A segmented control in its own section, stored as `ui.appearance`, and applied to every window's `overrideUserInterfaceStyle` by `Appearance.apply`, called from `RootView` on launch and on each change. **That's the third UIKit exception.** Widgets and Live Activities are left following the system. The two screenshot suites pin `-ui.appearance system`. | Andy asked for it. Segmented because it's three short words in one row, and it's monochrome as it comes, so the colour budget isn't touched. System is the default so nobody's app changes until they ask. **UIKit because the SwiftUI route failed on screen:** `.preferredColorScheme` on the root flipped everything going into Dark or Light, but going from Dark back to `nil` turned the screen behind the open Settings sheet light and left the sheet dark (iPhone 16 Pro, iOS 18.3, caught by `SettingsAppearanceUITests`' screenshots). A window's override reaches everything it presents, and `.unspecified` hands the choice back to iOS every time. The theme needed no work: every token is a `UIColor { traits in … }` on `userInterfaceStyle` and team logos pick `500-dark` from `\.colorScheme`. The widget and the Live Activity render in other processes, and they sit on a Home Screen and Lock Screen that follow the system, so a pinned app appearance there would clash with what surrounds them. The screenshot pin exists because the choice persists, and a Light left over from a previous run would override `simctl ui appearance dark` without anything visibly going wrong. |
| 2026-09-25 | **The day strip gets 12pt above and below its chips, up from 8pt** (`Spacing.sm` → `Spacing.md`), at Andy's call. The chips themselves and the 16pt horizontal inset are unchanged; the strip is 8pt taller overall. |
| 2026-09-25 | **The Scores header capsule insets its pills 8pt at the ends and 4pt top and bottom**, up from an even 4. | Andy: the left and right "doesn't match the top and bottom." The pills are shorter than their 44pt frames, so the top and bottom already read as about 6pt, and the capsule's round ends curve in on the Live pill and the calendar. An even inset looked tight at the sides. The extra 8pt of width comes out of the spacer beside the wordmark. |
| 2026-09-25 | **The scoreboard polls through kickoff, not only once a game is live.** Before, the 30s loop started only when a cached game was already `in`, so a slate opened before kickoff never polled and every listing page (Scores, team, conference) sat at kickoff times until someone pulled to refresh (Andy's report, same day). `ScoreboardStore.nextPollDelay` now decides the next fetch. A live game anywhere in the cache means 30s. A pre-game game on the window's days from one tick before its kickoff to **3h after** it also means 30s: ESPN lags the flip to `in`, and a weather delay can hold pre-game for an hour or more. The cap is for a postponement ESPN never marks, which would otherwise poll all night. A later kickoff is a sleep until then, which makes no request. A `timeTBD` placeholder schedules nothing. A window change reschedules the loop, so a sleep until an evening kickoff can't sit through a day that's live now. There's still no background polling: scenePhase cancels the loop exactly as before. The same change routes the Top 25 page and the conference Postseason tab through `Game.merging`, which they had never done. |
