import SwiftUI

struct TeamsScreen: View {
    var body: some View {
        NavigationStack {
            Text("Teams")
                .navigationTitle("Teams")
        }
    }
}

#Preview {
    TeamsScreen()
}
