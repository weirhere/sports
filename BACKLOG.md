# BACKLOG.md — sports

Priorities: **P0** = must exist before the season starts (~5 weeks). **P1** = in-season. **P2** = when it earns its spot. Nothing gets built that isn't here; new ideas get added here first and discussed.

## E0 — Foundation

- [x] **P0** Set `IPHONEOS_DEPLOYMENT_TARGET` to 18.0 in all build configs (currently 26.2). App runs on an iOS 18 simulator. *(Verified 2026-07-20: builds + runs on iPhone 16 Pro / iOS 18.3.1.)*
- [x] **P0** Create folder structure per ARCHITECTURE.md; delete template `ContentView.swift` in favor of `RootView`.
- [x] **P0** Theme tokens: mono ramp + `liveAccent`, light/dark, type styles, spacing. Acceptance: a theme preview screen shows every token in both modes; no view ever references a raw color.
- [x] **P0** ESPN DTOs + domain models + `ESPNClient` behind a `ScoresProviding` protocol. Acceptance: decodes a real July (empty) and a fixtured in-season scoreboard JSON without error; every field optional; unit test with a truncated/malformed fixture drops the bad event, keeps the rest. *(15/15 unit tests pass.)*
- [x] **P0** Week model parsed from ESPN calendar. Acceptance: strip data includes Wk 0–14 + CCG + Bowls + CFP with correct date ranges; nothing hardcoded.

## E1 — Scores screen

- [x] **P0** Week strip. Horizontal, current week auto-selected, past weeks left, future right. Acceptance: Sunday shows the just-completed week; Monday flips forward; postseason slots use names not numbers. *(Rollover rules unit-tested; rendered live 2026-07-20.)*
- [x] **P0** Game row, 3 variants. *(Pre variant verified on-device with real Wk 1 data; live/final verified in previews + decode fixtures — on-device check needs real games, see E5 note below.)*
  - Pre: logo, team, record, kick time, network. Network is prominent (CFB-specific must-have).
  - Live: score (monospaced digits, heavy weight), quarter + clock, possession indicator, red pulsing `LiveDot` — the only color accent on screen.
  - Final: score with winner in heavier weight, "FINAL"/"FINAL OT".
  - Acceptance: all 3 variants legible in light and dark; row height compact (FotMob density, not ESPN cards).
- [x] **P0** Section stack: Following → Top 25 → conferences, accordion collapse/expand. Acceptance: a game in multiple sections appears in all of them; Following section hidden when the user follows nobody; unknown conferenceId lands in "Other," never crashes. *(Unit-tested; cross-conference duplication confirmed on-screen — Fresno St @ USC renders in Big Ten and Mountain West.)*
- [x] **P0** Accordion memory. Following + Top 25 expanded by default; conference state persists across launches.
- [x] **P0** Conference chips = jump anchors. Tap scrolls to that section (expanding it if collapsed). Acceptance: chips never hide content.
- [x] **P0** Live toggle. One chip; on = each section shows only in-progress games, empty sections hide. Acceptance: toggling never loses scroll position wildly; off restores exactly the prior view. *(Filter logic unit-tested; chip only appears when games are live — on-device check needs real games.)*
- [x] **P0** Refresh: pull-to-refresh always; 30s auto-poll only while foregrounded AND ≥1 game is live. Acceptance: in-place updates preserve accordion + scroll state; no polling in background (verify with logs). *(Implemented with os.Logger instrumentation; log verification needs live games.)*
- [x] **P1** Day dividers inside sections spanning multiple days (Thu/Fri/Sat, whisper-quiet). Field note 2026-07-20: ESPN's 2026 "Week 1" spans two weekends (Aug 22–Sep 8), so weekday alone is ambiguous — dividers need dates ("Sat Aug 29"), and pre-row kick times may too. *(Built with dates; verified on-screen.)*
- [x] **P1** Conference section ordering personalizes: followed team's conference first, then P4 → G5 → Independents. *(Unit-tested; verified on-screen — following Georgia floats SEC above the alphabetical P4.)*
- [x] **P1** Conference logos in section headers. URLs hardcoded per conference id alongside the names, verified against ESPN's /scoreboard/conferences endpoint 2026-07-20. Following/Top 25/Other headers stay text-only. *(Originally grayscale for the monochrome chrome budget; switched to full color 2026-07-21 — the budget's logo exception now covers conferences too.)*
- [x] **P1** Screen states: skeleton loading, error-with-retry (keep last good data), offseason/empty week state with next-kickoff countdown. *(Skeleton rows + quiet refresh-error banner + countdown from the week calendar.)*
- [x] **P1** Header row above the week strip: placeholder wordmark left (name still open — see open question #1), season selector right. Past seasons via scoreboard `dates={year}` (verified live 2026-07-21), CFP-era floor (2014); a past season lands on its final slot (CFP), current season reapplies the rollover rule.
- [x] **P1** Logo loading resilience: replace bare `AsyncImage` with `LogoImage` + `LogoCache` (in-memory, dedupes in-flight fetches, retries on reappearance). Field note 2026-07-21: AsyncImage fetches once and never retries, so a network blip at cold launch permanently blanked the first-rendered section's logos (Following) while lazily-rendered rows loaded fine. Bonus: a team duplicated across sections now renders its logo instantly from cache.
- [x] **P1** Grouping toggle on Scores: a "By date" chip beside the Live chip switches sections to Following (pinned) → one accordion per calendar day, chronological, expanded by default. Live filter composes with either mode; mode persists (`ui.scoresGrouping`). Acceptance: day sections cover the full slate; nil-kickoff games land in a trailing "TBD" section; conference mode unchanged; chip row reachable with zero live games. Note: week-scoped day *grouping*, not day navigation — the no-date-picker rule stands. *(Chip lives in the header beside the season picker, not the Live chip row — that row hides with zero live games, which would have hidden the toggle. Day ids from local calendar components ("day-2026-08-29"), inverse-persistence so days start expanded; TBD shares the "day-" prefix. 8 unit tests cover pinning/no-dedupe, TBD, chronology, Live composition, persistence; smoke test round-trips the toggle on live data 2026-07-21.)*
- [x] **P2** Pinch to collapse/expand all sections (FotMob parity). Pinch in collapses every rendered accordion, pinch out expands them all; fires once per gesture at the threshold crossing; date-grouping day sections included; Scores only. Acceptance: scroll, pull-to-refresh, and header taps unaffected; state persists exactly like individual header taps; no new haptic (budget holds). *(MagnifyGesture via simultaneousGesture on the section ScrollView; bulk store ops unit-tested incl. the mixed expanded/collapsed-semantics case.)*
- [x] **P2** Swipe between weeks on Scores. Horizontal swipe on the game list pages to the adjacent week — swipe left for next, right for previous — with a directional slide; week-strip chip taps share the same direction rule so both entrances feel like one mechanism. Acceptance: vertical scrolling, pull-to-refresh, and the pinch gesture unaffected; the empty-week and error states are swipeable (that's where leaving the week matters most); a swipe past either end of the season is a quiet no-op; chips keep every week tappable (gesture-only accelerator, nothing swipe-gated for VoiceOver or switch users); no new haptic (budget holds). *(DragGesture via simultaneousGesture on the content stack, |dx| > 50 with a 1.5× horizontal-dominance check so diagonal scroll flicks never page; `ScoreboardStore.adjacentWeek(offset:)` unit-tested incl. both season ends and the pre-load state; push transition keyed on week id, suppressed on initial load and season switches where direction is meaningless.)*

- [x] **P1** The empty Following slot becomes the follow prompt (P2 of the follow-moment plan; census finding: sports apps teach in the empty state, never the feed — DAZN's empty Following tab, FotMob's dismissible Suggested): when the user follows nobody, Scores renders a card in the Following position — star glyph, "Follow your teams" headline + payoff line, a "Pick your teams" button landing on the Teams tab (new `Router.pendingTeamsBrowse` intent), and a quiet "Don't show again" (persisted `ui.followPromptDismissed`, stored-property pattern so the card leaves the moment it's tapped). Monochrome, no carousel, no suggestions engine — the catalog is ~134 teams and the tiered browse makes it scannable. Acceptance: card shows only when following nobody and not dismissed; the offseason empty-week state and onboarding sheet are unchanged; VoiceOver reads headline, payoff, and both actions distinctly. *(Shipped 2026-08-25: `FollowPromptCard` + `Router.pendingTeamsBrowse` + `ui.followPromptDismissed`. Verified on-simulator: card leads the Week 1 slate above Top 25 when following nobody, CTA lands on Teams browse.)*

- [x] **P1** GameRow adopts the 2240 Figma spec (Andy, 2026-08-25): matchup-card shape — 28pt logos in 32pt slots, names at a new 13-medium row scale with the **rank after the name** (Regular 10), records **upcoming-only** at the trailing edge beside a 55pt hairline, then the fixed 80pt status column (kick day/time/network for pre, LiveDot + clock for live, sentence-case "Final" + date for finals — no records on finals, just the score at the team rows' trailing edge). New `rowName`/`rowNameEmphasis`/`rowMeta`/`rowMetaMedium` tokens; live still spends weight (semibold at row scale); day-divider/timeOnly date rules and the VoiceOver sentence unchanged. *(Shipped 2026-08-25, verified on-simulator against the live Week 1 slate.)*
- [x] **P1** Interactive week swipe (Andy, 2026-08-25 — "the swipe should start happening as soon as the thumb moves"): the content tracks the finger from first horizontal movement (axis-locked so scroll flicks never jiggle), the adjacent week rides in as a skeleton pane, commit on 35%-width distance or a same-direction flick, season ends rubber-band at quarter travel, and the handoff swaps ids with no double animation (the offset resets when the week actually lands, so the skeleton covers the swap frame). Chip taps keep the push transition. *(Shipped 2026-08-25 — feel needs Andy's thumb for the final verdict.)* *(2026-08-31: the skeleton pane superseded, Andy's FotMob comparison — the adjacent week now rides in as its real slate: `ScoreboardStore` caches each fetched week per season and prefetches the swipe's ±1 neighbors once after every settle (never polled), the pane renders the cached week through `sections(from:)` with grouping/filters/expansion shared and interaction off, skeleton only before the first warm. Selecting a week seeds from the cache before the fresh fetch, so commits and chip taps stopped blanking too.)*
- [x] **P1** Slate filter on Scores (Josh Vertucci feedback via Andy, 2026-08-29 — ESPN's "FBS tab" habit: "just a way to filter your full list by literally any conference"): a funnel chip in the header opens an ESPN-style sheet — All games / Top 25 / every FBS conference (browse order, conference marks) — and the selection filters every section in both groupings, composing with the Live chip (SEC + Live = live SEC games). The by-date default made this the gap: date mode has no conference structure at all. Filter claims follow the section rules (Top 25 = any ranked participant; a conference = either side, so an FCS visitor stays under its FBS host). **Session-only, resets to All** — the landing page's full-slate promise survives; the active chip fills and names the selection, so a hidden slate is never a mystery state (the July objection to filtering chips). Empty filtered week shows "No SEC games this week" + a Show-all-games escape. Acceptance: all-games behavior byte-identical with the filter off; Following filters too (complete within the active filters, the Live precedent); widget, notifications, and search untouched. *(Shipped 2026-08-29: `ScoreFilter` on `ScoreboardStore`, `ScoreFilterChip` + `ScoreFilterSheet`, `Conference.orderedIds`; filter/compose/Following unit tests. Same-day follow-up on Andy's "the top is getting a little crowded": the sheet absorbed the grouping toggle (segmented) and season picker, the header slimmed to wordmark + funnel + Live, `GroupingChip` retired, and the funnel chip's label now carries filter + past season — UI-test helpers `setScoresGrouping`/`selectSeason` drive the sheet.)*
- [x] **P1** Conference accordion headers split into two surfaces (Andy, 2026-08-25, superseding the whole-width-toggle promise): mark + name push the conference page, everything after them toggles; Scores' trailing standings icon retired (the name button inherits its "X standings" accessibility label, keeping the UI-test hook), context menus and VoiceOver actions stay; non-conference headers keep the whole row as toggle. Applies to Scores sections and Teams browse groups. *(Shipped 2026-08-25.)*

- [x] **P1** Day dividers come out; the date moves into every row (Andy, 2026-08-25: "it's currently just noise"): the in-section "SAT, AUG 29" labels are gone, and the row's day line now carries weekday + date from two days out ("Sat, 8/29" — Today/Tomorrow keep their 48-hour window), since it's the only date on screen and a bare "Sat" is ambiguous in a two-weekend week. **Date grouping is the exception**: rows stay time-only under their day accordion header, which now **pins to the top** while that day's games scroll (`LazyVStack(pinnedViews:)`; the accordion emits a `Section` in date mode — header floats as its own card, rows become theirs). Ladder unit tests updated to the new rungs. *(Shipped 2026-08-25.)*

## E2 — Game detail

- [x] **P1** Header: teams, records, score, status; linescore grid by quarter (incl. OT columns). *(Pre-game header verified on-device; linescore decode-verified against the 2025 CFP championship fixture — visual check needs a completed game, see E5.)*
- [x] **P1** Scoring plays list, chronological with period markers. *(Decode-verified: 8 plays from the championship fixture.)*
- [x] **P1** Team stats comparison (total yards, pass/rush, 3rd down, TOs, possession) as opposing mono bars. *(Bar magnitudes parse "5-14" and "31:14" shapes; unit-tested.)*
- [x] **P1** Leaders (pass/rush/receive per team). *(From summary's top-level leaders; decode-verified.)*
- [x] **P2** Live auto-refresh on this screen while game is in progress. *(30s loop, same rules as the scoreboard: foregrounded + live only; stops itself when the summary comes back final. os.Logger instrumented; live verification folds into the E5 first-game pass.)*
- [x] **P2** Drive log (data already in `drives.previous[]`). *(DRIVES section after Leaders: one line per possession — logo, result with weight on scoring drives, ESPN's "5 plays, 20 yards, 2:39" line — grouped by quarter markers like SCORING. Decode-verified: 22 drives / 8 scoring from the championship fixture; VO speaks each drive as one sentence.)*

## E3 — Rankings

- [x] **P1** AP Top 25 default; segmented picker for Coaches and CFP (CFP appears only when ESPN returns it). *(Chip picker; FCS/DII/DIII polls filtered out; choice persists via ui.pollChoice.)*
- [x] **P1** Rank row: rank, logo, team, record, movement arrow (▲/▼/–, derived from `previous` − `current`), first-place votes where present. Monochrome arrows; movement shown by weight not color. *(Verified on-device incl. NEW tag for unranked-previous.)*
- [x] **P2** Tap rank row → team page. *(NavigationLink straight to TeamPage — RankedTeam already carries a full Team.)*
- [x] **P1** Rankings tab becomes the tables hub, FotMob-Leagues-shaped: the root is a list of rows — a Top 25 row first (trophy mark, "#1 Ohio State" teaser, pushes `PollScreen` where the poll + picker now live), then a CONFERENCES section — one row per known conference (logo, name, leader teaser like "Ole Miss · 7-1") pushing its ConferencePage, with a trailing follow star (same `FollowingStore.toggleConference` bit as the page pill). Followed conferences pin above the rest; then tier → name (the standings fetch's order). Teasers hidden preseason (a 0-0 "leader" is last season's carry-over, not information). Acceptance: poll content and picker unchanged one tap down; conference list renders offseason (rows without teasers); a follow star pins the row on next appearance and the Scores Following section reflects it; the CONFERENCES card hides if the standings fetch fails (never an error over a healthy Top 25 row); rows and stars accessible (rows speak name + teaser, stars speak "Follow SEC" not a generic label). Decided 2026-08-10 after rejecting a 4th tab — a Conferences tab would be a menu, not a destination. *(First cut kept the poll inline at the root with conferences below it; superseded same day — 25 rank rows buried the conference list past discovery, so the poll moved behind a Top 25 row, FotMob-style.)* *(Shipped: both cuts landed on main 2026-08-10 — `88a8956` then `fc8ed94`.)*
- [x] **P1** Rankings hub restructures FotMob-Leagues-style (P1 review of the Figma "P1 Variation" frame, Andy's edits 2026-08-25): a **Following** section leads — the Top 25 row (trophy + teaser, unchanged) pinned first, then one row per followed conference — then an **All conferences** section listing every conference in the existing tier → name order. Followed conferences appear in both sections (sections stay complete, never deduplicated); the pinning behavior is superseded by the Following section. Sentence-case floating headings ("Following", "All conferences") in `teamNameEmphasis` replace the CONFERENCES caps label; the tier labels from the earlier P1-3 cut were dropped in review — order stays, labels go. Rows keep the leader teaser and the follow star: the review mock pasted FotMob's plain rows plus a green "Edit" affordance, translated here to keep the shipped affordances and the color budget. **Open questions from the mock, not taken:** an Edit-mode for the Following section (instead of stars), dropping teasers. Acceptance: Top 25 pushes PollScreen unchanged; following a conference from any star makes it appear under Following on next render and unfollowing removes it; with no follows the Following section is just the Top 25 row; a standings-fetch miss hides conference rows, never errors over the poll row; headings carry the header trait. *(Shipped 2026-08-25: `RankingsScreen` restructure + shared `ListSectionHeading`; `ConferenceStandings.pinned` stays for its unit tests but the screen no longer uses it. Verified on-simulator against live preseason data — teasers correctly hidden at 0-0.)*

- [x] **P1** Poll picker fix: `ui.pollChoice` was a raw UserDefaults passthrough, invisible to @Observable — tapping AP/Coaches didn't re-render until the screen was rebuilt. Converted to the stored-property pattern (`scoresGrouping`'s recipe); the chips now switch in real time. *(Shipped 2026-08-25.)*

## E4 — Teams + following

- [x] **P1** Team browse: searchable FBS list grouped by conference. *(Sourced from standings?group=80 — the /teams endpoint has no conference data; see ARCHITECTURE. Sun Belt empty in ESPN's offseason data, renders as absent.)*
- [x] **P1** Follow/unfollow, persisted. Acceptance: follows survive relaunch; Scores Following section updates immediately. *(UI smoke test covers the full flow: search → follow → Following section appears.)*
- [x] **P1** Team page: identity header, record, current-season schedule with results, follow button. *(Schedule shows "Schedule TBA" until ESPN publishes 2026 season schedules; 2025 fixture decode-verified with W/L results.)*
- [x] **P2** Team page season selector: flip to past seasons and see results — chip trailing the SCHEDULE header, same capsule as the Scores header's (extracted to `SeasonMenuChip`), same 2014 CFP-era floor. An explicit year fetches exactly that season, never the silent year−1 fallback (nil-year keeps it); `TeamSchedule.year` decoded from ESPN's `requestedSeason` so the chip label can't drift from the data. Past-season header record derives from final results (`recordSummary`/`standingSummary` track ESPN's *current* season — the mapper nils them when `season.year ≠ requestedSeason.year`), standing hides; shares keep describing the current season. Fetched seasons cache per page visit (a season costs two requests), flipping back is instant. *(Decode-verified against the 2025 fixture: `year == 2025`, derived 12-1; trust rule + derived-record unit-tested.)*
- [x] **P1** ConferencePage: a pushed page per conference — standings table (conference + overall record per team, ESPN's standings order, rows → TeamPage) and a follow-conference pill. Following a conference puts all its games in the Scores Following section (an FCS visitor's game counts via its FBS host); widget and kickoff notifications stay team-follow-driven in v1. Entry points: trailing standings button on Scores conference headers and Teams conference group headers (the header surface stays the expand toggle), and TeamPage's header conference link (formerly the standing line — since 2026-08-24 it shows the conference mark + name even when the standing hides, so it's reachable offseason and on past seasons too). Data: new `conferenceStandings()` on `ScoresProviding` off the same standings endpoint (`stats[]` now decoded: `total`, `vsconf`, `streak`); CFBD parity via `/records`' `conferenceGames` split, sorted by conference win pct (**needs real-key verification** — joins the E6 CFBD list). Acceptance: standings order matches ESPN, not alphabetical; Teams/Onboarding browse unchanged; an empty offseason conference shows "Standings TBA"; preseason renders 0-0 honestly; a conference follow alone makes Following appear and counts as "following somebody"; VoiceOver reaches standings from the header via a custom action; header toggle behavior untouched. Resolves open question #5. *(Shipped: `Features/Conference/` — page, standing rows, follow pill/star, header standings link — merged via PR #21 (`7402596`); search results route here too (`2bb1b69`). CFBD `/records` parity still rides the E6 real-key verification.)*
- [x] **P2** Onboarding moment: first launch prompts "pick your teams" (skippable). *(Sheet over RootView, shown once and only when following nobody — an upgrader with follows never sees it; dismissed any way (Skip, Done, swipe) sets ui.onboardingSeen. Searchable conference-grouped list where the whole row toggles follow. Verified on-simulator: fresh install shows the sheet, flagged relaunch doesn't. Smoke test launches with -ui.onboardingSeen YES to stay deterministic. Field note: sheet content only inherits environment applied outside the .sheet modifier — RootView orders .environment after .sheet for that reason.)*
- [x] **P1** App-wide search: a `Tab(role: .search)` — the FotMob-style circular magnifier beside the floating tab bar on iOS 26, a plain fourth tab on iOS 18 — searches teams, conferences, and the selected week's games. No new fetching — teams/conferences from a shared `TeamDirectoryStore` (one standings fetch at launch), games from the already-loaded scoreboard. Results route through `Router` pending intents; a new `pendingConferenceId` lands on the Teams tab with that conference expanded and scrolled to (intent-only, so the in-flight conference-destination work can re-point one resolver). *(Re-pointed 2026-08-10 exactly as planned: the resolver now pushes ConferencePage — the conference's standings — instead of expanding the browse list; the expand-and-scroll machinery went with it.)* Acceptance: diacritic-insensitive matching ("jose" finds San José State), followed teams rank first, a no-results state exists, Cancel returns to the previous tab untouched. *(Shipped: `Features/Search/` + `TeamDirectoryStore`, merged via PR #20 (`1ee4a9d`).)*
- [x] **P1** TeamPage leads with the next game (P1 review of the Figma "P1 Variation" frame, Andy's edits 2026-08-25): the page moves to the app's card-on-recessed surface — identity header on `bgRecessed`, then a **Next game** card (both sides as matchup rows: logo, curated rank where ≤ 25, name, record — all already in the schedule payload — with kick day/time-or-TBD and network in a trailing status column; live games show the live line instead), then the **Schedule** card (season chip + rows unchanged inside). Card headings sentence case ("Next game", "Schedule") in `teamNameEmphasis`, replacing the SCHEDULE caps label. Next game = the current season's first pre/live game; a past season, empty schedule, or finished season shows no card. Tapping it pushes game detail — the Teams and Rankings stacks register a `Game` destination like Scores already does. The last-5 form strip from the original rec was **dropped in review** (the matchup card's records carry the state). **Second pass (same day — Andy: "make sure we get all the updates made"):** the team-color hero header and tabs came in after all. The hero paints in ESPN's `team.color` (newly decoded from the schedule payload) with white ink and a white logo disc — luminance guard flips to black ink on light colors, monochrome fallback when the payload has none; the bell / season chip / follow pill cluster moved into the hero (chip in a translucent scrim style, `onDark` variants on all three controls); a Games / Standings tab pair landed, Standings being the team's conference table with its own row anchored (absorbing the P2 anchor rec and the P3 mini-table); Overview and News stayed out — no content source. The season chip left the Schedule header, card headers went table-style (`CardHeader`), schedule row logos grew to 32 pt, and final scores became green/red chips (`rankUp`/`rankDown`, mine-first order as the non-color signal). `TeamConferenceLink` retired in favor of the hero's conference · record line. Both budget changes recorded in the decisions log. Acceptance: pre rows show "TBD" for `timeTBD` kicks; VoiceOver speaks the card as one sentence; header follow pill, bell, share, conference link, and season flipping all unchanged. *(Shipped 2026-08-25: new `NextGameCard`, `TeamPage` card-on-recessed restructure, `TeamScheduleSection` heading; `Game` destinations registered in the Teams and Rankings stacks. Verified on-simulator: Michigan shows WMU @ #16 Michigan, Sat 9/5 7:30 PM NBC, with TBD October kicks rendering honestly.)*
- [x] **P1** ConferencePage Games tab (Andy, 2026-08-29: "standings but no games"): the TeamPage tab pair lands on the conference page — Games (default) shows the season's full conference slate, one card per week with `GameRow` rows pushing game detail, postseason in its own trailing card (`Game.seasonType`, newly mapped from both backends, keeps a title game's restarted week number out of "Week 1"); Standings unchanged one tab over, and standings-anchored pushes still land there. The season chip drives both tabs, per-year cache; the page always opens at the top with the hero in view (a scroll-to-current-week first cut was reverted same day — jumping past the hero on push felt broken). Data: `conferenceGames(conferenceId:year:)` on `ScoresProviding` — ESPN scoreboard `groups={confId}&dates={year}` (one request per conference-season, no polling; verified live: ACC 2026 = 134 events incl. type 3), CFBD via `/games` regular+postseason filtered by mapped conference id. Acceptance: only games with a side in the conference; explicit year returns exactly that year; grouping unit-tested (weeks ascend, postseason trails, chronology within groups). *(Shipped 2026-08-29: `ConferenceGamesList` + `ConferenceSlate`, ConferencePage tab restructure, protocol + both clients; 3 grouping tests.)*
- [x] **P2** ConferencePage grows the season selector (P2/Rec 5 of the follow-moment plan; FotMob's league table leads with identity + time): `SeasonMenuChip` — third call site — trailing the STANDINGS header, same 2014 CFP-era floor. `conferenceStandings(year:)` joins `ScoresProviding` (a protocol-extension default keeps the no-arg call sites and test stubs working); ESPN passes `season={year}` (verified live 2026-08-25: 2024 returns Army 8-0 atop the American, season-scoped membership included), CFBD maps the year onto its already-year-scoped `/records`. Fetched seasons cache per visit. The Conference/Overall record *toggle* from the original rec is **deliberately not built** — the table already shows both records as columns, which is the toggle's whole job. Acceptance: an explicit year fetches exactly that year; flipping back is a cache hit; an empty past season shows "Standings TBA"; current-season behavior and every entry point unchanged. *(Shipped 2026-08-25 with the anchor rec below. Verified on-simulator: SEC 2024 renders the real table — Texas 7-1/13-3 over Georgia in ESPN's tiebreaker order.)*
- [x] **P2** Standings anchor "your" row (Rec 6, completing what the TeamPage Standings tab started): `ConferenceDestination` gains an optional `highlightTeamId` (defaulted, so every existing push compiles unchanged); the TeamPage hero's conference line passes its team id; ConferencePage tints that row `bgHeader` and scrolls it into view once standings land. Acceptance: pushes from Scores/Teams headers, search, and Rankings rows highlight nothing; the highlight survives season flips. *(Shipped 2026-08-25. Verified on-simulator: Alabama's hero conference line lands on the SEC page scrolled to Alabama's tinted row, and the tint held through a 2026 → 2024 flip.)*
- [x] **P2** Championship cut line in conference standings (P3/Rec 7 of the follow-moment plan — the monochrome translation of FotMob's colored table zones + legend; the plan's other P3, the TeamPage mini-table, was absorbed by the Standings tab and is closed): a full-width hairline (the existing divider token — the inset row dividers keep their leading pad, so full-bleed *is* the distinction) after the second row, with a one-line footnote at the table's foot ("Top two reach the championship game", meta type, textSecondary). Rendered by a shared `StandingsList` extracted from the two row loops (ConferencePage + TeamPage's Standings tab) so the rule lives once. Honesty gates: only when standings say something (the leader rule's non-0-0 record, so preseason shows nothing), only for seasons from 2024 on (divisional-era title games weren't top-two, and the line must not lie about 2019), and never for Independents (no title game). Acceptance: 2024+ past seasons show the line and footnote; the current preseason and pre-2024 seasons don't; Independents never; VoiceOver reads the footnote once, not per row; text legend, no swatches — chrome, not color. *(Shipped 2026-08-25 — with a data-honesty fix the verification surfaced: past-season standings payloads arrive sorted by OVERALL record (2024's American listed Memphis over 7-1 Tulane), so the mapper now orders by ESPN's `playoffSeed` when a conference is fully seeded (newly decoded; payload order kept on gaps — 2024 MAC ships zeros), and the cut line additionally requires the top two rows to literally be seeds 1 and 2. Sun Belt gated to 2026+ as the divisional holdout; Conference.titleGameIsTopTwo + seedOrdered unit-tested (158/158). Verified on-simulator: 2024 CUSA shows Jax State / WKU — the real title-game pair — above the full-bleed line + footnote, with 10-3 Sam Houston correctly third; 2026 preseason shows nothing.)*
- [x] **P1** Teams browse joins the review direction (same source): an **All conferences** floating heading (sentence case, `teamNameEmphasis`) separates the Following card from the conference accordions; the accordions reorder alphabetical → tier-then-name (`Conference.tier`, matching the Rankings mapper's order — ACC still sorts first, which the UI tests lean on); the trailing `ConferenceStandingsLink` icon comes **off the browse accordion headers** per the mock — standings stay reachable through the header's context menu and VoiceOver custom action, and every other ConferencePage entry point (Scores headers, TeamPage conference line, search, Rankings rows) is untouched. Tier labels dropped in review, tier order kept. Acceptance: browse rows and their follow stars unchanged; search results unchanged; the onboarding sheet's list unaffected; headers keep the whole-width toggle promise; `ConferenceUITests` re-routed through the context menu. *(Shipped 2026-08-25: browse-local tier sort in `TeamsScreen`, `ListSectionHeading("All conferences")`, trailing icon removed with context menu + VoiceOver action kept; 155/155 unit tests pass, verified on-simulator.)*

- [x] **P1** Standings tables gain a place column (Andy, 2026-08-25): 1-based position of the displayed order as the first column, GameRow's rank recipe (metaEmphasis, right-aligned), with the `# / TEAM / CONF / OVR` captions unified into the shared `StandingsList` so ConferencePage and the TeamPage Standings tab speak one table language; rows speak "Number 3, Georgia…". *(Shipped 2026-08-25.)*
- [x] **P1** ConferencePage moves onto the TeamPage template (Andy, 2026-08-25): monochrome hero (conferences ship no ESPN color) with the season chip + follow pill cluster top-right, mark in the `logoBacking` disc, name at `heroTitle` with a team-count line, and Standings as a `CardHeader` card on the recessed surface — anchor scroll, cut line, and per-year caching all carried over; nav title moves into the hero like TeamPage. *(Shipped 2026-08-25.)*
- [x] **P1** TeamPage hero tabs follow the Figma header component exactly (Andy, 2026-08-25 — "the tabs feel a little crammed"): 40pt gap, 14pt vertical padding, bold-14 labels at −2% tracking (new `tab` token), 3pt bottom bar spanning the tab, inactive ink at 50%. *(Shipped 2026-08-25.)*

- [x] **P1** TeamPage schedule rows push game detail (Andy, 2026-08-25) — `NavigationLink(value: game)` around each `ScheduleRow`; the Teams and Rankings stacks already registered the `Game` destination for the Next game card, so the rows ride the same rail. *(Shipped 2026-08-25.)*

- [x] **P1** Game detail moves onto the card template (Andy, 2026-08-25: "good bones" header, recessed page, content in cards): header keeps its layout on `bgPrimary`; linescore, Scoring, Team stats, Leaders, and Drives become `CardHeader` cards on `bgRecessed` (sentence-case titles replacing the caps labels, which came out of the sub-views); `CardHeader` gains an optional trailing subtitle carrying Team stats' "UGA · TENN" column legend; nav bar pinned to `bgPrimary`. *(Shipped 2026-08-25.)*
- [x] **P1** Game page preview cards (Andy, 2026-08-25, from the FotMob Preview reference): a **Game info** card pre-kick — kickoff line (TBD-aware), network, venue + city/state, capacity/surface, weather — from newly decoded `gameInfo` fields (address, capacity, grass, weather; all optional, rows degrade away); the pre-game footer retires (the card covers it, finals keep theirs). Plus a **Standings** card on every current-season game: just the two competing teams, each with conference caption, place number, and records (one caption + place-ordered rows when they share a conference), rows pushing the full table anchored. Standings ride the summary fetch (quiet failure, no 30s refetch); past-season games show neither current-season table. *(Shipped 2026-08-25, verified on-simulator: Ball St @ Ohio St renders kickoff/BTN/Ohio Stadium · Columbus, OH/Turf and MAC #7 / Big Ten #11.)* *(Amended same day: the card also hides preseason — its place numbers are last season's carried-over order, the leader-teaser rule — gated on the overall record, since 0-0 in conference is legitimate all September while non-con games fill the overall line. Shared `StandingsColumnCaptions` added so the CONF/OVR columns are labeled here and in the full tables.)*
- [x] **P1** Non-FBS team pages can't claim FBS conferences: a loaded FBS directory that doesn't contain the team vetoes the schedule payload's conference id — no conference line, no Standings tab, never a wrong one. *(Corrected 2026-08-25, same day: the motivating "bug" — NDSU showing Mountain West — turned out to be real data: ESPN's 2026 standings list NDSU as an MW member, so the directory contains them and the hero was right all along. The veto ships anyway as the guard for genuinely non-FBS opponents — Merrimack, Grambling — whose payload group ids would otherwise collide with the FBS table.)*
- [x] **P1** Hero color reaches the status-bar strip (Andy bug report, 2026-08-31: the area behind the back/share buttons showed gray): the in-content `-1000`pt background inflation never escaped the ScrollView's clip, so `HeroTopBand` now paints the color as a top-anchored band outside the scroll whose height tracks the offset — full inset at rest, growing through the bounce, gone before the solid-toolbar handoff; its bottom edge is the hero's top edge, so the old fixed-band bleed can't recur. ConferencePage gets the monochrome variant for its bounce. *(Shipped 2026-08-31.)*
- [x] **P1** Tab slides get one consistent choreography and logos ride the cards (Andy, 2026-08-31): the push edge was set in the same transaction as the tab, so the outgoing pane resolved against the stale edge and directions alternated — the edge now commits first (the week swipe's transaction split) and derives from tab ordinals; `.geometryGroup()` keeps each pane's subtree moving as one unit; `LogoCache` gains a synchronous NSCache front so freshly-inserted panes paint cached marks on their first frame instead of blank discs; the duplicated tab row extracted to `HeroTabBar`. TeamPage + ConferencePage. *(Shipped 2026-08-31.)*
- [x] **P1** TeamPage Overview tab (Andy, 2026-08-31): leads the tab row and is the landing tab — the same Next game / Current game card the Games tab keeps, plus a `TeamRecordCard` splitting the season into conference and overall W-L from the standings payload (fetched at page level now, so the record is ready without visiting Standings). Past seasons show overall only via the derived record; no-conference teams skip the conference row; preseason hides the card behind the 0-0 gate (the `MatchupStandings` rule). Supersedes the 2026-08-25 "Overview and News stayed out — no content source" note: the standings fetch is the source. News still has none. *(Shipped 2026-08-31.)*
- [x] **P1** Entity-page headers go card-color, FotMob-style (Andy's design review, 2026-08-31): game, team, and conference headers + their nav bars paint `bgCard` — the team-color hero retires (the budget's fourth exception comes out; back to three), and with it `colorHex`, `Color(espnHex:)`, the luminance guard, the tinted-glass bar circles, and TeamPage's transparent-until-scrolled bar. Team logos render bare on the header (`500-dark` carries dark mode; the white disc dropped); conference marks keep the dark-only `logoBacking` disc (ESPN serves no `ncaa_conf/500-dark` variants — probed live, all 404). `HeroTabBar` drops the 3pt underline (active/inactive is opacity alone). Controls consolidate onto the toolbar row — bell + follow + share beside the system back button (conference: follow pill) — and the season chip moves into the Games/Standings panes, trailing above the first card. ConferencePage reorders Standings-first (now the entry default; standings-anchored pushes unchanged). TeamPage's Standings tab gains past seasons via `conferenceStandings(year:)`, cached per year like ConferencePage; the Overview record card stays pinned to the current season. Known edge, pre-existing: divisional seasons (2019 AAC and friends) nest entries under division children the mapper doesn't read, so they degrade to "Standings TBA". *(Shipped 2026-08-31; verified on-simulator in both modes.)*

## E5 — Season hardening (in-season, field-notes-driven)

- [ ] **P1** First-live-game verification pass (earliest: Week 1, Aug 22): live/final row variants on-device, Live toggle, 30s polling logs, in-place update preserving scroll/accordion state. Everything is unit/preview-verified but ESPN serves only pre-game states until then. *(Field note 2026-08-23: "earliest Aug 22" was ESPN's week window, not a kickoff — the 2026 slate's first game is Sat Aug 29, 12:00 PM ET (UNC vs TCU), so the live pass runs then. Pre-game dry-run done today on 1.1.0 (8), iPhone 17 Pro / iOS 26.5 sim, real Week 1 slate (99 events): polling **negative case verified by log archive** — ~6 min foregrounded incl. an open game detail produced zero `polling:` lines from scoreboard or gamedetail and zero errors, while the widget's timeline builds logged the correct hourly idle cadence (next kickoff > 1 hr out), proving the log pipeline itself works. Live chip correctly absent with zero live games; pre-game detail header, day dividers, ranked badges, and dark-mode logo variants (Duke's white D, Cal's gold script) all render right. Remaining for Aug 29: live/final rows, Live toggle, `polling: started/tick` at 30s, in-place update preserving scroll/accordion, detail-poll self-stop on final, widget 15-min live cadence.)* *(Field note 2026-08-29, the live half — run against the real Week 1 slate (99 events) with SJSU @ USC, NCSU @ UVA and JVST @ NDSU in progress and UNC 15–10 TCU already final. **Passed:** live row (heavy monospaced score, `Q3 7:15`, red LiveDot caught mid-pulse across frames, possession football beside the team that has it); final row (winner in full-strength ink, loser grayed, score-only, sentence-case "Final"); Live chip present only with live games, and toggling it hides every non-live game *and* the sections that empty out (Following disappears when no followed team is live), toggling back restores sections, rows and scroll exactly; `polling: started` then `tick` every ~30.8s; `polling: stopped (scene inactive)` on backgrounding; `detail polling: started` + `tick` every ~32s on a live game's detail, whose linescore grid (1/2/3/4/T), quarter-grouped scoring plays, Leaders (passing/rushing/receiving) and Drives cards all populate live; in-place score updates (SJSU 0 → 3 → 11) landing with accordion and scroll untouched. Section completeness held under the filter — the two live games rendered across four sections (Top 25, ACC, Big Ten, Mountain West) with no dedupe. **Found:** halftime renders as `Q2 0:00` on every surface, observed live on NCSU @ UVA at 17:06 ET — filed as its own P1 above. **Also found:** an open live game detail popped itself back to Scores with no input, observed on SJSU @ USC at 18:03 ET — filed as its own P1 above. It also leaves the detail-poll **self-stop on final unverified**, since that pop, not a final whistle, ended every hold. **Not verified:** the widget's 15-min live cadence — the only followed team on the widget-bearing sim (Georgia) doesn't play until 9/5, so the provider correctly logged the hourly idle cadence all afternoon and the live branch never ran; `GameSelectionTests` still covers it in unit tests. Two environment notes for next time: `simctl openurl` can't drive `statside://` (the scheme is deliberately unregistered, so external opens fail with LSApplicationWorkspace error 115 — deep links only arrive via widgetURL/notifications), and a live row's accessibility label churns on every poll, so `element.tap()` loses the race and needs a frame-derived coordinate tap.)* *(Field note 2026-09-01 — **detail-poll self-stop on final: verified**, the sub-item Aug 29 couldn't reach because a ghost activation, not a whistle, ended every hold. Run deterministically against `FixtureScoresClient` (`fx-fades` goes final on the 8th fetch, poll compressed to 0.5 s) on iPhone 17 Pro / iOS 26.5: `detail polling: started for event fx-fades` at 14:44:12.303, ticks at :12.830 / :13.413 / :14.149 / :14.886, then `detail polling: stopped` at 14:44:14.902 — 16 ms after the tick whose summary came back final — and **zero** further detail ticks across the remaining ~20 s of the hold, while the scoreboard's own `polling: tick` never missed a beat through 14:44:34. That second stream is the control: the app was foregrounded and alive, so the stop was the `isLive` gate retiring the loop, not a crash or a pop. The detail stayed pushed throughout and the header read "Final". **Found + fixed while verifying:** the detail header's LiveDot and live-weight score font were gated on `game.isLive` — the *pushed snapshot*, frozen at push — instead of the merged status, so a game ending under an open detail kept a pulsing live dot above the word "Final". `GameShareCard` had always used the merge; the header's two sites were the outliers. Both now read `isLiveNow`, with `GameHeaderStateTests` covering the gate directly (final and postponed summaries stop the poll; a final snapshot under a still-live summary deliberately keeps polling). **Remaining, and the only thing still owed on this item:** the widget's 15-min live cadence observed in the field — it needs a followed team playing on a live Saturday, and stays unit-covered by `GameSelectionTests.liveGamePolls15Minutes` until then.)*
- [x] **P1** Halftime reads as a stopped clock. ESPN's `STATUS_HALFTIME` arrives as `state: "in"` carrying `period: 2`, `displayClock: "0:00"` and `detail: "Halftime"`, and every live-status renderer builds its string as `[quarter, clock]` joined, taking `GameStatus.live`'s `detail` only as a fallback for when both are nil — which halftime never is. So the app says **"Q2 0:00"** where every other scores app says "HALF", on all five surfaces: `GameRow`'s two status-column variants, the game detail header, the widget's `NextGameEntry`, `Game+ShareText`, and `GameShareCard`. VoiceOver inherits it too ("2nd quarter, 0:00 left"). `GameRow.liveDetail(clock:period:)` doesn't even take `detail` as a parameter, so it can't fall back at all. Same swallow hides "End of 1st/2nd/3rd Quarter", which ESPN also sends mid-`in`. Fix direction: honor `detail` when the clock has run out (or map the status name explicitly at the `ESPNClient` boundary, so the domain model carries a halftime case rather than every renderer re-deriving it) — a boundary mapping keeps the five surfaces from drifting again. Acceptance: halftime renders "Half"/"Halftime" on all five surfaces and speaks as halftime in VoiceOver; end-of-quarter stops claiming a running clock; OT periods keep their current labels. *(Found in the 2026-08-29 live pass, observed on NC State @ Virginia at 17:06 ET.)* *(Shipped 2026-08-31, the boundary-mapping direction: `GameStatus.live` gains a `LivePhase` (`playing`/`halftime`/`endOfPeriod`) mapped from ESPN's status type name (`STATUS_HALFTIME`/`STATUS_END_PERIOD`) in `ESPNMapper.status` — one boundary, so scoreboard, schedule, and summary all inherit it. The five per-surface `[quarter, clock]` joins collapsed into one shared `GameStatus.liveStatusText` ("Q3 5:24" / "Half" / "End Q1" / "OT 0:48", detail fallback, callers add `?? "Live"`); VoiceOver speaks "halftime" / "end of 3rd quarter". CFBD infers halftime from a parked Q2 clock only — a 0:00 in Q4 could be heading to OT. Unit-tested end to end; on-screen confirmation joins the next live-Saturday pass. Bonus: the detail header's double-OT label was hardcoded "OT" — the shared formatter's "2OT" corrects it.)*
- [x] **P1** An open live game detail pops itself back to Scores. Observed 2026-08-29 on SJSU @ USC: pushed at 18:01:31, `detail polling: tick` at 18:02:03, 18:02:35 and 18:03:07, then `detail polling: stopped` at 18:03:14 with no input. Per-minute screenshots bracket it: detail still up at 18:02 (Leaders and Drives populated), Scores list at 18:03. The stop is the view disappearing, not the game ending: same pid, no crash, `polling: tick` from the scoreboard never missed a beat, and both the scoreboard and `summary` endpoints still reported `STATUS_IN_PROGRESS` (Q4 1:44) six minutes after the pop. Intermittent — an earlier push on the same build sat on the same game for ten minutes without popping — so the trigger is a state change, not elapsed time. This is the app's core Saturday moment (watching one game live), and losing the screen mid-drive is worse than a stale clock. Acceptance: a live detail stays pushed across scoreboard refreshes, section re-sorts and a week's worth of status changes, including the game itself going final; a regression test holds a live (or fixture-live) detail across several refresh cycles and asserts the screen is still up. *(Found in the 2026-08-29 live pass.)* *(Resolved 2026-08-31 — **not an app bug: an XCUITest ghost activation on iOS 26.5.** Reproduced deterministically with a scripted backend (`FixtureScoresClient` behind `-data.provider fixture`, poll compressed to 0.5s via the DEBUG-only `poll.interval` default) serving a full Saturday's churn — scores/clock ticks, halftime phases, a game going final, a kickoff, a payload dropping a game, a rank flipping, transient fetch errors. Under an XCUITest hold that queries the accessibility tree every second, a navigation control occasionally fires with **no synthesized touch**: on this tree the detail header's team `NavigationLink` pushed a TeamPage (path-depth log 1 → 2, screen recording shows no touch, the xcresult records exactly one synthesized event — the original row tap), 2 fires in 10 holds; on the Aug-29 tree (`08810ec~1`, worktree backport) the ghost hit the **back button — pop to Scores, 3 of 3 runs, matching the field observation exactly** (one run also collapsed accordion headers afterward — the ghost keeps firing whatever's under it). Control experiments: the same app state held by hand (simctl launch + one tap, no XCUITest) never moved in 8+ minutes across two sims, and query-free XCUITest holds (sleep, then one final check) passed 3/3 — the trigger is the per-second a11y querying, same iOS 26.5 family as the alert-tap breakage above. The app can't defend against ghost taps of its own back button, so the fix is environmental: `LiveDetailHoldUITests` is the acceptance regression suite (live detail held across ~45 compressed refresh cycles in both groupings, under the Live filter, and across the held game going final — all green), its ghost-repro diagnostics stay behind `GHOST_REPRO=1`, and `ScoresScreen` now logs path-depth changes (`scoresnav` category) so any future field pop discriminates path-write vs state-reset vs ghost in one log line. **Real bug found en route, fixed:** one flaky pre-game summary for a live game permanently killed detail polling and demoted the header to pre — the summary always outranked the scoreboard snapshot, and with polling dead nothing ever refetched it, freezing the screen mid-drive. `GameHeaderState.status` now merges the two with a kickoff-doesn't-un-happen rule (a pre summary never demotes a live snapshot; final/postponed still win), which every header surface, the share card, and the poll gate inherit. Unit-tested in `GameHeaderStateTests`. Remaining for the Sep 5 live pass: the detail-poll self-stop on final against real ESPN (the fixture covers it, live confirmation is still owed).)*
- [x] **P1** Performance pass with a full Saturday slate (60+ events, all sections expanded). *(Field note 2026-09-01, static half — a code audit of the full-slate hot paths; the on-device Instruments run is still owed, see remaining below. Found + fixed three things: **(1)** the sections pipeline ran up to three times per body evaluation (error-banner check, content, swipe preview pane) — and ScoresScreen's body re-evaluates on every frame of the interactive week drag, since `dragOffset` is @State, so a drag rebuilt the full filter/sort/bucket over ~100 games at up to 120Hz; `sections(from:)` now memoizes in the store, keyed on full game content (never ids alone — a poll tick that only moves a clock must invalidate; regression-tested), bounded at 8 entries, `@ObservationIgnored` so a body-time fill can't re-invalidate views. **(2)** Every section re-sorted its own slice — Following, Top 25, ~17 conferences, day buckets, ~20 sorts of duplicated games per build; `visible` is sorted once up front and every bucket inherits chronology, byte-identical per-section results since the comparator is a total order (date, then id) and filter/bucketing preserve order. **(3)** A no-change 30s poll tick invalidated the whole scores tree: `fetchSelectedWeek` reassigned `weeks`/`seasonYear`/`games`/`lastError` and the week cache unconditionally, and @Observable notifies on every set regardless of equality — all five writes are now equality-guarded. Checked and healthy: LogoCache (deduped, negative-cached, sync NSCache front), UIStateStore (in-memory sets), GameRow's re-render scope under polling, LiveDot (GPU-composited opacity pulse). Flagged, not fixed: in date grouping a full Saturday's day section materializes all ~60 rows in one eager VStack inside its `Section` — per-row laziness would break the unified card shadow, so that's a design conversation only if profiling shows a build hitch when the section enters the viewport. Remaining, needs a Mac: run the unit suite (this pass was written off-Xcode; the refactor is covered by the existing section tests plus the new memo-staleness regression), then Instruments against a real Saturday slate — scroll hitch rate with everything expanded in both groupings, the week drag, memory after browsing several weeks, and the date-mode Saturday section's first-appearance cost.)* *(Field note 2026-09-01, dynamic half — the Mac verification and profiling the static half owed; item closed. **Verified:** full unit suite green on this Mac (197 tests, iPhone 17 Pro / iOS 26.5 sim) over the memo/sort/equality-guard refactor and the WeekLogic Sunday tiebreak. **Profiled** against the real Week 1 slate (99 events, Saturday Sep 5 = 68 games) driven by scripted gestures — expand-all pinch, flick-scrolls through both groupings fully expanded, interactive week drags, and the flagged date-mode Saturday section expanded cold from a collapsed screen: 10s of 1ms sampling during hard scrolling shows the main thread parked in the run loop with app code at single-digit samples (heaviest app frame 13/10000; the sections pipeline never surfaces — the memo works), and the Saturday section's eager 68-row build never rises above noise (~16ms of view-graph work across an 8s window that included the expansion plus five flicks through it). The flagged eager-VStack design conversation is closed as measured-healthy; the unified card shadow stays. **Found + fixed:** memory grew unbounded with browsing — 55 MB at launch, 372 MB after six weeks of slates — all decoded 500px logo bitmaps in `LogoCache`'s uncapped NSCache. Not a leak (NSCache sheds under pressure), but the cache now carries a 64 MB `totalCostLimit` with per-image decoded-byte costs, so the footprint is budgeted instead of riding jetsam — re-measured at 247 MB after an even heavier browse (twelve week transitions), the remainder being view-layer and render caches, not logo pixels. **Environment notes:** Animation Hitches is device-only (`Hitches is not supported on this platform`), and `xctrace record --attach` against a simulator hung on finalize twice under Xcode 26.6 (0.4% CPU, empty trace) — `sample` at 1ms is the workable CLI profiler for sim runs. True hitch-rate numbers therefore still want one on-device Instruments look someday — field-notes material for a device session, not a blocker: the CPU picture says the scroll is quiet.)*
- [x] **P1** ConferencePage's Games tab froze live games. Found 2026-09-01 while closing the detail-poll self-stop above — the same defect class, one screen over. The season slate is fetched exactly once per (conference, year) and never polled (right for a page that is mostly history), and the page rendered it raw, so a game that was live when the page opened stayed at that score and clock forever, and one that kicked off later stayed pre-game. Reproduced against the fixture: an SEC page sat on "Alpha State 20, Bravo Tech 24, halftime" for 8 s while the scoreboard advanced underneath it. This is the dashed-score bug (Andy, 2026-08-29) in its unfixed sibling — TeamPage's schedule merges the scoreboard's fresher copy, ConferencePage never did, and the scoreboard was already in its environment for the standings dots. Fixed by extracting that merge to one place (`Game.merging(_:withLive:)`, dictionary-keyed so a ~100-game season slate doesn't cost a linear scan per row on every body evaluation) and pointing both pages at it — written twice and fixed once is what let them drift. `LiveMergeTests` covers the rule; `ConferenceLiveMergeUITests` covers the wiring and was confirmed to fail without the fix. Also filled in `FixtureScoresClient.conferenceGames`, which returned `[]` and so hid this entire screen from the fixture.

- [x] **P1** Siri announced a fabricated 0-0. Found 2026-09-01 auditing the same defect class one more surface out. `NextGameIntent` built its whole answer from `teamSchedule`, whose payload carries no live score once a game kicks off — but whose *status* does map to live. So the live branch fired with `score == nil` and `liveLine` defaulted it: **"Live now: Georgia 0, Tennessee 0."**, spoken aloud, mid-drive. Worse-presenting than the dashed-score bug it shares a root with, because "0" is a plausible lie where "-" is visibly missing data. Two more, found in the same function: the live pick was `Dictionary.values.filter(\.isLive).first`, so with two followed teams playing at once Siri answered about a different game run to run; and the line carried no clock at all. Fixed: the intent now fetches the scoreboard concurrently with the schedules and merges via `Game.merging` (same rule as TeamPage and ConferencePage), picks the earliest kickoff among live games, never speaks a score it doesn't have ("Georgia and Tennessee are playing right now, in the 2nd quarter, 1:12 left"), and spells the clock out for speech with the halftime rule intact ("at halftime", not "Q2 0:00"). `NextGameIntentTests` covers every line shape.

- [ ] **P2** Rankings ghost-week handling (poll updates Sunday while week still shows Saturday's games). *(Scoped 2026-09-01, deliberately not coded: the item is observational, and the deciding fact can't be gotten from here — ESPN's API is outside the agent environment's egress, and the behavior only exists on a poll-drop Sunday. The open question: when the new AP poll lands (~2 PM ET Sunday), does ESPN flip `curatedRank` on the *completed* week's scoreboard events, or keep rank-at-game-time? If ranks flip, Saturday's finals start lying about what rank a team carried into the game, and Top 25 section membership shifts under the Sunday-pinned completed week — rows would need a rank snapshot taken at final. If ESPN keeps game-time ranks (likely — finals historically do), the "ghost week" is just the poll tab being newer than the slate, which is correct and needs nothing. Field plan for Sunday Sep 6: before the poll drops, note 2-3 ranked finals' displayed ranks; after it drops, pull-to-refresh the pinned week and diff the rows and the Top 25 section against the new PollScreen. Any code change waits on that observation.)*
- [x] **P2** Week rollover edge cases: Week 0, Army-Navy solo week, CFP date ranges. *(Closed 2026-09-01 against the real 2026 calendar (already captured in `scoreboard-live.json`; ESPN's API is outside the agent environment's egress policy, so no fresh probe). What the calendar shows: ESPN encodes Week 0 as the wide Week 1 (Aug 22 → Sep 8), Army-Navy's slot is inside Week 15 in 2026 (no solo week this season), and **Bowls (Dec 13 → Jan 28) and CFP (Dec 18 → Jan 28) overlap for the entire playoff**. Found + fixed: the Sunday rule ran before the ESPN-current-week check and took the *first* slot containing yesterday — array order puts Bowls before CFP, so every Sunday of the playoff pinned to Bowls even when ESPN said CFP. `WeekLogic.defaultSelection`'s Sunday branch now prefers ESPN's `(type, value)` among the slots containing yesterday, falling back to first-containing — which preserves September exactly (ESPN's flipped-forward week never contains yesterday) and the Army-Navy analog (Sunday Dec 13: ESPN on Bowls, but Saturday was Week 15's — Week 15 wins). Six new `WeekLogic` tests pin the overlap Sundays, the overlap weekday, the past-season CFP landing, and the wide Week 1's mid-span Sunday/Monday. Ghost-week note: the December UX around a live CFP Sunday still deserves one on-device look when the playoff arrives — field-notes material, not a blocker.)*
- [x] **P2** Accessibility pass: Dynamic Type, VoiceOver labels on rows ("Georgia 24, Tennessee 17, 3rd quarter"), contrast check on grays. *(Type tokens scale via UIFontMetrics, row logos via @ScaledMetric; every row speaks one sentence — game/rank/schedule label shapes unit-tested; chips get selected traits, accordion headers speak count + expanded/collapsed, LiveDot hidden from VO + respects Reduce Motion. Contrast: light textSecondary 0.44 → 0.42 to clear WCAG AA 4.5:1 on bgElevated; all other tokens verified. Remaining: an on-device VoiceOver listen-through and large-size layout check — field-notes material.)*

## E6 — App Store release

De-iceboxed 2026-07-21 (Andy's call). Gated on open question #6 (data licensing) before public release; everything below except submission itself can proceed now.

Status 2026-08-23: **1.0 (6) is approved and live**, 1.0.1 (7) was cut 2026-08-09, and the project now reads **1.1.0 (8)** (bumped in `b63706e`, carrying the Rankings hub, ConferencePage, app-wide search, week swipe, dark-logo variants, and the widget redesign; upload status not recorded here). Earlier status 2026-08-09: It shipped on ESPN — `DataProvider` still defaults to `ESPNClient`, with CFBD behind a `data.provider` switch that needs a key — so the open-question-#6 gate was crossed by taking the risk, not by resolving it. The remaining P0 below is the CFBD verification that would make the swap real.

- [x] **P0** App icon. The appiconset is empty — this alone blocks submission. Monochrome mark consistent with the design system (icon may spend the red live-dot accent; it's the app's one color). Provide light, dark (transparent bg), and tinted (grayscale) 1024pt variants per iOS 18 conventions. *(Shipped in 1.0 (6): three 1024×1024 PNGs in `sports/Assets.xcassets/AppIcon.appiconset/`, declared as the iOS 18 appearance triple — default, `luminosity: dark`, `luminosity: tinted` — in `Contents.json`.)*
- [x] **P0** Platform trimming: `SUPPORTED_PLATFORMS` → iPhone only (`iphoneos iphonesimulator`), `TARGETED_DEVICE_FAMILY` → 1. iPad runs the iPhone app in compatibility mode; no Mac/Vision availability. Matches the "don't build for other idioms" rule. *(Both set in all 8 build configs — four targets × Debug/Release.)*
- [x] **P0** Privacy policy page (required App Store Connect field even with zero data collection). Draft lives in repo (`PRIVACY.md`); needs a public URL before submission — GitHub Pages is the zero-cost answer. *(Live at `https://weirhere.github.io/statside-site/privacy.html`, with `docs/index.html` serving double duty as the Support URL. Source of truth is `docs/` here; this repo is private, so Pages is hosted from the separate public `weirhere/statside-site` repo and edits have to be copied over.)*
- [x] **P0** Apple Developer Program enrollment + App Store Connect listing: verify "StatSide" name availability, description, keywords, age rating (4+), export compliance (standard HTTPS exemption), privacy nutrition label ("Data Not Collected"). *(All of it landed with 1.0 (6), which is approved and live — the "StatSide" name cleared. Every field is recorded in `docs/appstore/listing.md`. Export compliance never gets asked per-build because `ITSAppUsesNonExemptEncryption = NO` is set in the Info.plist build settings. App id 6793266645, now also the store link in share text.)*
- [x] **P0** Screenshots: 6.9" and 6.5" sets. Real Saturday data reads best — capture during Week 1+ (pairs with the E5 verification pass). *(Five frames — scores, game detail, rankings, teams, team page — captured by `sportsUITests/AppStoreScreenshots.swift` into `docs/appstore/screenshots/` at 1320×2868. Two corrections to the plan above: a single 6.9" set carries forward to the smaller sizes, so the 6.5" set was never needed; and this account's drop zone rejects 1320×2868 outright, so `docs/appstore/screenshots-1284x2778/` holds the resized copies that actually upload. Shot in the offseason rather than Week 1+ — submission couldn't wait for real Saturday data. Screenshots carry forward across version updates; 1.0.1 reused them unchanged. The widget frame is still missing — that's E7's remaining P2.)*
- [ ] **P0** `CFBDClient`: a CollegeFootballData.com v2 backend conforming to `ScoresProviding` (Andy's call 2026-07-21, resolving open question #6 toward option a). ESPN stays the default; CFBD activates via `data.provider` = "cfbd" + a key in `cfbd.apiKey`/`CFBD_API_KEY`. CFBD's scoreboard rows don't carry records/ranks/logos/network inline, so the client joins `/teams/fbs`, `/records`, `/rankings`, and `/games/media` (all season-cacheable) onto `/games` + live `/scoreboard`. Acceptance: all five protocol methods return domain models with no view-layer changes; a malformed element drops the row, never the response; unit tests cover status mapping (pre/live/final-OT), conference name→id mapping, poll movement, and the live merge. **Needs verification with a real key (Tier 1+):** live-field shapes (`possession`, `clock`), whether CFBD team ids match ESPN's (follows persistence across the switch), and score latency vs ESPN on a live Saturday.
- [x] **P1** Version/build hygiene: marketing version tracks the release (1.0 shipped, 1.0.1 is build 7), build numbers increment per upload. Both `MARKETING_VERSION` and `CURRENT_PROJECT_VERSION` live in 8 places each in `project.pbxproj` — one per target per config — and the app and widget **must** match or the upload is rejected. Chore worth doing: hoist both to the project-level config so a bump is two lines, accepting that Xcode's Target → General fields write back per-target and would undo it. *(Held across both uploads: 6 for 1.0, 7 for 1.0.1, each time consistent across all 16 slots. The hoist stays undone on purpose — the write-back problem makes a project-level value a trap rather than a shortcut — so a bump remains a search-and-replace, verified with `grep` before archiving.)*
- [x] **P1** GitHub Actions CI: build + `sportsTests` on a macOS runner for every PR and every push to main (Andy, 2026-09-01 — the overnight agent loop's enabler: remote agent sessions have no Xcode, so a PR-triggered build is the only "build it" they get, and nothing agent-authored merges without it green). Workflow selects the newest Xcode on `macos-latest`, discovers an iPhone simulator instead of hardcoding one, runs unit tests only — `sportsUITests` drives the live ESPN API under simulator-state rules (CLAUDE.md) a fresh runner can't honor. Acceptance: the workflow goes green on a real PR containing a Swift change; a deliberately broken test would fail it. *(Shipped 2026-09-01, green on PR #40's first run — 6m27s on `macos-latest`, whose tree carried the E5 perf-pass Swift (memoization + new regression test), so the build-and-test path is proven end to end.)*
- [ ] **P2** App Store product page polish: preview video, promotional text. Only after the listing exists.

## E7 — App Review 4.2.2 response

Added 2026-08-04: Apple rejected 1.0 (5) under Guideline 4.2.2 Minimum Functionality ("does not sufficiently differ from a web browsing experience"). The fix is a native-functionality bundle — widgets and notifications come out of the Icebox (they were the roadmap's "killer feature" anyway; the rejection just moved the date). Resubmit as 1.0 (6).

**Outcome: it worked.** 1.0 (6) was approved and is live on the App Store. Everything below shipped in that build except the two P2s — the store link, which landed in 1.0.1 (7) once there was a URL to link to, and the widget screenshot frame, still uncaptured.

- [x] **P0** Home-screen widget (`StatSideWidgets` extension target): followed teams' live/next game, systemSmall + systemMedium + accessoryRectangular, monochrome design system, sparse budget-aware timeline (15 min live / hourly idle), last-good snapshot on network failure. *(Shipped in 1.0 (6). `NextGameWidget`, kind `StatSideNextGame`, all three families. `NextGameProvider` fetches ESPN directly and picks its own next reload via `GameSelection.nextRefresh`: 15 min when anything is live, otherwise the sooner of an hour out and a minute past the next kickoff — the extra minute gives ESPN a beat to flip the game live before we refetch. On a failed fetch it re-serves the App Group snapshot marked stale instead of going blank. `WidgetUITests` covers install, render, and deep-link. Family scope superseded 2026-08-16: systemSmall removed, systemLarge added — see the decisions log.)*
- [x] **P0** App Group `group.com.andyryanweir.sports`: entitlements for both targets, `FollowingStore` on the shared suite, one-time migration of existing follows. *(`StatSideShared/AppGroup.swift` owns the whole contract — suite id, follow key, widget kind, snapshot key — and falls back to `.standard` when the entitlement is missing (previews, tests) so nothing force-unwraps. Entitlements in `Config/sports.entitlements` + `Config/StatSideWidgets.entitlements`. `FollowingStore` reads the suite and reloads the widget timeline on every toggle, so a new follow shows on the Home Screen immediately rather than at the next scheduled reload. The migration deliberately leaves the standard-defaults copy in place — rolling back to build 5 must not lose anyone's teams. `AppGroupMigrationTests`.)*
- [x] **P0** Shared code to `StatSideShared/` (second synchronized root group; models + networking + Theme tokens compile into both targets). *(Second `PBXFileSystemSynchronizedRootGroup`, listed in the `fileSystemSynchronizedGroups` of both the app and widget targets. Holds models, ESPN + CFBD networking, `Theme`, `LogoCache`, `GameSelection`, `AppGroup`; app-only code stays under `sports/`. The deny-list alternative was rejected for the reason in the decisions log — an exception set on `sports/` would silently enroll every future app file in the widget target.)*
- [x] **P0** Deep links: `statside://game/{id}`, `statside://team/{id}`, `statside://teams` via `onOpenURL` + a small Router; scheme unregistered (widgetURL/notification taps only in v1). *(`sports/App/DeepLink.swift` parses the grammar, `Router` holds the pending id, `RootView.onOpenURL` switches tabs and hands off. Screens consume their pending id once the matching data loads; one that never resolves quietly expires when the next intent replaces it. Notification taps skip the URL entirely and set the pending id directly, with the tab switch living in one `onChange` so both entry paths land identically. Scheme still unregistered — no `CFBundleURLTypes` anywhere in the project.)*
- [x] **P0** Local kickoff notifications for followed teams: one reminder 30 min before kickoff, app-wide bell toggle on TeamPage, contextual permission (never at launch), nearest-24 cap, kickoff-encoded request ids so reschedules are a pure diff. *(`NotificationScheduler` + `NotificationDelegate`, all four properties as specified — `leadTime` 30 min, `maxScheduled` 24, ids shaped `kickoff.{gameId}.{kickoff epoch}` so moved kickoffs, cancellations, and TBD-times-becoming-real all fall out of a pending-vs-desired diff with zero special cases. Games where both teams are followed dedupe to one reminder. Fires on `UNTimeIntervalNotificationTrigger` from an absolute instant rather than a calendar trigger, so kickoff doesn't move when the user changes timezone. Bell is `NotificationBell` on TeamPage; permission is asked only on a bell tap or once right after the first-ever follow, and `refreshAuthorization` runs on scene-active so the bell can't lie after a round trip through Settings. `NotificationSchedulerTests` + `ReminderOfferUITests`.)*
- [x] **P0** Haptics (budget of exactly three): follow toggle, refresh success, live score change in game detail only. *(Exactly three, all `.sensoryFeedback`: light impact on the follow toggle (`FollowPill` + `FollowButton` — same event, two entry points), success on Scores refresh, medium impact on score change in `GameDetailScreen`. `GameRow` has none, by design.)*
- [x] **P0** ShareLink: game share text (status-shaped) in game detail + team page + row context menu. *(All three call sites over `Game+ShareText`; `GameShareTextTests` covers the status shapes. The sign-off later moved behind `ShareSignOff.appended(to:)` in 1.0.1 — see the P2 below.)*
- [x] **P0** Context menus: GameRow (follow either team, share), TeamBrowseRow + RankRow (follow). *(On the row inside `SectionAccordion` (both teams + share), plus `TeamBrowseRow` and `RankRow`.)*
- [x] **P0** Review notes rewrite in `docs/appstore/listing.md` (delete the "displays publicly available…" opener; lead with native functionality) + Resolution Center reply draft. *(Both in `docs/appstore/listing.md`. Notes now open on what the user does and enumerate the native surface area; the "displays publicly available…" framing is gone, replaced by an explicit "renders structured sports data in a native interface — it does not aggregate or display web content." Reply drafted to enumerate what changed rather than argue, and includes offseason demo steps since the current week can be empty for a reviewer. Build 6 was approved.)*
- [x] **P0** Build 6: `CURRENT_PROJECT_VERSION` = 6 in app **and** widget configs (must match). *(Matched at submission and accepted; both now read 7 for 1.0.1 — the standing discipline is E6's version hygiene item.)*
- [x] **P1** `NextGameIntent` App Intent / Siri Shortcut ("What's my next game?"). *(`sports/Intents/NextGameIntent.swift`. Reads the follow set straight from the App Group suite rather than through `FollowingStore`, answers with the live line if a followed team is playing right now and the next kickoff otherwise, and has its own dialog for following nobody. `openAppWhenRun = false`, so Siri answers without launching the app.)*
- [x] **P2** App Store URL appended to share text once the app is live. *(Shipped in 1.0.1 (7). The sign-off moved behind `ShareSignOff.appended(to:)` in `StatSideShared/Models/Game+ShareText.swift` because game detail composes its own body from the fresher summary score — two call sites, one sign-off, so the link can't drift out of one of them. Link sits on its own line after "— via StatSide" so messaging apps render a preview.)*
- [x] **P2** One home-screen-widget screenshot frame for the listing (manual simulator capture). *(Captured post-FotMob-redesign and slotted into listing position 2 — `d7f6147`, `a918c9c`.)*
- [x] **P1** Game share carries a rendered matchup card image, not just text. *(A `GameShareCard: Transferable` exports a PNG lazily — logos fetched (1.2 s budget) and `GameShareCardView` rendered by `ImageRenderer` only when a share target is picked, so the 60-row context menu costs nothing. Detail-header layout (logos flanking a status column), always light, type pinned to fixed sizes because the theme's Font tokens bake against the process content size. Share text with the App Store link rides along as `ShareLink(message:)` plus a text-only `ProxyRepresentation` fallback. The iMessage link-preview card itself can't show the game — that's built by the recipient's device from the App Store page's OG tags, and repointing it would need a per-game web page, i.e. a backend.)*

## E8 — FCS coverage (scoped, not scheduled)

Answers Andy's question, 2026-09-01 ("we have all of FBS — do we have all of FCS?"). Today: **no**, and not by accident — the app is FBS-only by construction. `ESPNClient.scoreboard` sends `groups=80` (`ESPNClient.swift:67`); `fbsConferences()` and `conferenceStandings(year:)` send `group=80` (`:105`, `:113`); `Conference.names` is 11 hardcoded FBS ids (`Team.swift:32`), so any FCS id degrades to "Other" with no logo and no tier; `RankingsScreen.pollTypes` filters the FCS poll out (`:11`); and `TeamPage.resolvedConferenceId`'s veto (`:80`) exists specifically to *strip* an FCS opponent's conference claim when it collides with our FBS table. Net: an FCS team is visible only as an FBS opponent — real logo, real record, no conference of its own, sectioned under its FBS host (the 2026-07-21 "Other" fix). FCS-vs-FCS games never enter the pipeline at all. Web matches: `Division = "FBS" | "FCS"` exists in `web/src/lib/types.ts:17`, but `web/src/lib/espn/transformers.ts:162` hardcodes `division: "FBS"` and nothing ever writes `"FCS"` — and `web/PRD.md:83`'s "Browse all FBS and FCS conferences" is aspiration, not shipped behavior.

**Live probe, 2026-09-01** (read-only, group 81 = FCS): `standings?group=81` returns **14 conferences / 116 teams**, and `scoreboard?groups=81` works and ships the *byte-identical* week calendar as group 80 — so the week strip needs no new model. Week 2 2026: group 80 = 86 events, group 81 = 82, **overlap 37** (every FCS-at-FBS game is in both payloads), union 131 — FCS adds ~45 genuinely new games that week, about +52% rows. 13 of the 14 conferences have `ncaa_conf` logo slugs (`big_sky`, `caa`, `fcs_independents`, `ivy`, `meac`, `missouri_valley`, `northeast`, `ovc`, `patriot_league`, `pioneer`, `southern`, `southland`, `swac`); **United Athletic (177) ships none** — the `logoSlugs` miss already degrades to no logo. The FCS Coaches Poll already rides in the *same* `/rankings` response as `type: "fcs"` — no extra fetch to surface it.

- [ ] **P2** **The scope decision — the gate on every item below.** Three shapes, cheapest first: **(a) directory only** — FCS teams get real conference lines, TeamPages, and standings, but the Scores slate stays FBS; **(b) opt-in division** — the funnel sheet gains an FCS section and the scoreboard fetches group 81 only when an FCS conference is selected or followed; **(c) full union** — every week fetches both groups and merges. Recommendation: **(b)**. The Saturday sort order is the product, and (c) drops ~45 extra rows a week in front of someone who came for the SEC, while (a) creates pages whose games the app can't show. (b) makes the cost proportional to the ask. Note FCS is currently iceboxed in *both* BACKLOG (Icebox) and ROADMAP ("Explicitly later — do not build yet"); nothing below starts until Andy closes this.
- [ ] **P2** Registry: FCS ids, names, logo slugs, and a division concept in `Conference` (`Team.swift`). The 14 ids and slugs are enumerated above. `Tier` currently means P4 / G5 / Independent / Other — FCS conferences fit none of them, so either `Tier` gains an `.fcs` rung below `.independent` (keeps one ordering function, and `orderedIds` keeps working) or a separate `Division` axis crosses it. Acceptance: `Conference.name(for:)` returns real names for all 14; `logoURL(for: 177)` returns nil, never a broken image; existing FBS ordering, tiering, and `orderedIds` byte-identical; CFBD name map (`cfbdNames`) either gains FCS names or explicitly documents that CFBD parity stays FBS-only.
- [ ] **P2** Data layer: division-aware fetches on `ScoresProviding`. `scoreboard(...)` and the standings pair take a division (or group id) instead of assuming `Conference.fbsGroupId`; a union fetch merges the two payloads **by event id** — the 37-game overlap is real duplication at the source, and merging there keeps the "sections are complete, never deduplicated" rule about *sections*, where it belongs. Acceptance: FBS-only call paths produce identical results to today; a merged week contains each overlapping event exactly once; `FixtureScoresClient` gains an FCS fixture; `ScoreboardStore`'s per-week cache and ±1 prefetch key on (season, week, division) so a division switch can't serve a stale slate.
- [ ] **P2** **Polling budget — the real risk.** A union fetch doubles every 30s tick to two scoreboard requests, which is the politest-guest rule getting quietly halved. Under scope (b) the second request only exists while FCS is surfaced; under (c) it's permanent. Decide explicitly, and if FCS polls, consider a slower cadence for the FCS half (FCS clocks don't need 30s fidelity to answer "what's the state of the sport"). Acceptance: whatever the choice, `polling:` log lines name which groups are in flight, so the Saturday log archive stays readable.
- [ ] **P2** Teams browse + search corpus + `TeamDirectoryStore` off `group=81`. 116 more teams roughly doubles the browse list (~134 FBS today), so browse needs a division split — FBS groups first, FCS below, or a segmented control. Acceptance: search ranks an FCS team below an equally-matching FBS team (the corpus grew, the common query didn't); the follow-prompt copy and onboarding browse still read right at 250 teams; a conference with zero standings entries still renders (SWAC ships **0** entries today — the Sun Belt hole, second instance).
- [ ] **P2** Rankings hub: an FCS poll row and FCS conference rows. `pollTypes` gains `"fcs"` (already in the same response; DII/DIII stay filtered), and the CONFERENCES card gains the 14 FCS rows under their own header. Acceptance: AP stays the default; the picker doesn't grow past thumb reach; `ui.pollChoice` round-trips the new value; an FCS conference row's leader teaser follows the existing preseason 0-0 gate.
- [ ] **P2** Revisit `TeamPage.resolvedConferenceId`'s veto (`:80`). Once the directory knows all 116 FCS teams, the disproof-shaped veto's whole premise — "an FCS opponent's payload can reuse a group id that collides with our FBS table" — changes: the collision it defends against becomes a resolvable id instead of an unknown one. Acceptance: NDSU, Sacramento State, and Tennessee State (179) all resolve to their real conference; the Sun Belt regression the veto was rewritten to fix (2026-08-29) stays fixed; the existing veto tests still pass or are replaced with better ones and the reason is written down.
- [ ] **P2** Filter sheet + Following: FCS conferences join `ScoreFilter`, and under scope (b) selecting one is what turns the group-81 fetch on. Acceptance: "All games" means the same thing it means today unless the user opted into FCS (the landing page's full-slate promise is FBS-shaped and should stay that way until Andy says otherwise); a followed FCS conference makes Following appear exactly like an FBS one; **widget and kickoff notifications stay FBS/team-driven** — no scope creep into the 64-pending cap.
- [ ] **P2** Web parity, after iOS lands: honor the existing `Division` type in `transformers.ts` instead of hardcoding `"FBS"`, mirror the registry, and either implement or correct `web/PRD.md:83`. The frozen favorites-migration map (`favorites-migration.ts:79`, "only the 11 known FBS group ids survive") is deliberately frozen — extend the *live* registry, not the migration table.

**Known limitations to accept up front:** ESPN serves group 81 the FBS calendar verbatim, so the FCS postseason renders under the labels "Bowls" and "CFP" — wrong for a 24-team FCS playoff, and not fixable from this endpoint. SWAC currently returns zero standings entries, and United Athletic has no conference mark. All three degrade quietly with today's code; none is worth blocking on.

## Icebox (deliberately not now)

- ~~Widgets~~ / Live Activities *(widgets de-iceboxed 2026-08-04 → E7; Live Activities stay iced — no push story without a backend)*
- ~~Notifications~~ *(local kickoff reminders de-iceboxed 2026-08-04 → E7; live-event push still needs a backend, still iced)*
- College basketball (the offseason answer: same schools, same follows)
- FCS/lower divisions *(scoped 2026-09-01 as E8 — the epic exists so the shape is known; still not scheduled, and its first item is the go/no-go)*
- News content (may be never; scores-first is the identity)
- iPad/Mac/visionOS layouts

## Open questions

1. ~~**App name.**~~ *Resolved 2026-07-21: **StatSide** (Andy's pick). Wordmark + home-screen display name updated.*
2. ~~**Live accent color.**~~ *Resolved 2026-07-21: red stays, decided from side-by-side renders of the real live row. At 6 pt a monochrome dot reads as a stray fleck, and Reduce Motion users lose the pulse — color is the remaining signal.*
3. ~~**Top 25 section scope.**~~ *Resolved 2026-07-21: any ranked participant, confirmed against 2025 data — any-ranked runs 15–21 games/week (~one thumb-scroll), ranked-vs-ranked only 3–6 and breaks the completeness promise.*
4. ~~**FCS games in `groups=80`.**~~ *Resolved 2026-07-21: rows render cleanly (ESPN ships logos/records; `curatedRank` 99 maps to no badge). Found + fixed: FCS visitors' unknown conference ids were double-bucketing 48 Week-1 games into "Other" — Other is now strictly the both-sides-unknown backstop.*
5. ~~**Following a conference** (not just teams) — worth it?~~ *Resolved 2026-08-09: yes — a follow pill on the new ConferencePage (E4). Scope: Scores Following section only in v1; widget and kickoff notifications stay team-driven (a conference has no single "next game", and ~9 reminders/week would evict team reminders under the nearest-24 cap). See decisions log.*
6. **Data licensing for public release.** ESPN's unofficial API is fine for a personal build but is undocumented, unlicensed, and revocable at will — the reason App Store release was iceboxed. Options: (a) CollegeFootballData.com — free key, CFB-only, but live-score latency/coverage needs verification against the time-to-score promise; (b) paid provider (SportsDataIO, Sportradar) — real license, real monthly cost, only sensible if the app ever charges; (c) ship on ESPN and accept the sudden-death risk — the DTO layer contains the swap blast radius to one file if it comes to that. Related: team logos off ESPN's CDN are university trademarks; same decision, same options. Andy decides before submission; E6 technical work proceeds regardless.
   **CFBD field notes (verified against their v2 OpenAPI spec + published tiers, 2026-07-21):** `/scoreboard` carries everything the game row needs — tv, period, clock, possession, situation, lastPlay, linescores, win probability — plus `/rankings`, `/calendar` (weeks), `/drives`, `/games/teams` for detail; records/ranks/logos aren't inline on scoreboard rows and would join from `/teams/fbs` + `/records` + `/rankings` (all cacheable weekly). Two catches: **(1) live scoreboard requires Tier 1 ($1/mo Patreon), free tier is historical-only and capped at 1,000 calls/month; (2) all tiers are per-key monthly caps** — one Saturday of 30s polling is ~1,400 calls, so a keyless client-side app doesn't work: the key would ship in the binary (extractable) and every install shares one quota (100 users ≈ 1M calls/mo, above the largest $30/500k tier). A public CFBD app therefore needs a thin caching proxy (e.g. Cloudflare Worker free tier: poll CFBD once per 30s while games are live ≈ 8–10k calls/mo → Tier 2 $5/mo, serve all users from cache) — which amends the "no backend" constraint to "no *stateful* backend." Personal-use TestFlight could ship CFBD keyed at Tier 2 without the proxy. Logos: CFBD's `/teams` logo URLs point at ESPN's CDN anyway, so the trademark posture is unchanged by switching.
