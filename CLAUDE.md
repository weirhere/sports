# CLAUDE.md — sports

Context file for AI-assisted development sessions on this project. Read this before writing any code.

## What this is

A football scores app for iOS — college football and the NFL — inspired by FotMob's information architecture. Two leagues, done fast and beautifully, in black and white. College football came first and still leads; the NFL joined at 2.0 (2026-09-05), and the league axis it introduced is what a third sport would arrive through.

The target user checks scores 20+ times a fall weekend and is tired of ad-stuffed everything-apps where their sport is one tab among 30. Speed and focus are the product.

**On "one sport, done fast":** that was the 1.x charter, and the discipline behind it survives — the app still does a small number of things completely rather than many things partly. What changed is that a football fan's weekend is Saturday *and* Sunday, and the two leagues share teams' worth of structure (the same ESPN shapes, the same follows, the same widget). What they do not share is a calendar, which is why the week strip retired for a day strip.

**Monorepo note:** this repo also carries `web/` — a Next.js prototype web app (`college-football-hub`), merged in with full history from a standalone web repo (confusingly also named `weirhere/sports` at the time, since deleted) on 2026-09-01 via `git subtree`. It has its own README and PRD under `web/` and none of the iOS conventions below apply to it. Everything else in this file is about the iOS app. This repo itself is **`weirhere/sports`** — renamed from `weirhere/sports-ios` on 2026-09-01; GitHub redirects the old URLs.

## Product principles

1. **The Saturday sort order is the product.** The landing page answers "what's the state of college football right now" in one thumb, one scroll. Everything else hangs off that.
2. **The day is the unit of time on Scores; the week still is everywhere else.** Superseded 2026-09-05 for the Scores screen only, when both leagues landed on one page — see the decisions log. Team pages, conference pages, and the polls still think in weeks, because within one league fans do (Wk 0–14, championship week, bowls, CFP).
3. **Sections are complete, never deduplicated.** A game can (and should) appear in Following, Top 25, and its conference section simultaneously. Each section keeps its promise of completeness.
4. **Monochrome chrome, color only where it earns it.** See design system below.
5. **Fast beats complete.** ESPN will always have more data. We win on time-to-score. No interstitials, no ads, no splash screens doing work.

## Design system

- **Palette:** Black, white, and grays only for all UI chrome, text, dividers, and backgrounds. Support light (white bg) and dark (black bg) via semantic colors from day one.
- **The color budget:** exactly three exceptions to monochrome:
  1. Logos — team and conference — render in full color (grayscale logos would make Michigan and Iowa look like the same team).
  2. The live indicator (a small pulsing dot + live score emphasis) may use a single red accent.
  3. Rankings movement indicators: green up, red down (the same red as the live accent — the app carries exactly one red). Arrows carry the meaning too; color is never the only signal.
- **Live state spends the visual budget:** heavier type weight on live scores, pulsing dot, possession indicator. Pre-game and final rows stay quiet.
- **Typography:** system font (SF Pro). Weight and size create hierarchy, not color. Scores use monospaced digits (`.monospacedDigit()`) so they don't jitter as clocks tick.
- **Density target:** FotMob-level. A game row is one compact line: logo, team, record, score/time, network. No cards-with-shadows padding inflation.

## Technical constraints

- **Language/UI:** Swift + SwiftUI only. No UIKit unless a specific need forces it. No third-party dependencies without an explicit decision (goal: zero for v1).
- **SDK/target:** Built with Xcode 26.3 / iOS 26 SDK. **Deployment target is iOS 18.0.** Do not use iOS 26-only APIs without `#available` guards. Prefer APIs available in iOS 18.
- **Backend:** none. The app talks directly to ESPN's unofficial API. No accounts, no server, no analytics in v1.
- **Persistence:** UserDefaults for followed teams and UI state (accordion expansion). No database in v1.
- **Concurrency:** async/await throughout. Note the project sets `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` (Xcode 26 default) — types are MainActor-isolated unless marked otherwise; put network work in `nonisolated` or actor-isolated types deliberately.

## Project facts (verified from project.pbxproj)

- `objectVersion = 77` with **PBXFileSystemSynchronizedRootGroup**: any file added under `sports/` on disk is automatically part of the app target. No pbxproj surgery needed to add files.
- Deployment target currently `26.2` in all 6 build configs — must be changed to `18.0` (see BACKLOG E0).
- `SUPPORTED_PLATFORMS` includes macOS, visionOS; `TARGETED_DEVICE_FAMILY = 1,2,7`. iPhone is the only design target for v1; don't spend effort on other idioms.
- Bundle id: `com.andyryanweir.sports`.

## Data source: ESPN unofficial API

Base: `https://site.api.espn.com/apis/site/v2/sports/football/college-football`

| Purpose | Endpoint |
|---|---|
| Scoreboard | `/scoreboard?groups=80&limit=300&week={n}&seasontype={2\|3}` |
| Rankings | `/rankings` |
| Game detail | `/summary?event={eventId}` |
| FBS teams by conference | `https://site.api.espn.com/apis/v2/sports/football/college-football/standings?group=80` (the `/teams` endpoint has no conference data — see ARCHITECTURE.md) |
| Team schedule | `/teams/{id}/schedule` (score is an object here, not a string) |

Response shapes were verified live on 2026-07-21 — see ARCHITECTURE.md § API reference for the field-level breakdown.

**Rules for working with this API:**
- It is undocumented and can change without notice. Every response field is optional in our decoders. No force-unwraps, no `try!` on decode paths. A missing field degrades the row, never crashes the app.
- Isolate ESPN's shapes in DTO structs; map them to our own domain models at the client boundary. If we ever swap to CollegeFootballData.com, the blast radius is one file.
- Poll the scoreboard no faster than every 30s, and only while the app is foregrounded and games are live. Be a polite guest.
- Not for commercial use long-term. Fine for a personal v1; needs a licensing answer before any public/App Store ambition beyond personal use.

## Conventions

- File layout under `sports/`: `App/`, `Theme/`, `Stores/`, `Intents/`, `Features/{Scores,Rankings,GameDetail,Teams}/`. Shared code (models, networking, theme tokens) lives in top-level `StatSideShared/`, compiled into both the app and `StatSideWidgets/` (the widget extension). Entitlements live in `Config/`.
- State: `@Observable` classes (iOS 17+, fine for our 18.0 floor), owned by views via `@State`, passed via `@Environment`.
- One view per file. Views over ~100 lines get decomposed.
- Names say what things are: `GameRow`, `WeekStrip`, `ConferenceAccordion`, `LiveDot`. No `Manager`, no `Helper`, no `Utils`.

## Running the UI tests

`sportsUITests` drives the real app against the live ESPN API — no fixtures, with one deliberate exception: `LiveDetailHoldUITests` runs the scripted `FixtureScoresClient` (`-data.provider fixture`, poll compressed via the DEBUG-only `poll.interval` default) because live-game churn can't be scheduled for a test run. Six environment rules, all learned the hard way:

- **XCUITest can ghost-activate controls on iOS 26.5.** A hold that queries the accessibility tree every second (an `exists` loop) occasionally fires a navigation control with no synthesized touch — a back button (pops the held screen), a NavigationLink (pushes a page nobody tapped), an accordion toggle. Verified 2026-08-31 with screen recordings and the one-synthesized-event xcresult; hand-driven holds and query-free holds never fire. This is what the 2026-08-29 "detail pops itself back to Scores" field report was. Keep long holds query-sparse, treat a spontaneous navigation during a querying hold as this artifact until the `scoresnav` path-depth log says otherwise, and see `LiveDetailHoldUITests`' `GHOST_REPRO=1` diagnostics to demonstrate it.

- **Uninstall the app before `ReminderOfferUITests`** (`xcrun simctl uninstall <udid> com.andyryanweir.sports`). Fresh-user state for `notifications.enabled` must come from a fresh install, not an argument-domain override: overrides beat the app's own writes, and `refreshAuthorization()` re-reads the key on scene-active, so the override flips the bell back off right after the grant. The uninstall also resets the TCC notification permission so the springboard prompt path actually runs.
- **Never `tap()` an alert-panel button** — the app's SwiftUI alerts and springboard's permission prompts alike. Under Xcode 26.6+/iOS 26.5 the synthesized tap resolves an activation point that lands somewhere else on screen (one stray toggled the reminder bell mid-test). Use `tapUntilDismissed(_:dismissing:via:)` in `UITestSupport.swift`: app-rooted coordinate taps at the button's settled frame, verified by the panel actually going away.

- **Pin `-ui.scoreFilter none` in any suite that queries game rows.** The Scores slate filter persists across launches by design (2026-08-29), so a suite inherits whatever the last run — or whoever last used the simulator — left selected, and every row query then searches a filtered slate. The failure looks like the app lost its games. Safe as an argument-domain override, unlike the `notifications.enabled` case above, because `UIStateStore` reads the key once in `init`; `"none"` parses to no filter. `FCSOptInUITests` selects a filter as its whole point, which is what surfaced this: it left every other fixture-backed suite failing until all three pinned it.

- **Always pass `-parallel-testing-enabled NO`.** Parallel runs clone the simulator, and the clones fail wholesale with `Invalid device state` / `Mach error -308 — server died` before a single assertion runs. The failure looks nothing like a test failure, so it's easy to misread as a broken build.
- **Check the simulator's Dynamic Type size before believing a failure.** At `accessibility-*` content sizes the header chips and the Teams search field no longer fit, so queries for them find nothing and previously-passing tests fail in unrelated-looking places. `ScreenshotTests` documents setting appearance and text size for accessibility passes; that state persists on the device afterward. Reset with `xcrun simctl ui <udid> content_size large` — `large` is the iOS default, so it's what both the tests and the App Store screenshots assume. Pass the **UDID, not the device name**: `iPhone 17 Pro` exists on several runtimes and xcodebuild's pick between them isn't stable.

Because the data is live, assertions must not encode calendar facts. Don't wait on a specific poll (the AP Top 25 doesn't exist until mid-August, and `RankingsScreen` hides the picker entirely when only one poll came back) or on a specific week's games. Shared helpers for the recurring traps — retrying a tab tap that a navigation transition swallowed, scrolling the week strip without oscillating — live in `sportsUITests/UITestSupport.swift`.

## Decisions log

| Date | Decision | Why |
|---|---|---|
| 2026-07-21 | Data: ESPN unofficial API for v1 | Free, no key, live scores. Risk accepted + contained via DTO layer |
| 2026-07-21 | Scope: Scores + Rankings + Game detail + Team following in v1 | Andy's call; scores screen leads |
| 2026-07-21 | Week-based navigation, not date-based | CFB's native clock; kills the empty-Tuesday problem |
| 2026-07-21 | Section order: Following → Top 25 → conferences (accordions) | Fixes ESPN's "where are my teams" failure |
| 2026-07-21 | Games duplicate across sections | Section completeness beats dedupe cleverness |
| 2026-07-21 | Conference chips jump-scroll, they don't filter | Filtering creates a mystery state |
| 2026-07-21 | One "Live" toggle is the only filter | The 3:30-Saturday use case |
| 2026-07-21 | Accordion state persists; Following + Top 25 open by default | FotMob's memory pattern |
| 2026-07-21 | Week rolls over Monday morning; Sunday still shows the completed week | Sunday is for catching up + new poll drops in place |
| 2026-07-21 | Monochrome + logos-in-color + single red live accent | Distinctive, and live state pops harder |
| 2026-07-21 | iOS 18.0 deployment target, iOS 26 SDK | Andy's call |
| 2026-07-20 | Conference jump chips removed (supersedes the jump-scroll decision) | Accordions cover section navigation; the chip row was redundant. The Live toggle stays, now as its own `LiveFilterChip` |
| 2026-07-21 | Rankings movement gets red/green (third color-budget exception) | Andy's call; movement direction reads instantly. Down reuses the live red; arrows keep it color-blind safe |
| 2026-07-20 | Conference logos in section headers render grayscale (inverted in dark mode) | Headers are chrome, and the color budget's logo exception is team-specific (disambiguation); conference headers already carry the name. ESPN has no dark-variant conference logos, so dark mode inverts the grayscale mark |
| 2026-07-21 | Conference logos render full color (supersedes the grayscale decision) | Andy's call after seeing it in practice; the color budget's logo exception now covers all logos, not just teams. Also drops the dark-mode invert hack |
| 2026-07-21 | App name: **StatSide** | Andy's call, closing open question #1. Wordmark + `CFBundleDisplayName` updated |
| 2026-07-21 | Live accent stays red (closes open question #2) | Decided from side-by-side renders; a 6 pt monochrome dot doesn't register at scroll speed, and Reduce Motion removes the pulse fallback |
| 2026-07-21 | Top 25 = any ranked participant (closes open question #3) | 2025 data: any-ranked is 15–21 games/wk, ranked-vs-ranked 3–6 — too thin for a section named Top 25 |
| 2026-07-21 | "Other" section = both-sides-unknown only | FCS visitors were double-bucketing ~48 Week-1 games into Other; an FCS visitor now stays in its host's conference section |
| 2026-07-21 | Dark mode backs conference marks with a light disc (`logoBacking`) | Navy marks (Big Ten, ACC) vanish on black; a backing disc is chrome, not color, so the budget holds |
| 2026-08-04 | Widgets + local notifications de-iceboxed (epic E7) | Apple rejected 1.0 (5) under 4.2.2 Minimum Functionality; they were the roadmap's "killer feature" anyway. Live Activities stay iced (no push story) |
| 2026-08-04 | pbxproj-edit rule amended: target/build-phase/build-setting edits allowed when adding targets, verified by lint + build; file references still never touched by hand | The widget extension is a genuinely new target; synchronized groups still handle all file membership |
| 2026-08-04 | Shared code lives in `StatSideShared/` — a second synchronized root group compiled into both app and widget targets | The alternative (exception-set deny-list on `sports/`) silently adds every future app file to the widget target |
| 2026-08-04 | App Group `group.com.andyryanweir.sports`; `FollowingStore` reads the shared suite, migrated once from standard defaults (standard copy kept) | Widget needs the follow set; migration flag lives in the suite |
| 2026-08-04 | Deep links `statside://game/{id}` / `statside://team/{id}` / `statside://teams`, scheme deliberately unregistered (no `CFBundleURLTypes`) | widgetURL and notification taps deliver to `onOpenURL` without registration; registering would force a partial Info.plist for external openers nobody needs yet |
| 2026-08-04 | Notifications: one local reminder 30 min before kickoff, app-wide bell toggle on TeamPage, permission asked contextually (bell tap or post-first-follow alert), never at launch | One reminder beats two under the 64-pending cap; per-team granularity is icebox material |
| 2026-08-04 | Haptics budget: exactly three (follow toggle, refresh success, live score change in game detail only) | Mirrors the color budget's discipline; GameRow haptics on a 60-game Saturday would machine-gun the Taptic engine |
| 2026-08-04 | UIKit import exception: `UIApplication.openNotificationSettingsURLString` for the bell's denied state | No SwiftUI equivalent exists |
| 2026-08-04 | Widget provider fetches ESPN directly (no app-written snapshot as primary) | The widget must be live at 3:30 Saturday without the app opening; WidgetKit's ~40–70/day reload budget is itself the politeness throttle (~48 requests on a full Saturday). Adds an unattended request surface to the unofficial-API risk — volume negligible |
| 2026-08-09 | Scores rows spend exactly as much date as the kick needs: "Today 3:30 PM" / "Tomorrow 3:30 PM" inside 48 hours, the bare weekday out to a week, weekday + date ("Sat, 9/5 3:30 PM") past that | On the Saturday itself — the app's whole use case — "Sat" is dead weight; but a weekday alone means *the next one*, so it lies about a game three weeks out. Scores-screen rows only; share text, widget, and schedule strings stay absolute because they outlive the moment they're generated |
| 2026-08-09 | A row under a day divider never repeats the date; `SectionAccordion` passes its existing `spansMultipleDays` down as `GameRow.dayIsLabeled`. VoiceOver always speaks the full date | Dividers and rows were both saying "Aug 29" in conference sections. The divider is the section's date mechanism, so the row defers to it — but only visually, since a VoiceOver swipe can land on a row without passing the divider |
| 2026-08-09 | Pinch on Scores collapses (in) / expands (out) every rendered section — FotMob's gesture, the app's first gesture. Fires once per pinch in `onChanged` at 0.8/1.25 magnification, persists exactly like per-header taps, day sections included, Scores only | Batch accelerator for the dozen-tap Saturday problem. No haptic (the budget of three holds; the animation is the feedback). Gesture-only: every header keeps its focusable toggle button, so nothing is pinch-gated for VoiceOver or motor-impaired users |
| 2026-08-09 | Team share = invitation framing ("Keep up with…" + record + next game) through `ShareSignOff` — the last share site to adopt it. Body never names the app; the sign-off is the only branding. No `statside://` link in shares | "Following X on StatSide" carried no link and no substance. Custom schemes aren't tappable in iMessage and there's no domain for universal links, so the store link stays the one tappable link; the unregistered-scheme decision holds |
| 2026-08-09 | Game shares attach a rendered matchup-card PNG (`GameShareCard: Transferable`, lazy async export); share text + store link move to `ShareLink(message:)` | The generic App Store card was the only visual in a shared thread. The card renders always-light at fixed type sizes (theme Font tokens bake against the process content size, so they can't be pinned via the environment) from `GameHeaderState`, the extracted detail-header derivations. Repointing the iMessage *link preview* itself would need per-game web pages — a backend — so it stays the App Store card, beneath the real game image |
| 2026-08-09 | Rows inside a day-grouped accordion drop the date entirely (`GameRow.timeOnly`) — kick time + network only. VoiceOver still speaks the full date | The accordion header already says "Saturday, Aug 29"; even the weekday the divider rule keeps is dead weight when the whole section is one day |
| 2026-08-09 | App-wide search is a native search tab (`Tab(role: .search)`) — the FotMob-style circular magnifier beside the floating tab bar on iOS 26, a plain fourth tab on iOS 18; corpus = shared team directory + FBS conferences + the selected week's already-loaded games (no new fetch surface); conference results emit a `Router` intent (`pendingConferenceId`), never a hardcoded destination | Andy's call after seeing per-header magnifiers (superseded same day): FotMob's bottom placement beats three header buttons, and the OS renders it. Time-to-answer beats corpus breadth, and the intent indirection keeps search decoupled from the in-flight conference-page worktree. Enabler: `ScoreboardStore` + new `TeamDirectoryStore` hoisted to RootView's environment — single fetch, polling lifecycle owned by the scene, not a tab |
| 2026-08-09 | Conference standings via a new `conferenceStandings()` protocol method; `fbsConferences()` and `ConferenceTeams` untouched | Same endpoint, second mapper: browse keeps its alphabetical contract, ConferencePage keeps ESPN's standings order (not derivable from records — tiebreakers). The standings mapper keeps empty conferences so the page can say "Standings TBA" offseason |
| 2026-08-09 | Conference follows are a second set on `FollowingStore` (App Group suite, new key); v1 scope is the Scores Following section only — widget and kickoff notifications stay team-driven (closes open question #5) | One store owns "do I follow this game"; a conference has no single "next game" for the widget, and ~9 reminders/week would evict team reminders under the nearest-24 cap. Conference follows count toward `followsAnyone`; an FCS visitor's game joins Following via its FBS host's conference |
| 2026-08-09 | Conference accordion headers gain a trailing standings button (`ConferenceStandingsLink`); the header surface stays the expand/collapse toggle; context menu + VoiceOver custom action mirror the GameRow precedent | Two side-by-side buttons beat gesture cleverness — the toggle's whole-width promise is never diluted, and the small trailing target gets redundant accessible paths |
| 2026-08-09 | Scores and Teams stacks switched typed paths (`[Game]`/`[Team]`) to `NavigationPath` | A homogeneous typed path can't push a second destination type; `NavigationPath` keeps every existing `NavigationLink(value:)` and the deep-link pending-id resolution intact. Rankings keeps its view-builder links but registers the same value destinations. (Main reached the same conclusion independently for game-detail team links — the merge kept `NavigationPath`) |
| 2026-08-10 | Rankings is the tables hub: CONFERENCES card (one row per conference, leader teaser, follow star, followed pinned first) below the poll — no 4th tab | Andy's call after weighing a Conferences tab: a tab would be a menu, not a destination, and Rankings already answers "who's good" — now at both levels. The teaser hides preseason (a 0-0 "leader" is last season's carry-over); a standings fetch miss hides the card, never errors over the poll |
| 2026-08-10 | Rankings root is a FotMob-Leagues-style row list: Top 25 row (→ `PollScreen`) above the conference rows (supersedes the poll-stays-inline-at-root first cut, same day) | Andy's call from the FotMob reference: 25 inline rank rows buried the conference list below the fold where nobody would discover it. The poll costs one obvious tap now; the whole hub is visible in one screen |
| 2026-08-10 | Search conference results land on ConferencePage — the Teams resolver pushes the standings page instead of expanding/scrolling the browse list (takes over the seam the search decision left open) | A conference's page beats its roster mid-list as a search destination. Resolution needs no loaded data (the page fetches its own standings), so the browse-list scroll machinery (`scrolledSection`, `scrollPosition`, the 450ms layout wait) came out entirely |
| 2026-08-16 | Team logos switch to ESPN's `500-dark` variants in dark mode, app + widget, via URL derivation (`URL.darkTeamLogoVariant`); missing dark asset → light fallback, with the 404 negative-cached in `LogoCache` | Black artwork (Ohio State's lettering) vanishes on the black background. The scoreboard payload ships no `logos[]` array, so the dark URL can't be decoded — derivation covers every surface from one helper. Share card stays always-light; conference marks derive nothing and keep `logoBacking` (no verified dark conference assets) |
| 2026-08-17 | Horizontal swipe on the Scores content pages to the adjacent week (left = next, right = previous), with a directional push slide; week-strip chip taps share the direction rule; season ends are a quiet no-op | Andy's call; the strip is a top-of-screen reach on a couch Saturday, and swiping the content is the FotMob-native way to walk weeks. Gesture-only accelerator like the pinch — chips keep every week tappable — and no haptic, so both budgets hold |
| 2026-08-17 | Unannounced kickoffs render "TBD", never the placeholder clock: ESPN's `timeValid: false` (date parked at midnight ET) maps to `Game.timeTBD`, honored by every time-rendering surface — schedule rows, scores rows, widget, shares, share card, Siri line — while the day part stays (it's real). Kickoff reminders skip TBD games | Team pages were showing "12:00 AM" for every un-timed future game; a reminder would have fired at 11:30 PM for nothing. Day-only degradation beats dropping the row's date, and the kickoff-encoding request id already handles TBD-becomes-real |
| 2026-08-17 | Team pages get a season selector: a `SeasonMenuChip` (extracted from the Scores header — one capsule, two call sites) trailing the SCHEDULE header, 2014 CFP-era floor. An explicit year fetches exactly that season — the silent year−1 fallback stays nil-year-only; `TeamSchedule.year` comes from ESPN's `requestedSeason`; the mapper trusts `recordSummary`/`standingSummary` only when `season.year == requestedSeason.year` (they always track ESPN's *current* season), so past seasons show a record derived from final results and no standing line; shares stay current-season; fetched seasons cache per page visit | Andy's ask. The Scores chip set the pattern and the floor; a user who picked 2019 must never silently get 2018. "1st in SEC" isn't derivable from games (tiebreakers), so honesty beats completeness for past standings — and the offseason page now shows last season's real record instead of ESPN's 0-0 |
| 2026-08-23 | Medium family gets compact metrics: masthead scales to 16pt star + new `sectionHeaderProminentCompact` (14 heavy), header gap 12 → 8, logos 20 → 16; card padding stays 8 in both families | The masthead + two 60pt cards overflow medium's ~138pt content box, and WidgetKit was crushing the padding. Consistent padding beats consistent type — scale the chrome, never squeeze the breathing room |
| 2026-08-23 | Widget header grows into a masthead: 24pt star, "Following" at new `sectionHeaderProminent` (16 heavy, −2% tracking), header-to-list gap tightens 16 → 12 | Andy's Figma remix, third pass: the widget has no nav chrome, so its header carries the identity weight a screen title would |
| 2026-08-23 | Widget status column gains a second line: the TV network under the day/time (`metaMedium`, right-aligned), pre-game and live only — supersedes the widget's no-network rule from 2026-08-16. Snapshot carries it optionally so old blobs still decode | Andy's Figma remix, second pass: the card rows have the vertical room the old one-line rows lacked, and "where do I watch" is the pre-game row's second question. Finals stay quiet — nothing left to tune into |
| 2026-08-23 | Widget rows become bgHeader cards (8pt padding, 8pt continuous radius, 2pt apart, stretching to split leftover height), dividers dropped; abbreviations move to `chip`/new `chipEmphasis` (14 medium/semibold), records + pre-game times to new `metaMedium`; large capacity drops 5 → 4 (provider limit follows), mock's 16pt header gap and 12pt footer gap adopted | Andy's Figma remix (StatSide file, ↳ Widget page): cards read as one game each at a glance better than hairlines, and five card rows were too cramped — Andy's call after seeing it installed. Stretch-to-fill is the mock's flex rows: no dead gap above the footer |
| 2026-08-23 | `textPrimary` light mode softens to 0.10 (#1A1A1A), off true black; dark mode stays pure white. Mirrored as the `text/primary` Figma variable (Theme collection, StatSide file) | Andy's call: true black on white causes eye strain. Contrast stays ~17:1 on bgPrimary, ~15:1 on bgRecessed. Backgrounds keep true black/white — the budget's mono ramp is otherwise unchanged |
| 2026-08-16 | Widget families become medium + large (small removed; lock-screen accessory stays), both rendering one FotMob-style stacked list: ★ Following header, 8pt-padded rows, medium 2 games / large 5 + updated/as-of footer, provider limit 5 for all families. Pre-game rows drop scores entirely (no 0–0, no dashes) and the status is time-only — weekday + time, date joining at 7+ days out, no network | Andy's call from FotMob side-by-sides: the 3-row medium was too tight, and FotMob's medium/large split fits the "list of my games" promise better than a single-game small. A 0–0 pre-game score is noise pretending to be signal, and the network doesn't earn its width in a widget row. Placed small widgets go blank — accepted |
| 2026-08-25 | TeamPage gets a team-color hero header — the color budget's **fourth exception**: nav bar + header paint in ESPN's `team.color` (schedule payload, newly decoded), white identity ink and a white logo disc (black ink on light colors via a luminance guard), bell + season chip + follow pill move into the hero, and a Games / Standings tab pair lands (Standings = the conference table with the team's own row anchored). Monochrome fallback when the payload carries no color; Overview/News tabs not built (no data source). `TeamConferenceLink` retired — the hero's conference · record line replaces it | Andy's call reviewing the P1 Figma variation, second pass ("make sure we get all the updates made") — FotMob's hero reads instantly as the team's home; chrome stays mono everywhere else |
| 2026-08-25 | Schedule results render as green/red score chips (`rankUp`/`rankDown` tokens, the W/L letter dropped); row logos grow to 32 pt; card headers go table-style (`CardHeader`) | Andy's Figma edit, same review. Mine-first score order keeps a non-color signal and VoiceOver still speaks won/lost; `bgPrimary` chip ink inverts with the scheme so dark mode's brighter green/red keep contrast |
| 2026-08-25 | Day dividers removed; the row's day line carries weekday + date from two days out ("Sat, 8/29" — Today/Tomorrow keep their window). Date grouping is the exception: rows stay time-only and the day accordion header pins to the top while its games scroll. TeamPage schedule rows push game detail (supersedes the 2026-08-09 divider/date-suppression pair) | Andy's calls: the divider was "just noise", and one consistent per-row pattern beats two date mechanisms; in date mode the sticky header does the date's job |
| 2026-08-25 | Conference accordion headers split into two surfaces: mark + name push the conference page, the rest of the row toggles (supersedes the whole-width-toggle promise and retires Scores' trailing standings icon — the name button inherits its accessibility label). Non-conference headers keep the whole row | Andy's call: "tapping the conference name should take you to the conference page, not expand the accordion" |
| 2026-08-25 | GameRow adopts the 2240 Figma matchup spec: 13-medium row scale (new `row*` tokens), rank after the name, records upcoming-only beside a hairline-divided 80pt status column, finals show score-only with sentence-case "Final". Week swipe becomes interactive (content tracks the finger, skeleton pane rides in, commit on distance or flick); chip taps keep the push | Andy's Figma + "swipe should start the moment the thumb moves". Live rows still spend weight; VoiceOver sentences unchanged |
| 2026-08-25 | ConferencePage moves onto the TeamPage hero template (monochrome — conferences ship no color); standings tables everywhere gain a place column + shared captions via `StandingsList`; hero tabs follow the Figma header component specs exactly (bold 14, 40pt gap, 14pt padding, 3pt bar, 50% inactive) | Andy's calls, same review session. One template for entity pages, one table language for standings |
| 2026-08-29 | Live rows show the network under the clock (Scores status column + accessibility-size layout), and the live game-detail header gains a quiet network line — the widget's pre+live rule adopted app-side. Finals stay quiet everywhere; the share card's status line is deliberately untouched | Josh Vertucci feedback: "I want to know what the scores are and quickly know where I can watch." A live game's detail page previously showed no network at all (the info card is pre-game-only) |
| 2026-08-29 | Scores grouping defaults to by-date (`.date`); conference grouping stays one chip tap away, stored choices untouched | Josh Vertucci: "'by date' takes priority — I want to know what's 'now' first." Users who never toggled the chip land on by-date after updating; the Saturday sort order lives on inside each day |
| 2026-08-29 | Scores gains a slate filter (supersedes "one Live toggle is the only filter"): a funnel chip opens an ESPN-style sheet — All / Top 25 / any conference — filtering every section in both groupings, composing with Live. Session-only, never persisted; the active chip fills and names the selection | Josh Vertucci's ESPN habit, and the by-date default left no conference view in date mode. The July "chips don't filter" objection was about unlabeled mystery state — a labeled chip plus reset-on-launch is the antidote, and the full slate always greets a fresh launch |
| 2026-08-29 | The filter sheet becomes the Scores view-options sheet: grouping (segmented By date / By conference) and the season picker move in from the header, which slims to wordmark + funnel + Live (same-day follow-up; supersedes the By-date-chip and season-chip header placements). The funnel chip's label carries filter + past season ("SEC · 2019"); grouping stays unlabeled — the section headers on screen already say which view is active | Andy: "the top is getting a little crowded" — five chips wrapped the wordmark and their own labels. One door for every view decision, and past seasons stay surfaced so a 2019 slate is never mistaken for this week |
| 2026-08-29 | The Live chip becomes a permanent header fixture, left of the funnel (supersedes the appears-only-while-live rule). Toggling it with nothing live lands on the narrowed-slate empty state ("No live games right now" / "No live SEC games right now") with one Show-all-games button that clears both live and the conference filter | Andy's call, same review: a stable home beats a chip that materializes mid-Saturday, and an explanatory empty state beats hiding the toggle |
| 2026-08-29 | Header controls adopt FotMob's grouped-capsule language: one bgElevated capsule holds the Live pill and the funnel segment (FotMob's calendar slot), chipEmphasis labels, ~52pt group, active segments fill textPrimary. Function, accessibility, and the color budget unchanged — the red dot stays Live's only color | Andy, from the FotMob reference: "I really like how fotmob does this in terms of tap target areas and larger buttons" |
| 2026-08-29 | Liquid Glass adopted for custom chrome, chrome only: the header control cluster, the selected week chip (ink-tinted glass), and `SeasonMenuChip` (both variants — the hero scrim becomes glass over team color) via `Theme/GlassSurface.swift` helpers, every site `#available(iOS 26)`-guarded with today's flat fills as the 18.0-floor fallback. Content cards, rows, and accordions stay ink — glass never stacks on glass, so the pills inside the glass group capsule keep their solid fills. System chrome (tab bar, search circle, sheets) was already glass from the iOS 26 SDK | Andy: "give the whole app a liquid glass effect" — scoped to the floating control layer per Apple's guidance; glassing scrolling content would shimmer everything and fight the FotMob density target |
| 2026-08-29 | ConferencePage gains a Games tab (TeamPage's tab pair, monochrome ink): the season's full conference slate as one card per week, postseason last, rows are `GameRow` pushing game detail; the season chip drives games and standings together, cached per year. Data: new `conferenceGames(conferenceId:year:)` on `ScoresProviding` — ESPN's scoreboard with `groups={confId}&dates={year}` returns the whole season in one call (verified live 2026-08-29, types 2+3 with per-event weeks); `Game.seasonType` newly mapped so a title game's restarted week number can't land it in "Week 1". Entry default: Games tab, except standings-anchored pushes (a team's standing line) which land on Standings; the page always opens at the top, hero in view (a scroll-to-current-week first cut was reverted same day — Andy: "it still should load at the top of the page") | Andy: "rankings conference pages has standings but no games." One fetch per (conference, season) keeps the polite-guest rule — no polling on this page |
| 2026-08-29 | The Scores filters persist across launches (supersedes the same-day session-only call): `liveOnly` and `scoreFilter` move to `UIStateStore` (App-standard defaults, stored-property pattern, `ui.liveOnly` / `ui.scoreFilter` with a token encoding) and `sections()` takes them as parameters like the grouping. Season selection stays session-scoped — it's time navigation, not a preference. UI tests pin both via the argument domain | Andy, after living with it: "you should be able to customize it to the way you want it and keep it even after a new session begins." The labeled chips and explanatory empty states are what made persistence safe — the July mystery-state objection no longer applies |
| 2026-08-29 | Turning the Live filter on navigates to where live games are: the current season's rollover week (`ScoreboardStore.currentWeekSlot`, retained from initial load; `selectCurrentWeek()` reapplies the rollover rule when coming back from a past season). In-season the jump slides like a week-chip tap; turning the filter off stays put | Andy: "the user will be navigated to the relevant week the live games are happening on" — filtering a future week to an empty screen answers the wrong question |
| 2026-08-29 | Team-page live games read live: the lead card retitles **Current game** while in progress and its body becomes the Scores `GameRow` itself (per-side scores, clock + network status column — "just reuse that same component"); TeamPage merges the scoreboard's fresher copy of a schedule game (`fresher(_:)`, optional-environment `ScoreboardStore`) into the card and schedule rows — the schedule payload carries no live scores or clock. Where scores still haven't landed, rows show "Live", never a dashed non-score; VoiceOver says "live now". Standings tabs (Team and Conference pages) drop the redundant "Standings" card header, and the team name fades into the nav bar's principal slot once the hero title scrolls under (`onScrollGeometryChange`, iOS 18) | Andy's live-Saturday review: NDSU's card showed "● –-–" mid-game, the tab + header double-said Standings, and a scrolled team page lost its identity |
| 2026-08-29 | The live accent turns green (supersedes "live accent stays red"): `liveAccent = rankUp`, one green app-wide; `rankDown` keeps its own red, still the app's only red. Every live surface follows from the token | Andy's call during the live-Saturday session |
| 2026-08-29 | Standings rows badge live fortunes: a team playing right now gets a dot — green winning, red losing, gray tied (`Game.liveResult(for:)`, `StandingsList.liveGames` from the environment scoreboard; current season only; VoiceOver speaks "playing now, winning"). The movement pair's colors put to live use | Andy's ask, watching live games against 0-0 tables |
| 2026-08-29 | Team and Conference pages: swipe between the Games and Standings tabs (threshold swipe, push slide, buttons keep everything reachable — the week swipe's sibling); the hero color extends behind a transparent nav bar at rest so the system's Liquid Glass buttons refract team color, FotMob-style (bar goes solid exactly when the inline title arrives; the color rides inside the scroll — a fixed background band bled green behind content and was reverted same day) | Andy's calls from the FotMob reference |
| 2026-08-29 | Conference-mode accordions clip their collapsing rows (`.clipped()` on the card VStack) | Collapsing rows painted over the next section's header until the animation settled |
| 2026-08-29 | The "FCS conference bug" investigation concluded no bug: ESPN's FBS standings confirm NDSU → Mountain West and Sacramento State → MAC as real 2026 realignment (their schedules corroborate), and truly-FCS ids (Tennessee State's 179) already degrade to no conference. What WAS broken: TeamPage's FCS veto keyed on "directory knows the team", and ESPN still ships the Sun Belt with zero standings entries — every Sun Belt page was silently losing its conference line and Standings tab. The veto is now disproof-shaped: an unknown team's claim falls only when the claimed conference has a roster that excludes it | Chased from Andy's "fix the FCS conference bug"; a membership-scrub hardening was designed and rejected when the Sun Belt hole surfaced — it would have stripped every Sun Belt game |
| 2026-08-29 | Hero-bar controls get contrast tints, still Liquid Glass: back and share become custom dark-tinted glass circles with white ink (system glass wrapper hidden on iOS 26 via `sharedBackgroundVisibility(.hidden)`; system back button hidden, swipe-back kept), and `SeasonMenuChip`'s onDark variant tints its glass dark. Untinted glass over team color washed out | Andy: "still very hard to make out... higher color contrast but still liquid glass" |
| 2026-08-24 | TeamPage header gains its own conference line (`TeamConferenceLink`, replacing the standing line): conference mark + name, the season's placement folded in ("Big Ten · 1st") when a standing exists (hidden at offseason 0-0, past seasons, CFBD) — one tap target → ConferencePage's full standings. The id resolves schedule-payload-first (`team.groups`, now decoded; deliberately outside the summaries trust rule) → pushed team → `TeamDirectoryStore` lookup | The only conference link on a team page was the standing string, which vanished offseason, on past seasons, and for teams pushed from Rankings/game detail (nil `conferenceId`). Andy's call after seeing a merged record-row version planned: the conference reads as its own line, and "Big Ten · 1st" avoids double-saying the conference the standing string would. CFBD parity came free via `Conference.id(forCFBDName:)` |
| 2026-08-31 | Hero color reaches the status-bar strip via `HeroTopBand` (supersedes the `-1000`pt in-content background inflation): a top-anchored band outside the ScrollView whose height tracks scroll offset — full top inset at rest, growing through the bounce, zero before the solid-toolbar handoff. ConferencePage carries the monochrome variant | Andy's bug report: the strip behind the glass back/share buttons showed plain gray — the inflated background lived inside the ScrollView, which clips its content. The fixed-band revert's objection (color bleeding behind scrolled content) can't recur: the band's bottom edge is the hero's top edge by construction |
| 2026-08-31 | Entity-page tab slides commit the push edge in a transaction BEFORE the tab switch (the week swipe's split), edge derived from tab ordinals; `.geometryGroup()` on the switched pane; `LogoCache` gains a synchronous `nonisolated` NSCache front read by `LogoImage` for first-frame paints; tab rows extracted to `HeroTabBar` | Setting edge + tab together made the outgoing pane resolve `.push` against the stale edge, so slide directions alternated wrongly; `.id(tab)` recreated every LogoImage whose actor-hop cache lookup landed a frame late — marks sat frozen as blank discs while cards moved |
| 2026-08-31 | The week swipe's incoming pane shows the adjacent week's real slate (supersedes the skeleton pane): `ScoreboardStore` caches each fetched week per season (`WeekSlot.id` key, cleared on season switch) and prefetches the ±1 neighbors once after every settle — never polled; the pane renders through `sections(from:)` with grouping/filters/expansion shared, interaction disabled, skeleton only before the first warm. `select(week:)` seeds from the cache before its fresh fetch | Andy's FotMob comparison: mid-swipe blankness answers nothing when the data is one request away. Seeding also killed the blank flash on swipe commits and chip taps. ~1 extra request per week change once warm keeps the polite-guest rule |
| 2026-08-31 | The hero's conference line drops the record — conference name only (amends the 2026-08-24 "Big Ten · 1st"/record line) | Andy's call after the Overview tab landed: the Record card now owns the numbers, and the hero was double-saying them one scroll above |
| 2026-08-31 | Dark mode adopts light mode's elevation logic (FotMob reference): nearer surfaces are lighter in **both** modes — dark ramp is now page/chrome 0.00 → card 0.11 → header tint/highlight 0.15 → elevated 0.17, replacing the numerically-inverted ramp (cards were pure black *below* an 0.08 page). New `bgCard` token (light 1.00 / dark 0.11) carries the card role; `bgPrimary` is chrome + inverted ink only (nav bars, screen roots, widget container, chip ink) and stays black in dark. Light values untouched everywhere; widget and StandingsList anchored-row highlight fixed by the retune alone | Andy's call with FotMob dark screenshots: "everything is elevation… things closer to the surface should be lighter." Cards had no dark elevation signal at all — the fixed black cardSurface shadow is invisible on black |
| 2026-08-31 | TeamPage gains an Overview tab — first and default: the Games tab's Next game / Current game card (kept on Games too) plus `TeamRecordCard` (conference + overall W-L from the standings payload, now fetched at page level). Past seasons: overall only, via the derived record; no conference → overall only; preseason hides the card on the 0-0 gate. Supersedes the "Overview/News not built (no data source)" note — standings are the source; News still has none | Andy's ask. The record split was already decoded (`vsconf`/`total`) and unused on the page; the tab row now always renders since Overview + Games always exist |
| 2026-08-31 | Entity-page headers paint `bgCard` — game, team, and conference pages plus their nav bars — and the **team-color hero retires** (supersedes the 2026-08-25 fourth exception; the color budget is back to three). `colorHex`/`Color(espnHex:)`/the luminance guard deleted; the tinted-glass bar circles and TeamPage's transparent-until-scrolled bar retire with the paint they existed for (system back button returns). Team logos render bare on the header (`500-dark` carries dark mode; the white disc dropped); conference marks keep the dark-only `logoBacking` disc — ESPN serves no `ncaa_conf/500-dark` variants (probed live, all 404). `HeroTabBar` drops the 3pt underline: opacity alone separates active from inactive | Andy's design review against FotMob's league page: "all headers should have the same color as the cards" |
| 2026-08-31 | Hero controls consolidate onto the toolbar row (FotMob's pattern): TeamPage puts bell + follow pill + share beside the system back button, ConferencePage its follow pill; the **season chip moves into the tab panes** — trailing above the first card on Games and Standings (Overview stays chipless). TeamPage's Standings tab gains past seasons via `conferenceStandings(year:)`, cached per year like ConferencePage; the Overview record card stays pinned to the current season, and live standings dots gate on it. Known pre-existing edge: divisional seasons (2019 AAC) nest entries under division children the mapper doesn't read → "Standings TBA" | Andy's calls, same review — five controls didn't fit one row, and the chip belongs with the content it scopes |
| 2026-08-31 | ConferencePage tabs reorder: Standings first and the entry default, Games second (amends the Games-default entry rule from the Games-tab decision; standings-anchored pushes land where they always did) | Andy, same review — a conference page's first question is the table, FotMob's Leagues order |
| 2026-08-31 | Game-detail scores go FotMob-centered: once a game has scores (live or final), one large "7 – 33" line sits between the logos (new `scoreHero`/`scoreHeroLive` 34pt tokens, `.fixedSize()` so the equal-thirds header can't wrap it), the status line demoted beneath it; the per-side score under each logo retires, with the loser's number keeping its muted ink inside the combined line. VoiceOver team links still speak "team + score" | Andy's FotMob reference: the final score is the page's headline, not a footnote under each logo |
| 2026-08-31 | Live clock breaks become a domain fact: `GameStatus.live` carries a `LivePhase` (`playing`/`halftime`/`endOfPeriod`) mapped once at the ESPN boundary from the status type name (`STATUS_HALFTIME`/`STATUS_END_PERIOD` arrive as `state: "in"` with the clock parked at 0:00), and every live surface renders through one shared `GameStatus.liveStatusText` — "Q3 5:24" / "Half" / "End Q1" — replacing five per-surface `[quarter, clock]` joins. VoiceOver speaks "halftime"/"end of 3rd quarter". CFBD infers halftime from a parked Q2 clock only | The E5 live-pass finding: halftime rendered "Q2 0:00" on all five surfaces because every renderer re-derived the line and ignored `detail`. A boundary mapping + single formatter keeps them from drifting again |
| 2026-08-31 | The 2026-08-29 self-popping live detail closes as an **XCUITest/iOS 26.5 ghost activation, not an app bug**: per-second accessibility querying during a hold occasionally fires a nav control with no touch (back button on the Aug-29 tree → the observed pop; a header team link on main → a TeamPage push). Ships instead: `FixtureScoresClient` (DEBUG-only scripted backend, `-data.provider fixture` + `poll.interval`), the `LiveDetailHoldUITests` regression suite, gated `GHOST_REPRO=1` diagnostics, and `scoresnav` path-depth logging in ScoresScreen | Reproduced on both trees with screen recordings; hand-driven and query-free holds never fire (0 in 8+ min vs 3/3 on the old tree under querying). The app can't defend against ghost taps of its own back button |
| 2026-08-31 | A pre-game summary never demotes a live scoreboard snapshot: `GameHeaderState.status` merges the two (kickoff doesn't un-happen; final/postponed still win), and every header surface, the share card, and the detail's poll gate render from the merge | Found via the fixture: one flaky pre summary for a live game froze the detail mid-drive — header demoted to pre AND polling cancelled for good, since a dead poll loop is the only thing that refetches the summary. `GameHeaderStateTests` |
| 2026-08-31 | The Leaders card goes FotMob two-sided: each category is a centered label with the two leaders anchored to their header sides — away left-aligned on the left, home right-aligned on the right — 40pt headshot discs on the outer edges wearing a 15pt team mark (the abbreviation column retired; alignment + badge carry team identity, VoiceOver speaks the team location). ESPN's `athlete.headshot` newly decoded; CFBD leaders stay photo-less (quiet disc); accessibility sizes keep stacked abbreviated rows. `LogoImage` gains a `.fill` content mode for cropped photos | Andy's FotMob reference ("if the player plays for the team on the left in the game header, show that player on the left"). Headshots ride the logo exception's rationale — content imagery in color, chrome stays mono |
| 2026-09-01 | The Scores sections pipeline memoizes in `ScoreboardStore` (keyed on full game content, bounded), sorts once up front instead of per-section, and `fetchSelectedWeek` equality-guards every store write | E5 performance pass, static half: ScoresScreen's body re-evaluates on every frame of the interactive week drag and asked for the full pipeline up to three times each, and a no-change 30s poll tick invalidated the whole scores tree (@Observable notifies on every set). Content keys make stale sections structurally impossible — a clock-only poll change still invalidates |
| 2026-09-01 | Overnight agent loop adopted: agent sessions work the backlog continuously — implement on the agent branch, PR, CI green, merge, reset, repeat — gated by new GitHub Actions CI (newest-Xcode build + `sportsTests` on `macos-latest`; UI tests stay local, they drive the live ESPN API). Nothing agent-authored merges on a red or absent check | Andy's call ("I wanna test running agents overnight"). Remote sessions have no Xcode, so CI is the loop's only "build it" — it had to exist before the first unattended merge |
| 2026-09-01 | The Sunday rollover rule tie-breaks by ESPN's current slot: among the week slots containing yesterday, ESPN's `(type, value)` wins, first-containing otherwise (supersedes plain first-containing) | E5 edge-case pass: Bowls (Dec 13 → Jan 28) and CFP (Dec 18 → Jan 28) overlap for the whole playoff, so first-containing pinned every playoff Sunday to Bowls. September is untouched — ESPN's flipped-forward week never contains yesterday, so the completed-week pin still fires |
| 2026-09-03 | `main` is protected: force-pushes and deletion blocked, a PR required (0 approvals — the author merges their own), and both CI checks required green. Admins are exempt, so Andy can still push straight to main; agent sessions can't. Both workflows drop their path filters — a required check that path-filters itself out of a PR never reports, which blocks the PR permanently instead of passing it | The repo went public, so standard runners are free and unlimited — the wasted half of every unfiltered run costs nothing. Turns the overnight loop's "nothing merges on a red or absent check" rule from convention into enforcement |
| 2026-09-05 | Game detail becomes tabbed — **Summary / Box score**, two tabs not three: Summary keeps every existing card in its existing order (Scoring included), and the tab row is hidden entirely when there's no player box score | Josh Vertucci: *"the one thing I'm obsessed with looking at is box scores… that's where I get a lot of context."* Splitting Scoring or Drives into a third tab would have moved a primary surface off the default tab to no one's benefit; hiding the row when `boxScore` is empty keeps pre-kick, CFBD, and player-stat-less games pixel-identical to before |
| 2026-09-05 | Box-score columns are carried from the payload per response, never named in code (`BoxScore.Category.columns` from ESPN's `labels[]`) | Verified live: a **live** `passing` group ships five columns and the **same group ships six once final** (QBR only lands at the end). Any hardcoded schema misaligns every row mid-game — the one thing a stats table must never do. Rows and totals whose count doesn't match the header are dropped rather than rendered |
| 2026-09-05 | Scoring rows say whose points those were three ways: the team's mark leads the row, the scoring side's number takes the weight in the running score, and the row gains its first VoiceOver label | Josh Vertucci couldn't tell who was scoring and was matching player names against the Leaders card to find out. Weight, not color — the budget stays at three. The emphasized number is what survives an unfamiliar FCS mark, and VoiceOver had the identical problem the sighted row did |
| 2026-09-05 | **The day replaces the week as the Scores screen's unit of time** (supersedes "week is the unit of time, not the day" and the never-a-date-scroller don't): a `DayStrip` of the season's days, swipe = ±1 day, a leading Today chip to jump home. Bounds come from `SeasonSpan` — a derived rule (August → the last league's rollover month), not a fetch | Andy's call. The week principle assumed one league. College football's Week 2 and the NFL's are different date ranges, and its single "Bowls" slot swallows four NFL playoff rounds whole — a week strip can only ever be honest about one league, and the app now shows both at once. A day is the only unit every league agrees on, and the one every future sport will |
| 2026-09-05 | **The league selector retires; leagues become accordions** on the day's slate — Following pinned first (cross-league), then one section per league, `GameSection.league` carrying the identity. The conference stack and the day sections both come off Scores; the breakdown by conference lives on Tables | Andy: "one accordion for NCAAF and one for NFL… a much more scalable solution." A segmented control shows one league at a time and grows a segment per sport; stacked accordions show them all and cost one row each. Sections stay complete — a followed game is in Following *and* its league |
| 2026-09-05 | Section assembly moves from `ScoreboardStore` up to `LeagueScoreboards`: the store is now one league's games held a day at a time, and the page's sections are assembled across leagues. `ScoresGrouping` and the grouping control retire with it | The sections span leagues now, so no single-league store can build them. Drops the sections memo with it — the pipeline is a filter and two buckets, not the three-way sort it replaced |
| 2026-09-05 | Scores fetches a **five-day `dates=` window** per league (`scoreboard(days:divisions:)`), centred on the shown day; the inner three days are recorded as loaded. One request covers the day and both swipe neighbours, so the prefetch and the week cache retire | Verified live 2026-09-05: `dates=20260904-20260908` works for both leagues and past seasons (a 2019 range returns 2019 games), but returns **no calendar** and pins `season.year` to the current season — so the strip's bounds are derived, not fetched. ESPN reads `dates=` on the Eastern clock, and the two-day margin absorbs the ET-to-local offset for every time zone; the outer days stay unrecorded so a half-slate can't pass for a whole one |
| 2026-09-05 | The app never opens on a dead screen: when the day it landed on is empty, `firstDayWithGames` probes forward in fortnights and moves there. Only ever on a day the *app* chose — launch and season switches; a day the user picked stays picked | An August Tuesday, and a past season selected from the funnel (which lands on the season's nominal August start, weeks before anyone plays). An app that slides out from under a deliberate tap is a worse bug than an empty Tuesday |
| 2026-09-05 | The slate filter narrows the league sections and **leaves Following alone**; Live composes with everything. A filter also hides the leagues it can't speak for outright rather than emptying them | Live is a state, not a scope. Narrowing "my games" to the SEC would silently empty the section for a Michigan fan — the mystery state the labeled chip exists to avoid — and "SEC" is not a question the NFL's slate can answer |
| 2026-09-05 | The Scores league scope retires app-wide with the selector: `uiState.league`, the cold-launch live-league auto-pick, and `followedGamesElsewhere` all go. Search's ranking tiebreak becomes `FollowingStore.preferredLeague` — the league you follow most, nil on a tie | Nothing has to be picked when both leagues are on the page. The tiebreak was the scope's only other consumer, and "the league you follow most" is a better signal than "the tab you last tapped" |
| 2026-09-05 | The tables hub drops its league selector for one accordion per league — College Football (Top 25 row, then its conferences) and NFL (AFC, NFC), each a Teams-browse card with persisted collapse state. Tables is now the one screen not scoped to the app-wide league; Scores keeps the selector, and following it no longer changes the league there. Following leads as before, now spanning both leagues in one card | Andy's call: a segmented control shows one league at a time and grows a segment per sport, while stacked accordions show them all and cost one row each — the scalable shape when a third league arrives. "Who's good" has an answer per league and they fit on one page, so scoping the hub was hiding half the answer behind a tap. The Top 25 row moves inside College Football (a poll row floating above two leagues belongs to neither), and Following's row ids are league-qualified — the SEC and the AFC are both group id 8 |
| 2026-09-05 | A divisional conference is one row in the tables hub — "Sun Belt", not "Sun Belt - East" and "Sun Belt - West" — via `[ConferenceStandings].foldingDivisions()`, which merges a parent's divisions, re-applies the mappers' tier sort (an unknown division id sorts last; its conference doesn't), and leaves divisions whose parent we can't name unfolded. A folded table is marked `spansDivisions` and reports no `leader`, so no row teases a division leader as the conference's | The hub names conferences; the standings still table each division, because that order encodes tiebreakers (2026-09-05's divisional decision is unchanged). Folding also restores the conference's own mark and follow id — the division rows carried the football-glyph fallback and would have followed half a conference |
| 2026-09-05 | The week-rollover machinery retires with the week strip: `WeekLogic.defaultSelection`, its private `lastGameDay`, and `League.completedWeekWeekdays` are deleted with their tests (supersedes the 2026-07-21 Monday-rollover rule, the NFL's Monday/Tuesday pin, and the 2026-09-01 Bowls/CFP tie-break). `WeekSlot` stays — `Scoreboard.weeks` is still decoded, and `CFBDClient` still files a day range against a season | The day strip left the rule with zero callers, and it can't come back unchanged: a week strip is only ever honest about one league at a time, so a future week surface would be per-league and re-derived. The expensive part — why several slots can contain yesterday during the playoff — is written down in the 2026-09-01 entry above, so deleting the code doesn't delete the reasoning |
| 2026-09-05 | A divisional conference's page keeps its divisions as separate tables — one card each, headed "East"/"West" (the conference's name stripped off ESPN's "Sun Belt - East"), each ranked from 1 — on ConferencePage and TeamPage's Standings tab alike; the hero count still sums them ("14 teams"). The top-two championship cut is suppressed whenever the tables are divisions, whatever `titleGameIsTopTwo` says for that season | Andy's call, closing the wart the hub fold surfaced: the merged table numbered teams 1 through 14 across two divisions ESPN never ranked against each other — the tiebreaker guesswork the standings contract forbids, printed as a place column. "Divisional" is read off the payload (`parentId`), not off our own table, so a conference that drops its divisions needs no code change; and in a divisional format the division winners meet, so a per-division top-two footnote would claim the wrong thing |
| 2026-09-05 | ConferencePage's Games tab gains a team filter — a menu chip leading the pane's control row, "All teams" or one member, narrowing the slate before the week grouping so filtered weeks vanish rather than empty. Roster is the season's standings (the slate carries every non-conference opponent too), slate teams as the fallback where ESPN ships none. Session-scoped like the season chip beside it, and held across season switches by a disproof guard: a pick the new season's conference doesn't have reads as "All teams" | Andy's ask. A conference's season is ~150 games; the one question the whole card stack can't answer at a glance is "when does my team play". Resetting on a season switch would punish flipping back and forth, but a stale name must never empty the pane — so the pick survives only while that season's conference can honor it |
| 2026-09-05 | The Top 25 is followable: a third follow set on `FollowingStore`, keyed by league (`following.pollLeagues`, tokens `"cfb"`), with a star on the hub row and a pill on the page. A followed poll leads the Tables Following section and still repeats inside its league's accordion. It stays out of `followsAnyone` and `preferredLeague` — those gate game-shaped surfaces, and a poll carries no games | Andy's ask. Keyed by league rather than a flag because the NFL has no poll today and a league that grows one must need no migration; the set is the conference set's shape, so both read the same way |
| 2026-09-05 | PollScreen moves onto the entity-page template (hero mark + title + ESPN's poll line, follow pill on the toolbar row, content on the recessed ground, inline title on scroll) with **no tab bar**, and the AP/Coaches/CFP segmented picker becomes a `PollMenuChip` in the season chip's slot — trailing, above the card it scopes | Andy's call: the poll page was the last screen not built like the others, and the three polls are the same 25 teams read by different voters — a filter, not two pages. A one-item tab row would be chrome saying nothing, so the page has none. ESPN's headline ("2026 AP Poll: Preseason") moves from card caption to hero subtitle, where the other pages put their subtitle |
| 2026-09-05 | The NFL gets a whole-league row on the Tables hub, above the AFC and the NFC: ESPN's league group (id 9, `Conference.leagueWideId(in:)`), all 32 teams ranked by win percentage, on the same ConferencePage the conferences use. Built by merging the conference tables the hub already fetched — `[ConferenceStandings].leagueTable(in:)` — so the row costs no request. Two things it needed fixed: conference follows now walk the group chain (`Conference.chain(for:)`), and the NFL's in-group standings column is captioned CONF | Andy: "we need to add another league in here for the whole NFL as well, not just the different conferences." `standings?level=1` was the obvious source and is the worse one: its current-season order is the same win-percentage ranking, but its past-season order is a stale seed interleave that puts 14-2 Baltimore ninth (probed live). Ordering it ourselves doesn't breach the never-sort-standings rule — that rule protects orders encoding tiebreakers, and the NFL ranks nothing across its conferences. The follow walk-up: ESPN's NFL scoreboard gives a team its *division* id, so a followed AFC, NFC or NFL matched no game at all. The caption: the mapper's `divisionRecord` fallback asked for a key the payload spells `divisionrecord`, so every NFL table has been showing the `vsconf` record under a DIV caption — the caption moved to match the number, since the 32-team table has a conference record to show and no division one worth a column |
| 2026-09-05 | The Teams tab becomes the follow list: one card per followed team, stacked, and the conference browse accordions retire with `TeamBrowseRow`. Joining a team moves into an **Add teams sheet** — search over the whole directory, with a curated popular shortlist standing in until the first keystroke replaces it with results; its rows toggle follows (onboarding and the sheet now share one whole-row `TeamFollowRow`). Reaching a team page for a team you don't follow is search's job; browsing by conference is the Tables hub's | Andy: "the teams page doesn't need to really surface all the teams grouped in accordions… just display each team that is a favorite as its own card." The tab was carrying ~250 directory rows to answer a question about a handful of teams. "Popular" is curated, not measured — the app has no analytics and ESPN publishes no popularity figure, so the shortlist is a hand-picked set of the biggest followings in each league, skipping any id the directory doesn't carry |
| 2026-09-05 | The entity pages' tab row and its filter chips are **sticky**: hero splits into an identity block that scrolls away and a pinned `LazyVStack` section header carrying the tab row (bgCard) over a bgRecessed control strip, on TeamPage and ConferencePage alike. The strip renders its gap whether or not that tab has a chip — a collapsed one merges a bgCard card into the bgCard tab row — and the tab strip's paint overhangs upward by the identity block's own bottom gap, covering the seam where a pinned header settles under the bar | Andy: "tabs and filters should be sticky as the user scrolls." A conference season is ~150 games and a team's is a full schedule; switching tab, team, or year shouldn't cost a scroll back to the top. Lazy only for the pinning — two children, and the section's content is the whole pane in one subtree, so the standings anchor scroll still finds its row |
| 2026-09-05 | The Scores header keeps Live and nothing else: the Top 25 chip and the season chip both leave (supersedes the Top-25-survives-the-funnel and season-rides-the-day-strip calls from the same day). The slate filter has no control on Scores any more — `ScoresScreen` passes none, so a filter stored by an earlier build can't narrow the day invisibly — and the strip runs the full width to its trailing edge | Andy: "remove the top 25 and season selector on this scores page." Live is a state the day itself is in; ranked-team and past-season browsing both have real homes one tab over on Tables, where a poll and a season chip scope a page that is about them |
| 2026-09-05 | The Top 25 becomes an entity page on the ConferencePage/TeamPage template: Standings and Games tabs, and the poll table adopts the standings tables' own column language (`#`/TEAM/OVR captions, the place gutter, 10pt rows) with a `MOV` column where a standings table has none. The poll chip (AP/Coaches/CFP) stays the Standings pane's control and scopes the Games tab too — its 25 teams are the slate's members, by the "any ranked participant" rule | Andy: "this page needs to mirror the ui of all the other league/conference pages in terms of the table and we need tabs for standings and games." The Top 25 *is* an entity whose members are 25 teams, so the same template answers it — and the poll table was the last table in the app speaking its own dialect |
| 2026-09-05 | Games tabs gain a shared control row on every page that has one — `SlateControlRow`: **Weeks** and **Date** as on/off toggles (two answers to one question, so turning one on turns the other off; both off is one chronological card) and **Team** as the dropdown, at rest labelled by the control's own name. Cards head "Week 1" or "Saturday, September 5" | Andy, in three passes: the filters first, then "All weeks should just read 'Weeks' and be a toggle... All teams should just be 'Team' and the dropdown is perfect", then "these filters need to go on all leagues/conferences pages (not just top 25)". Week and date are a *view* choice — every game stays in the list either way — where the team pick is the only narrowing one. The row is a plain HStack, not a scroller: a horizontal ScrollView clipped the chips' own shadows |
| 2026-09-05 | The season chip moves back to the toolbar row on every entity page — Top 25, ConferencePage, TeamPage — left of the follow pill, in a new `.bar` style (the follow pill's outlined capsule, since glass over an opaque bar reads as a smudge). Supersedes the 2026-08-31 move into the panes; TeamPage's Overview is the exception it always was and shows no chip, its record card being pinned to the current season | Andy: "season selector needs to be in the header not inline with the table on all conference/league pages." The season scopes every tab, so it belongs beside the page's identity rather than above one pane's cards — and the pane row is now the filters' |
| 2026-09-05 | Past-season polls come from ESPN's **core API** (`sports.core.api.espn.com`), the only surface with a season axis for rankings: the final AP and Coaches votes at `types/3/weeks/1`, the CFP's selection-day table at the last regular week (404 in the postseason). Its ranks name their team by `$ref` alone, so names and marks are resolved against a one-request `/teams` directory (760 teams, cached per client); a rank the directory can't name is dropped, and an empty directory fails the season rather than tabling dashes | The site API's `/rankings` is latest-only — it ignores `season`, `week`, `year` and `dates` alike (probed live 2026-09-05), so a season selector on the poll page had no other source. ~4 requests per past season keeps the polite-guest rule; the current season still takes the one shipped request |
| 2026-09-05 | A season's slate is fetched by **date window, never `dates={year}`**: `conferenceGames` now asks for the season's own span (Aug 1 → the rollover month's end), and the new division-wide `seasonGames(year:)` splits that span at November 1 into two `limit=900` requests. A bare year is the *calendar* year — it opened the 2026 slate with ten `season.year: 2025` bowl games and truncated before December (verified live 2026-09-05) | Found building the Top 25's Games tab, which needs the whole FBS season (~950 events) in one shot. The same bug was quietly seasoning every ConferencePage Games tab with last January's bowls |
| 2026-09-06 | **FCS moves inside College Football's accordion on the tables hub** — one card, 11 FBS conferences then 14 FCS ones, no second header and no separate collapse state (supersedes the FCS-as-its-own-collapsed-card shape E8 shipped). The two fetches stay separate and still fail independently; only the display folds, in one `foldingDivisions()` pass over the union, which is what sorts the divisions apart — the `.fcs` tier rung already sits below `.independent` | Andy: "FCS is separated from the rest of college football. We need to group FCS and FBS together all as college football." The hub's accordions are leagues, so a card beside College Football read as a third league — and the row a user is looking for is a conference either way. The opt-in rule that E8's collapsed card enforced lives in `ScoreboardStore.divisions` (following an FCS conference is what widens the fetch), not in whether the hub lists the row |
| 2026-09-06 | League accordions wear their league's mark — Scores and the Tables hub alike, in `ConferenceLogo`'s 18pt footprint (Following keeps its star; a card with no mark falls back to the football glyph). Supersedes the name-only header rule: college football's mark does exist, at `redesign/assets/img/icons/ESPN-icon-football-college.png`, which is what ESPN's own NCAAF scoreboard declares in `leagues[].logos[]`. Held statically on `League.logoURL` and deriving no dark twin — the backing disc is a header badge's dark-mode answer, and the NFL's light-inked `leagues/500-dark` shield would vanish into it | Andy: "add logos to the college football (NCAAF) and NFL accordions like we used to have for each conference." The 2026-09-05 probe searched the `leagues/500/` bucket, where only the NFL's is filed, and read the miss as "no mark exists" — the hole that forced name-only headers was a wrong lookup, not a missing asset |
| 2026-09-06 | NFL standings gain a **scope filter** — League / Conference / Division — as a menu chip in the Standings pane's control row, the team filter's twin one tab over. Which scopes a page offers is read off the league's hierarchy (`StandingsScope.scopes(for:)`), so the league page shows all three, a conference page the two below it, and every college-football page none. League and Conference are two arrangements of the response the page already has; Division is the one scope that costs a request — ESPN's shipped standings stop at the conferences, so it asks `standings?level=3` and caches it per year, only once a user picks it. The new `ESPNMapper.divisionStandings` walks the whole tree instead of `standingsGroups`' one-depth-or-the-other collapse, because a deeper response can carry both, and takes parentage from our own registry first (the payload's nesting only where the id is new) | Andy: "we need a filter on the standings page to sort through by the whole league … but also by conference (afc/nfc) as well as by division." One 32-row table answers "who's best in football" and nothing about a division race, which is the question the NFL's own season is built on. A scope is a *view* of one fetch rather than a different set of teams, which is why the hero still says 32 teams at every scope — and why the control is a dropdown, not a set of toggles: the three are one question. Fixes a hole on the way: an NFL division page (AFC East) had no table at all, because the shipped response never carries one |
| 2026-09-06 | **The preseason gets its own cards**, ahead of the regular season's, and the app's season now opens in July: `ConferenceSlate.groups(from:)` buckets `seasonType == 1` into its own namespace (`preseason-{week}`), and `League.seasonOpensIn` moves the NFL's `SeasonSpan` floor from August 1 to July 1 so the Hall of Fame Game is inside the window at all. Cards read "Hall of Fame Game", then "Preseason Week 1–3" — ESPN numbers the HOF weekend as preseason week 1 and the three preseason weekends as 2–4, so the label is the week number minus its opener; a league that numbers its preseason from 1 keeps its own numbers | Andy: "add a way to group preseason and hall of fame game separate from the regular season." The preseason's week numbers restart exactly like the postseason's, so the NFL's Hall of Fame Game and its opening Thursday were both filing under "Week 1" — the same bug the `seasonType == 3` split fixed for title games in 2026-08-29. The July floor is the honest span: a season opens when its first game can, and an August floor cut the front off the NFL's. Costs the day strip ~30 empty July days, which the launch's `firstDayWithGames` probe already walks past. **The 1 = Hall of Fame week mapping is reasoned from ESPN's calendar, not probed** — the labels shift by one if it's wrong, and nothing else does |
| 2026-09-06 | **College football breaks back down by conference on Scores; the NFL stays one accordion** (supersedes the one-accordion-per-league shape from 2026-09-05). CFB conference sections in tier order (P4 → G5 → Independents → FCS → Other), then the NFL's single section; every section but Following defaults open, and a conference header is two surfaces again — mark + name push its page (the 2026-08-25 split, back now that a header has somewhere to go), the rest toggles | Andy: "instead of two accordions on the scores page (college football nfl), let's actually switch back to the conference (big 10, sec) but nfl can remain as is." One "College Football" accordion is 60 rows on a Saturday with no way in, and conferences are how fans already carve one up. The NFL's 16 games in one section *is* the slate at a glance, and its divisions would be four rows a section |
| 2026-09-06 | **Following on Scores is your teams; a followed table gets hoisted instead.** A conference or poll follow no longer pours its slate into Following — it moves that table's section directly under Following (a CFB conference *moves* up the stack rather than being cloned; a poll or an NFL group, which the stack has no counterpart for, gains a section, its games still repeating in the NFL's). `FollowedTable` is the new shared identity over both follow sets, and `GameSection.table` is what makes the move a move | Andy: "if a user is following a table/league/conference, those teams/games shouldn't show up in the following list… the table should just show up higher up on the list." A Big Ten follow is ~8 games on a Saturday, which buries the three you actually care about. Sections stay complete: the game is still in the table's section and its conference's |
| 2026-09-06 | **Followed tables are drag-reorderable on the Tables hub**, and that order is the order they lead the Scores page in — one list, one order, both screens (`following.tableOrder`, `FollowingStore.orderedTables`). A new follow lands at the end; a set saved before dragging existed falls back to the hub's own tier order, so nothing needs migrating. Drag-and-drop rather than an edit mode (a `List` nested in the hub's ScrollView fights it for every gesture), with Move up / Move down VoiceOver actions since a drag is not an accessible affordance | Andy: "users should be able to drag and rearrange tables that they're following on the table page to reflect the order they want to see them under following on the scores page." The Following card was already both follow sets in one place; it just had no order of its own |
| 2026-09-06 | Day chips carry their month — "Sun, Sep 27" — and yesterday and tomorrow get their names beside today's | Andy's ask. The strip spans a whole season that crosses a year boundary, so a bare "Sat 5" stops meaning anything the moment you drag past the fortnight either side of today; and "Yesterday" is what anyone calls yesterday. VoiceOver already spoke all three names — the chips now agree with it |
| 2026-09-06 | The **Today jump leaves the day strip** for a floating button, centred over the slate just above the tab bar, and inverts — `textPrimary` ground under `bgPrimary` ink, dark on light and light on dark. It still only exists while it has somewhere to go | Andy's ask. Pinned to the strip's trailing edge it cost ~70pt of strip on every day but today; floating costs nothing and sits where the thumb already is. Inverted because it is the one control on the page that *changes* the day rather than describing it — the same pairing the selected day chip wears. The strip now runs its full width always |
| 2026-09-06 | **A followed thing gets a card, not a row**: the tables hub's Following section becomes one card per table (drag reorder intact), and the Add teams sheet becomes one card per team with the league headings dropped and the shortlist interleaved across both leagues (`PopularTeams.teams`, proportional round-robin). The hub's league-accordion headers grow to a card's height to match | Andy: one card holding fifteen teams under a heading "feels like a mistake to be leaving certain teams out" — separate cards read as suggestions, which is what they are, and the whole directory is a keystroke away. Interleaved because dropping the headings would otherwise have put fifteen college programs above the first NFL team. Two sizes of card in one stack read as an accident, hence the header height |
| 2026-09-06 | **Team routing is league-qualified end to end** (`TeamRef`), and `statside://team/{league}/{id}` joins the grammar; a bare legacy link prefers a followed team. Search results key on `followKey`, never `Team.id` | Searching "Browns" opened UAB: both are ESPN id 5, the intent carried only the id, and the directory publishes college football first. The same collision one layer up had the Bills' row wearing Auburn's mark (both id 2) — SwiftUI reused one row's state for the other as the query changed. `FollowKey` and `ConferenceID` already knew ids collide; the router and one `ForEach` didn't |
| 2026-09-06 | The poll picker's rows tag with a non-optional `String` (`PollMenuChip.options`) | `Poll.type` is `String?` and the selection binding is `String`, so no tag could ever match: the AP/Coaches chip looked alive and did nothing. The three other menu chips were already correct — `ConferenceTeamFilterChip` even spells its optional tags out, which is the pattern |
| 2026-09-06 | Widget: the kickoff's **day and time split onto their own lines** (`WidgetGame.statusDetail`, optional in the snapshot so old blobs decode), and scores drop to the app's row scale — 13 medium, 13 semibold live, a losing side giving up its colour rather than its weight | "Sun, 9/13 1:00 PM" does not fit a 64pt column at any text size, and the column is fixed on purpose (a `minWidth` made the score column zig-zag). `GameRow` already splits day from time, so the widget borrows the split rather than widening. The 17pt `score` tokens belong to the detail header; in a 60pt card they shouted over the team names beside them |
| 2026-09-06 | **A calendar sheet joins the day strip** — a button beside Live, opening season-bounded month grids (pinned weekday captions, inverted selected day, Today in the toolbar). Supersedes the "no calendar modal" don't | Andy's ask, from FotMob, which ships both. The don't was written when the strip replaced the week strip and the objection was to a *picker standing in for the axis* — the strip is still the axis. Dragging is fine for the fortnight either side of today and hopeless for "the Iron Bowl in November", which is the only thing this answers |
| 2026-09-06 | Team pages **fetch the preseason** (`seasontype=1`, a third parallel request) and split the Games tab into a card per phase — Preseason / Regular Season / Postseason — each rendered only where it has games | Andy's ask. The client only ever asked for types 2 and 3, so preseason never arrived; a fan checking in mid-August had nothing to look at. Splitting is what keeps exhibition football from reading as games that counted, and what lets the regular-season card keep the bye week. "Schedule" was renamed because once the preseason has a card the old name no longer says which games are in this one — and calling a card "Regular Season" while it held bowl games would have been a new lie. Note: kickoff reminders now see preseason games, against the 64-pending cap |
| 2026-09-06 | The Top 25 wears **college football's mark, not a trophy** — the hub row, the poll page's hero, and the Scores section header alike (`FollowedTable.logoURL`) | Andy: "Top 25 is going to become confusing" as sports are added. The name never said whose, which is fine while one league polls and wrong the moment a second one does |
| 2026-09-06 | Conference standings mark the **championship cut with a bar down the qualifying rows' leading edge plus a keyed legend** (FotMob's promotion/relegation mechanism), replacing the full-bleed divider. Ink, not colour | Andy's ask. The divider could only say where the line fell; the bar says which teams are in, and survives a scroll that leaves the line off screen. The budget stays at three — and VoiceOver hears "in the championship game" in the row's own sentence, since it reads no edges |
| 2026-09-06 | **The postseason becomes its own tab, drawn as a bracket** on the NFL's league page and the Top 25: compact match cards in two fitted columns joined by hairline connectors, a round chip row, and a threshold swipe between rounds. Deliberately not the card-list format every other pane uses — a round of games is a list, but a bracket is a shape, and the shape is the information | Andy's ask from the ESPN playoff-bracket and FotMob knockout references. Costs no request: the Games tab already fetches the whole season, and the playoff games stay there too, in date order — the bracket answers "who plays whom", the Games tab answers "when". The columns are sized to fit rather than scrolling sideways, which is what frees the horizontal axis for the round swipe; the entity pages' tab swipe stands down on this tab. The swipe is threshold-only, not finger-tracked like the day swipe: a tracking gesture must claim the drag from its first movement and the page's vertical ScrollView wins that every time — the day swipe can track because it is attached outside its scroll view |
| 2026-09-06 | **Bracket connectors are earned, never assumed**: a line is drawn only where a completed game's winner turns up in a later game, and a team that reached a round without playing the previous one is shown as a **bye** entry of its own. Each next-round game sits level with the midpoint of its own sources | The first cut laid both columns out chronologically and the result "doesn't make sense with how the advancements occur" (Andy): a winner's line crossed the column to reach whichever game the calendar put it beside, and the top seeds appeared in the second column having never been in the first. ESPN publishes no bracket tree, so the alternative to reading advancement off results is assuming games 1 and 2 feed the next round's game 1 — wrong the moment a format reseeds, and wrong invisibly. An unplayed round therefore draws nothing and wires itself up as results land; byes are only synthesised where a result already connects the two rounds, so no phantom byes appear where bowls precede a playoff |
| 2026-09-06 | Full-width tappable surfaces on the Scores swipe pane take a new `SwipeSafeButtonStyle` (`.buttonStyle(.swipeSafe)`) instead of `.plain` — the game row, the conference-name link and the accordion toggle. `.plain` fires on touch-up whenever the touch is still inside the button, and a full-width row is *wider* than any swipe, so the day swipe committed and the row's `NavigationLink` pushed a detail page on the way out. The style triggers on a `TapGesture` instead, whose movement tolerance is the system's own | Found cutting 2.1: `SmokeUITests` failed, and a hand-driven swipe (no accessibility querying, so not the iOS 26.5 ghost-activation artifact) reproduced it on 2.1 **and on a build of `eb4ad45`** — so it shipped in 2.0 and is live. Vertical drags were never affected: the enclosing ScrollView claims those and cancels the press. Chosen over a `.disabled()` flag driven from the pane's drag state, which would have to reach every row through the environment and flip mid-drag, invalidating the whole scores tree on the very gesture the E5 pass made cheap. Narrow capsules (Retry, the follow prompt, the day chips) keep `.plain` — a swipe leaves their bounds, so they cancel on their own |
| 2026-09-06 | The postseason tab is **the playoff only** — college football's bowls are excluded (a leftover rule, so "Quarterfinal at the Rose Bowl" still reads as a quarterfinal) — and the NFL's **Pro Bowl hangs beneath the final** as an exhibition rather than taking a round chip | Andy: "reserve that tab just for the playoffs", and the Pro Bowl "stack underneath the superbowl kinda how fotmob shows the bronze metal". Thirty-eight exhibitions under a "Bowls" chip were the biggest thing on a screen whose subject is a twelve-team bracket, and nothing about a bowl is bracket-shaped. ESPN files the Pro Bowl as `seasontype=3` week 4; a chip of its own put an all-star game between the conference championships and the Super Bowl, which is the one thing a bracket must never imply |
| 2026-09-06 | Game detail gains a **Plays tab** — every possession newest-first, each an accordion row expanding into its plays — and the **Drives card moves into it** (supersedes the 2026-09-05 line about not splitting Drives off the default tab). One card of accordion rows rather than a card per drive: a full game is 22 possessions, and the density target is the reason. Scoring narrows with the Games tabs' own control language — All plays / Scoring as two toggles answering one question. Tabs are now per-availability, so a game with no drives shows no Plays tab and a pre-kick game still shows Summary alone | Andy, from ESPN Play-by-Play and FotMob Commentary side-by-sides. Both reference apps expand a drive into its plays; ours stopped at "Punt · 5 plays, 20 yards, 2:39" because `drives.previous[].plays[]` was decoded away. The 2026-09-05 objection was that splitting Drives off would move a primary surface to no one's benefit — that held while the drive log was all there was, and stops holding once collapsed drives *are* the play list's top level; leaving it on Summary would print the same rows in two tabs |
| 2026-09-06 | A live game's detail page gets the **Gamecast strip** — possession mark, down and distance, the spot, the drive so far, the last play, over a monochrome field bar — leading the Summary tab. Derived entirely from `drives.current`'s last play (`end.shortDownDistanceText` / `possessionText` / `yardsToEndzone`), not from a second live-only payload, and ESPN drops `drives.current` at final, so the card retires itself with no clock check. The bar draws the away end zone left and the home right, matching the header's logo order, with an arrow for the drive's direction — `yardsToEndzone` counts toward whichever end zone the *offense* attacks, so the same number is opposite ends of the bar depending on possession | The Scores row has shown possession since 1.0 while the detail page — the screen you open *because* the game is live — showed a clock and nothing else. Monochrome because the header already carries the live dot and a field this small reads on position: the budget's own rule that a design wanting color usually wants spacing. **`drives.current` is reasoned from ESPN's live shape, not probed** — the captured fixture is a final game and ships none — so the strip needs an eyes-on check on a live Saturday; every field is optional, so a wrong guess is a missing card, not a crash |
| 2026-09-07 | **A day swipe commits its day on the frame the thumb lifts**, not when the slide lands: `LeagueScoreboards.select(day:)` splits into a synchronous `show(day:)` and an async `loadSelectedDay()`, and the panes keep drawing the outgoing day from a new `ScoresScreen.settlingFrom` for the ~0.3s it spends leaving | Andy: "there is a second delay before the day in the strip changes to the accurate day." The commit was living in the settle animation's completion, so the strip, the header and the Today button all waited out a 0.32s spring plus the fetch's task hop before agreeing with the gesture. Separating the two lets the model move now and the picture catch up — the panes need a day of their own precisely because the selected one is no longer the one on screen |
| 2026-09-07 | A **team page's Standings tab gets the league/conference/division scope chip** — the NFL's team pages only. Where a conference page scopes down into what it contains, a team page scopes out into what contains it (`StandingsScope.scopes(forTeamIn:)` walks the team's own chain), so Division is the team's own four, Conference its sixteen, League the whole thirty-two — every scope a table with the team's row in it. The pane still opens on the conference, and the chip's ink becomes `isNarrower(than:)`: scoping out reads as quiet as the default, only a narrowed table wears the fill | Andy: "team standings page needs the league/conference/division selector that the league has." A Browns page tabling the AFC could answer neither "who's winning the North" nor "where do we sit in the league" — the two questions a division race is made of. A team sits in the middle of its own hierarchy rather than at the top of it, which is why the conference page's widest-view-of-itself default has no meaning here and the ink rule had to learn the difference |
| 2026-09-07 | **Scores section headers tag their league** — a quiet uppercase `League.shortName` caption riding the name ("ACC CFB", "Top 25 CFB", "Other CFB"), on every section that has a league and doesn't already say it: the NFL's own section is titled "NFL" and Following spans both, so neither takes one. VoiceOver hears the long form ("ACC, college football, 8 games") | Andy: the college football accordions need "NCAAF or CFB or something similar to anchor it to." Breaking college football back down by conference (2026-09-06) left every header on the page naming a conference and none of them naming a sport — "ACC" is only obviously college football to someone who already knows. Reuses the caption the game rows and search results already tag with, so the app has one word for a league everywhere |

| 2026-09-06 | The Game info card gets the venue treatment FotMob gives it: stadium name in ink over its city in meta gray, then the crowd as label/value pairs — Capacity beside Surface before kickoff, **attendance leading with capacity as its context** and an ink fill meter beneath once ESPN publishes one. Capacity itself comes from the **core API's venue resource** (`/venues/{id}`, reached via the venue id the site payload does carry), memoized per venue for the client's life; the card's pre-kick and played builders merge into one `GameInfoRows` | Andy's ask. The capacity row shipped with the 2026-08-25 preview cards and has never rendered once: ESPN's site API ships no `capacity` on any venue object, in either league, pre-game or final — so the field was decoded, always nil, and silently absent. The meter is ink, not FotMob's green: a meter is chrome, so the budget holds at three. One extra request per venue per session, never per 30s poll |

## Don'ts

- Don't add colors beyond the budget. If a design problem seems to need color, it needs weight, size, or spacing instead.
- Don't dedupe games across sections.
- Don't let a date picker *replace* the day strip. The strip is the Scores screen's axis (2026-09-05); the calendar sheet added 2026-09-06 is a jump-to shortcut beside it, bounded by the same season, and supersedes this don't's original "not a calendar modal" clause.
- Don't add third-party packages, analytics, or a backend without an explicit conversation first.
- Don't edit `project.pbxproj` file references by hand — synchronized groups make file management automatic. Build settings, and target/build-phase additions when adding a new target, are allowed (amended 2026-08-04 for the widget extension); always verify with `plutil -lint` + a full build.
- Don't build for iPad/Mac/Vision idioms in v1.
- Don't start coding a feature that isn't in BACKLOG.md — add it to the backlog and discuss first.
