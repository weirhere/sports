import { SettingsView } from "@/components/settings-view";

export const metadata = {
  title: "Settings | College Football Hub",
};

export default function SettingsPage() {
  return <SettingsView />;
}
